---
type: markdown
title: Kia EV6
content: >-
  ## Kia EV6 GT-Line RWD

  Snow White Pearl reference vehicle with a dashboard shell ready for mapped
  vehicle data. This card will consume `vehicle`, `battery`, `location`, and
  `controls` mapping keys as implementation cards mature.
card_mod:
  style: |
    ha-card {
      background: linear-gradient(135deg, var(--kia-surface-raised), var(--kia-surface-elevated));
      border: 1px solid var(--kia-border-subtle);
      border-radius: var(--kia-radius-large);
      box-shadow: var(--kia-shadow-raised);
      color: var(--kia-text-primary);
    }
