---
type: custom:decluttering-card
template: kia_mapped_section_note
variables:
  - title: History
  - content: >-
      Energy history will collect chart-ready metrics once statistics entities
      or helper sensors are added to the mapping layer. Keep historical charts
      here rather than on Overview.
