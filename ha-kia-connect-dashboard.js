const KIA_DASHBOARD_CARD_VERSION = "2.16.0";
const KIA_DASHBOARD_NL = {
  "AC charge target": "AC-laaddoel",
  "AC charging limit": "AC-laadlimiet",
  "AC target": "AC-doel",
  "Access": "Toegang",
  "Action cancelled": "Actie geannuleerd",
  "Active": "Actief",
  "Active theme": "Actief thema",
  "Adjust target": "Doel aanpassen",
  "All configured entities are reporting usable states.": "Alle geconfigureerde entiteiten melden bruikbare statussen.",
  "All systems normal": "Alle systemen normaal",
  "Appearance": "Uiterlijk",
  "Attention": "Aandacht vereist",
  "Available": "Beschikbaar",
  "Available battery signals": "Beschikbare batterijsignalen",
  "Awaiting action-safety review": "Wacht op veiligheidscontrole van acties",
  "Back to Overview": "Terug naar Overzicht",
  "Based on EUR": "Gebaseerd op EUR",
  "Battery": "Batterij",
  "Battery details": "Batterijdetails",
  "Battery health": "Batterijconditie",
  "Battery condition": "Batterijconditie",
  "Traction battery and auxiliary system": "Tractiebatterij en hulpsysteem",
  "Battery level": "Batterijniveau",
  "Cabin": "Interieur",
  "Cabin comfort": "Interieurcomfort",
  "Cabin target": "Gewenste interieurtemperatuur",
  "Cable state": "Kabelstatus",
  "Card styling": "Kaartopmaak",
  "Cargo opening": "Kofferopening",
  "Charge controls": "Laadbediening",
  "Charge limits": "Laadlimieten",
  "Charge port": "Laadpoort",
  "Charger control": "Laadpuntbediening",
  "Charger power": "Laadpuntvermogen",
  "Charger status": "Laadpuntstatus",
  "Charging": "Bezig met laden",
  "Charging current": "Laadstroom",
  "Charging finished": "Laden voltooid",
  "Charging history": "Laadgeschiedenis",
  "Charging days": "Laaddagen",
  "Cost": "Kosten",
  "30 days": "30 dagen",
  "90 days": "90 dagen",
  "1 year": "1 jaar",
  "Charging history unavailable": "Laadgeschiedenis niet beschikbaar",
  "Charging power": "Laadvermogen",
  "Charging session": "Laadsessie",
  "Charging state": "Laadstatus",
  "Check configured entities": "Controleer geconfigureerde entiteiten",
  "Climate": "Klimaat",
  "Climate command state": "Status klimaatopdracht",
  "Climate details": "Klimaatdetails",
  "Climate system": "Klimaatsysteem",
  "Closed": "Gesloten",
  "Color mode": "Kleurmodus",
  "Comfort": "Comfort",
  "Commands use the mapped Home Assistant entities and ask for confirmation.": "Opdrachten gebruiken de toegewezen Home Assistant-entiteiten en vragen om bevestiging.",
  "Configuration": "Configuratie",
  "Configured charging targets": "Geconfigureerde laaddoelen",
  "Connected": "Verbonden",
  "Consumed": "Verbruikt",
  "Connection": "Verbinding",
  "Connection health": "Verbindingsstatus",
  "Connection diagnostics": "Verbindingsdiagnose",
  "Connector current": "Aansluitstroom",
  "Coordinates unavailable": "Coördinaten niet beschikbaar",
  "Current driving estimate": "Huidige rijbereikschatting",
  "Daily driving history": "Dagelijkse rijgeschiedenis",
  "Distance today": "Afstand vandaag",
  "Drive mode": "Rijmodus",
  "Driving analytics": "Rij-analyse",
  "Driving context": "Rij-informatie",
  "Current state": "Huidige status",
  "Dashboard actions": "Dashboardacties",
  "Dashboard administration": "Dashboardbeheer",
  "Dashboard version": "Dashboardversie",
  "Data current": "Gegevens actueel",
  "Data delayed": "Gegevens vertraagd",
  "Data stale": "Gegevens verouderd",
  "Dark mode": "Donkere modus",
  "Data provided by Kia Connect": "Gegevens geleverd door Kia Connect",
  "DC charging limit": "DC-laadlimiet",
  "DC target": "DC-doel",
  "Defrost": "Ontdooien",
  "Disconnected": "Niet verbonden",
  "Door": "Deur",
  "Door lock": "Deurvergrendeling",
  "Doors": "Deuren",
  "Doors &amp; openings": "Deuren &amp; openingen",
  "Enabled": "Ingeschakeld",
  "Energy": "Energie",
  "Energy details": "Energiedetails",
  "Energy price": "Energieprijs",
  "Energy history": "Energiegeschiedenis",
  "Energy used": "Verbruikte energie",
  "Energy regenerated": "Teruggewonnen energie",
  "Entity mapping": "Entiteitstoewijzing",
  "Estimated cost": "Geschatte kosten",
  "Estimated range": "Geschat bereik",
  "Estimated remaining range": "Geschat resterend bereik",
  "EV battery overview": "Overzicht EV-batterij",
  "Exterior lights": "Buitenverlichting",
  "For unavailable data, verify the entity in Home Assistant and this card's entity mapping before troubleshooting the layout.": "Controleer bij niet-beschikbare gegevens eerst de entiteit in Home Assistant en de entiteitstoewijzing van deze kaart voordat je de lay-out onderzoekt.",
  "Front left": "Linksvoor",
  "Front right": "Rechtsvoor",
  "Freshness": "Actualiteit",
  "From mapped session cost": "Uit toegewezen sessiekosten",
  "Grid power": "Netvermogen",
  "Grid support": "Netondersteuning",
  "Headlights": "Koplampen",
  "History helpers ready": "Geschiedenishulpen gereed",
  "Map charger_total_energy to load permanent Home Assistant energy statistics.": "Wijs charger_total_energy toe om permanente energiestatistieken van Home Assistant te laden.",
  "Home Assistant theme": "Home Assistant-thema",
  "Home charger": "Thuislaadpunt",
  "Home charger connection": "Verbinding thuislaadpunt",
  "Home charging": "Thuisladen",
  "Home consumption": "Thuisverbruik",
  "Home energy": "Thuisenergie",
  "Hood": "Motorkap",
  "HVAC mode": "HVAC-modus",
  "Inactive": "Inactief",
  "Initializing": "Initialiseren",
  "Items to review": "Te controleren punten",
  "Last known location": "Laatst bekende locatie",
  "Last parked": "Laatst geparkeerd",
  "Last updated": "Laatst bijgewerkt",
  "Last vehicle update": "Laatste voertuigupdate",
  "Latest scan": "Laatste controle",
  "Latest session": "Laatste sessie",
  "Latest session and totals": "Laatste sessie en totalen",
  "Loading charging history": "Laadgeschiedenis laden",
  "Long-term statistics": "Langetermijnstatistieken",
  "Light mode": "Lichte modus",
  "Lights": "Verlichting",
  "Live connection context": "Live verbindingsinformatie",
  "Live session": "Live sessie",
  "Location": "Locatie",
  "Location details": "Locatiedetails",
  "Location unavailable": "Locatie niet beschikbaar",
  "Lock": "Vergrendeling",
  "Locked": "Vergrendeld",
  "Locks &amp; lights": "Vergrendelingen &amp; verlichting",
  "Low risk": "Laag risico",
  "Maintenance feedback": "Onderhoudsfeedback",
  "Map preview needs tracker coordinates": "Kaartvoorbeeld vereist trackercoördinaten",
  "Mapping healthy": "Toewijzing in orde",
  "Mapping needs attention": "Toewijzing vereist aandacht",
  "Monthly cost": "Maandelijkse kosten",
  "No charging days in this period": "Geen laaddagen in deze periode",
  "No": "Nee",
  "No active dashboard warnings. Review Vehicle and Settings after each Home Assistant update.": "Geen actieve dashboardwaarschuwingen. Controleer Voertuig en Instellingen na elke Home Assistant-update.",
  "No active warnings": "Geen actieve waarschuwingen",
  "No command result mapped": "Geen opdrachtresultaat toegewezen",
  "No entities configured": "Geen entiteiten geconfigureerd",
  "No mapping issues detected": "Geen toewijzingsproblemen gevonden",
  "No timestamp available": "Geen tijdstip beschikbaar",
  "Normal": "Normaal",
  "Not charging": "Niet aan het laden",
  "Not configured": "Niet geconfigureerd",
  "Not connected": "Niet verbonden",
  "Not mapped": "Niet toegewezen",
  "Odometer": "Kilometerstand",
  "Off": "Uit",
  "Offline": "Offline",
  "On": "Aan",
  "Online": "Online",
  "Open": "Open",
  "Open the mapped raw-data entity": "Open de toegewezen ruwegegevensentiteit",
  "Open tracker details": "Open trackerdetails",
  "Outside": "Buiten",
  "Overview": "Overzicht",
  "Parking context": "Parkeerinformatie",
  "Paused": "Gepauzeerd",
  "Paused by charger": "Gepauzeerd door laadpunt",
  "Perimeter": "Omtrek",
  "Plug connected": "Stekker aangesloten",
  "Port state": "Poortstatus",
  "Position context": "Positie-informatie",
  "Power flow context": "Energiestroom",
  "Prepare the cabin": "Bereid het interieur voor",
  "Quick Actions": "Snelle acties",
  "Range": "Bereik",
  "Range and connection": "Bereik en verbinding",
  "Range context": "Bereiksinformatie",
  "Read only": "Alleen-lezen",
  "Ready for future trip data": "Klaar voor toekomstige ritgegevens",
  "Ready to charge": "Klaar om te laden",
  "Ready-state check": "Controle vóór vertrek",
  "Rear left": "Linksachter",
  "Rear right": "Rechtsachter",
  "Regenerated": "Teruggewonnen",
  "Refresh Data": "Gegevens vernieuwen",
  "Refresh vehicle": "Voertuig vernieuwen",
  "Remote climate session": "Klimaatsessie op afstand",
  "Remote controls": "Bediening op afstand",
  "Request current Kia data": "Vraag actuele Kia-gegevens op",
  "Resume": "Hervatten",
  "Road contact": "Wegcontact",
  "Service opening": "Serviceopening",
  "Session energy": "Sessieverbruik",
  "Session state": "Sessiestatus",
  "Settings": "Instellingen",
  "Settings detail": "Instellingendetails",
  "Solar production": "Zonneproductie",
  "Start": "Starten",
  "Start charging": "Laden starten",
  "Start Charging": "Laden starten",
  "Start Climate": "Klimaat starten",
  "State of charge": "Laadniveau",
  "Steering wheel heat": "Stuurverwarming",
  "Rear window heat": "Achterruitverwarming",
  "Seat comfort": "Zetelcomfort",
  "Driver seat": "Bestuurderszetel",
  "Passenger seat": "Passagierszetel",
  "Rear left seat": "Zetel linksachter",
  "Rear right seat": "Zetel rechtsachter",
  "Driver seat heating": "Bestuurderszetel verwarmen",
  "Passenger seat heating": "Passagierszetel verwarmen",
  "Rear left seat heating": "Zetel linksachter verwarmen",
  "Rear right seat heating": "Zetel rechtsachter verwarmen",
  "Driver seat ventilation": "Bestuurderszetel ventileren",
  "Passenger seat ventilation": "Passagierszetel ventileren",
  "Rear left seat ventilation": "Zetel linksachter ventileren",
  "Rear right seat ventilation": "Zetel rechtsachter ventileren",
  "Climate schedule": "Klimaatschema",
  "Next departure": "Volgend vertrek",
  "Departure schedule 1": "Vertrekprogramma 1",
  "Departure schedule 2": "Vertrekprogramma 2",
  "Departure time 1": "Vertrektijd 1",
  "Departure time 2": "Vertrektijd 2",
  "Departure days 1": "Vertrekdagen 1",
  "Departure days 2": "Vertrekdagen 2",
  "Schedule and departure context": "Schema- en vertrekinformatie",
  "Open details": "Details openen",
  "Ready": "Gereed",
  "Turn off": "Uitschakelen",
  "Turn on": "Inschakelen",
  "Activate rear window heat?": "Achterruitverwarming activeren?",
  "Deactivate rear window heat?": "Achterruitverwarming uitschakelen?",
  "Change climate comfort setting?": "Klimaatcomfortinstelling wijzigen?",
  "Stop": "Stoppen",
  "Stop charging": "Laden stoppen",
  "Stop Climate": "Klimaat stoppen",
  "Stopped": "Gestopt",
  "Summary": "Samenvatting",
  "System state": "Systeemstatus",
  "Temperature": "Temperatuur",
  "This month": "Deze maand",
  "This year": "Dit jaar",
  "Today&apos;s driving": "Ritgegevens van vandaag",
  "Total regeneration": "Totale regeneratie",
  "of consumption": "van het verbruik",
  "This week": "Deze week",
  "Average per charging day": "Gemiddeld per laaddag",
  "Estimated period cost": "Geschatte periodekosten",
  "Show daily charging data": "Dagelijkse laadgegevens tonen",
  "Hide daily charging data": "Dagelijkse laadgegevens verbergen",
  "Reading permanent Home Assistant energy statistics.": "Permanente energiestatistieken van Home Assistant worden geladen.",
  "From Home Assistant Recorder": "Uit Home Assistant Recorder",
  "Tire pressure": "Bandenspanning",
  "Tire Status": "Bandenstatus",
  "Tire status": "Bandenstatus",
  "Today": "Vandaag",
  "Top view": "Bovenaanzicht",
  "Top view of vehicle": "Bovenaanzicht van voertuig",
  "Total imported": "Totaal afgenomen",
  "Tracker": "Tracker",
  "Tracker entity not configured": "Trackerentiteit niet geconfigureerd",
  "Trip context": "Ritgegevens",
  "Trunk": "Kofferbak",
  "Unavailable": "Niet beschikbaar",
  "Unlocked": "Ontgrendeld",
  "Updated": "Bijgewerkt",
  "Vehicle connection": "Voertuigverbinding",
  "Vehicle": "Voertuig",
  "Vehicle charging power": "Laadvermogen voertuig",
  "Vehicle charging state": "Laadstatus voertuig",
  "Vehicle checks normal": "Voertuigcontroles normaal",
  "Vehicle data details": "Details voertuiggegevens",
  "Vehicle data incomplete": "Voertuiggegevens onvolledig",
  "Vehicle data updated within the expected interval.": "Voertuiggegevens zijn binnen het verwachte interval bijgewerkt.",
  "Vehicle data is arriving later than expected; wait for the next Kia scan.": "Voertuiggegevens komen later dan verwacht; wacht op de volgende Kia-controle.",
  "No recent Kia data. Check Kia Connect authentication, integration repairs, or a Kia cloud outage.": "Geen recente Kia-gegevens. Controleer Kia Connect-authenticatie, integratiereparaties of een Kia-cloudstoring.",
  "The last-updated entity is unavailable. Check the Kia integration and card mapping.": "De entiteit voor de laatste update is niet beschikbaar. Controleer de Kia-integratie en kaarttoewijzing.",
  "Map last_updated to monitor Kia data freshness.": "Wijs last_updated toe om de actualiteit van Kia-gegevens te bewaken.",
  "Home charger reports an active connection.": "Het thuislaadpunt meldt een actieve verbinding.",
  "Home charger reports that it is offline.": "Het thuislaadpunt meldt dat het offline is.",
  "The charger connection entity is unavailable.": "De verbindingsentiteit van het laadpunt is niet beschikbaar.",
  "Map charger_online to monitor the home charger separately.": "Wijs charger_online toe om het thuislaadpunt afzonderlijk te bewaken.",
  "Kia authentication state is not exposed to dashboard cards. Stale data can indicate reauthentication or a Kia cloud outage.": "De Kia-authenticatiestatus is niet beschikbaar voor dashboardkaarten. Verouderde gegevens kunnen wijzen op herauthenticatie of een Kia-cloudstoring.",
  "Just now": "Zojuist",
  "min ago": "min geleden",
  "h ago": "u geleden",
  "d ago": "d geleden",
  "Vehicle detail": "Voertuigdetails",
  "Vehicle energy": "Voertuigenergie",
  "Vehicle plug": "Voertuigstekker",
  "Vehicle position": "Voertuigpositie",
  "Vehicle security": "Voertuigbeveiliging",
  "Vehicle Status": "Voertuigstatus",
  "Window": "Raam",
  "Windows": "Ramen",
  "Yes": "Ja",
  "Range, charging state, limits, and cable status in one focused view.": "Bereik, laadstatus, laadlimieten en kabelstatus in één overzicht.",
  "Range changes with temperature, driving style, speed, and climate use.": "Het bereik verandert door temperatuur, rijstijl, snelheid en klimaatgebruik.",
  "Review access points, exterior lights, and tire status before departure.": "Controleer vóór vertrek de toegangspunten, buitenverlichting en bandenstatus.",
  "Review temperature, HVAC state, comfort features, and remote controls.": "Controleer temperatuur, HVAC-status, comfortfuncties en bediening op afstand.",
  "Remote lock and light commands remain disabled pending the action-safety review.": "Opdrachten voor vergrendeling en verlichting blijven uitgeschakeld totdat de veiligheidscontrole van acties is afgerond.",
  "State-of-health and battery temperature will appear when mapped entities become available.": "Batterijconditie en batterijtemperatuur verschijnen zodra toegewezen entiteiten beschikbaar zijn.",
  "Charging history and estimated completion can be added when the integration exposes those entities.": "Laadgeschiedenis en een geschatte eindtijd kunnen worden toegevoegd zodra de integratie die entiteiten beschikbaar stelt.",
  "Timer and requested mode will appear when mapped state is available.": "Timer en gevraagde modus verschijnen zodra de toegewezen status beschikbaar is.",
  "Vehicle and charger in one energy view": "Voertuig en laadpunt in één energieoverzicht",
  "Follow the live charging session, choose a charging strategy, and compare home energy flows without coupling the dashboard to one charger brand.": "Volg de live laadsessie, kies een laadstrategie en vergelijk energiestromen thuis zonder het dashboard aan één laadpuntmerk te koppelen.",
  "Vehicle data remains available when no separate home charger is configured. The Battery page owns vehicle charge limits.": "Voertuiggegevens blijven beschikbaar wanneer geen afzonderlijk thuislaadpunt is geconfigureerd. De pagina Batterij beheert de laadlimieten van het voertuig.",
  "Smart and Solar remain charger-defined strategies; this card only reflects and selects the mapped Home Assistant mode.": "Smart en Solar blijven door het laadpunt bepaalde strategieën; deze kaart toont en selecteert alleen de toegewezen Home Assistant-modus.",
  "Map daily, weekly, or monthly utility-meter sensors to replace this placeholder with actual totals.": "Wijs dagelijkse, wekelijkse of maandelijkse energiemeters toe om deze tijdelijke aanduiding door werkelijke totalen te vervangen.",
  "The tracker state is the latest known parking area. Update freshness stays visible alongside it.": "De trackerstatus is de laatst bekende parkeerlocatie. Het tijdstip van de update blijft ernaast zichtbaar.",
  "Route, destination, and movement details remain read-only placeholders until mapped entities are defined.": "Route-, bestemmings- en bewegingsgegevens blijven alleen-lezen aanduidingen totdat toegewezen entiteiten zijn gedefinieerd.",
  "Review entity mapping, theme context, safe actions, and integration feedback for this card.": "Controleer de entiteitstoewijzing, het thema, veilige acties en integratieterugkoppeling voor deze kaart.",
  "Refresh requests use the configured refresh entity. Vehicle details remain a local, read-only Home Assistant surface.": "Vernieuwingsverzoeken gebruiken de geconfigureerde vernieuwingsentiteit. Voertuigdetails blijven een lokale, alleen-lezen Home Assistant-weergave.",
  "Semantic Home Assistant tokens": "Semantische Home Assistant-tokens",
  "Charging strategy": "Laadstrategie",
  "EVSE status": "EVSE-status",
  "Est. range AC on": "Geschat bereik met AC aan",
  "Pause": "Pauzeren",
  "Remote climate actions remain disabled pending safety review": "Klimaatacties op afstand blijven uitgeschakeld tot de veiligheidscontrole is afgerond",
  "Set charger_controls: true after reviewing the mapped action entities to enable controls.": "Stel charger_controls: true in nadat de toegewezen actie-entiteiten zijn gecontroleerd om de bediening in te schakelen.",
  "Change the home charger current limit?": "De stroomlimiet van het thuislaadpunt wijzigen?",
  "Start climate now?": "Klimaat nu starten?",
  "Stop climate now?": "Klimaat nu stoppen?",
  "Start charging now?": "Laden nu starten?",
  "Stop charging now?": "Laden nu stoppen?",
  "Start the home charger now?": "Het thuislaadpunt nu starten?",
  "Pause the home charger now?": "Het thuislaadpunt nu pauzeren?",
  "Resume the home charger now?": "Het thuislaadpunt nu hervatten?",
  "Stop the home charging session now?": "De thuislaadsessie nu stoppen?",
  "Change charger mode to": "Laadmodus wijzigen naar",
  "Resume the home charger in": "Het thuislaadpunt hervatten in",
  "Previous charger mode is unknown. Choose Standard, Smart, or Solar to resume safely.": "De vorige laadmodus is onbekend. Kies Standard, Smart of Solar om veilig te hervatten.",
  "Action entity missing:": "Actie-entiteit ontbreekt:",
  "Unsupported action entity:": "Niet-ondersteunde actie-entiteit:",
  "Action failed for": "Actie mislukt voor",
  "PIN verification failed in the Kia integration.": "PIN-verificatie in de Kia-integratie mislukt.",
  "Cannot adjust": "Kan niet aanpassen:",
  "expected number entity.": "een number-entiteit werd verwacht.",
  "Cannot select an option on": "Kan geen optie selecteren op",
  "expected select entity.": "een select-entiteit werd verwacht.",
  "Sent": "Verzonden",
  "Set": "Ingesteld",
  "Coming in 2.0.0": "Beschikbaar in 2.0.0",
  "The vehicle hero and section navigation remain available above.": "De voertuigheader en sectienavigatie blijven hierboven beschikbaar.",
  "Controls remain disabled until reviewed feedback through <code>last_charging_result</code> is available.": "De bediening blijft uitgeschakeld totdat gecontroleerde terugkoppeling via <code>last_charging_result</code> beschikbaar is.",
  "Vehicle doors are unlocked.": "De voertuigdeuren zijn ontgrendeld.",
  "Headlights are on.": "De koplampen zijn aan.",
  "Vehicle data is incomplete:": "De voertuiggegevens zijn onvolledig:",
  "missing or unavailable. Check Settings for vehicle mappings.": "ontbreekt of is niet beschikbaar. Controleer Instellingen voor voertuigtoewijzingen.",
  "No active warnings in available data, but": "Geen actieve waarschuwingen in de beschikbare gegevens, maar",
  "Locks, openings, lights, charge port, and tire pressure report normal states.": "Vergrendelingen, openingen, verlichting, laadpoort en bandenspanning melden normale statussen.",
  "Entity IDs stay in this card's <code>entities</code> configuration. Missing or unavailable mappings are listed here before card changes are considered.": "Entiteits-ID's blijven in de <code>entities</code>-configuratie van deze kaart. Ontbrekende of niet-beschikbare toewijzingen worden hier vermeld voordat kaartwijzigingen worden overwogen.",
  "Theme": "Thema",
  "Charging command state": "Status laadopdracht",
  "Map charger_energy_price, charger_session_cost, or expose a tariff attribute": "Wijs charger_energy_price of charger_session_cost toe, of stel een tariefattribuut beschikbaar",
  "from mapped energy price": "uit de toegewezen energieprijs",
  " is open.": " staat open.",
  " tire pressure needs attention.": " bandenspanning vereist aandacht.",
  "; data incomplete": "; gegevens onvolledig",
  "Capacity, energy, and pack condition": "Capaciteit, energie en accupakketconditie",
  "State of health": "Batterijconditie",
  "Remaining energy": "Resterende energie",
  "Nominal capacity": "Nominale capaciteit",
  "Pack voltage": "Accuspanning",
  "12V battery": "12V-accu",
  "Auxiliary battery condition": "Conditie hulpaccu",
  "Fault state": "Storingsstatus",
  "Fault detected": "Storing gedetecteerd",
  "No fault": "Geen storing",
  "Thermal management": "Thermisch beheer",
  "Battery temperatures and conditioning": "Batterijtemperaturen en conditionering",
  "Minimum temperature": "Minimumtemperatuur",
  "Maximum temperature": "Maximumtemperatuur",
  "Coolant temperature": "Koelvloeistoftemperatuur",
  "Battery heating": "Batterijverwarming",
  "Preconditioning": "Preconditionering",
  "Winter mode": "Wintermodus",
  "Heater power": "Vermogen batterijverwarming",
  "Estimated charge duration": "Geschatte laadduur",
  "Charging history remains on Energy; this panel focuses on the current battery session.": "Laadgeschiedenis blijft op Energie; dit paneel focust op de huidige batterijsessie.",
  "Average energy consumption": "Gemiddeld energieverbruik",
  "90-day energy consumption": "Energieverbruik over 90 dagen",
  "Smart key battery": "Smart Key-batterij",
  "Key status": "Sleutelstatus",
  "Replace battery": "Batterij vervangen",
  "Smart key battery needs attention.": "De Smart Key-batterij moet worden vervangen.",
  "Vent all windows": "Alle ramen ventileren",
  "Vent all windows now?": "Alle ramen nu ventileren?",
  "Window controls": "Raambediening",
  "Close": "Sluiten",
  "Open front left window?": "Raam linksvoor openen?",
  "Close front left window?": "Raam linksvoor sluiten?",
  "Open front right window?": "Raam rechtsvoor openen?",
  "Close front right window?": "Raam rechtsvoor sluiten?",
  "Open rear left window?": "Raam linksachter openen?",
  "Close rear left window?": "Raam linksachter sluiten?",
  "Open rear right window?": "Raam rechtsachter openen?",
  "Close rear right window?": "Raam rechtsachter sluiten?",
  "Lock the vehicle now?": "Voertuig nu vergrendelen?",
  "Unlock the vehicle now?": "Voertuig nu ontgrendelen?",
  "Lock vehicle": "Voertuig vergrendelen",
  "Unlock vehicle": "Voertuig ontgrendelen",
  "Lock command sent; waiting for vehicle status.": "Vergrendelopdracht verzonden; wachten op voertuigstatus.",
  "Unlock command sent; waiting for vehicle status.": "Ontgrendelopdracht verzonden; wachten op voertuigstatus.",
  "Vehicle status refresh requested; waiting for the updated lock state.": "Vernieuwing van de voertuigstatus aangevraagd; wachten op de bijgewerkte vergrendelstatus.",
  "Automatic vehicle status refresh failed; waiting for Home Assistant to update.": "Automatisch vernieuwen van de voertuigstatus is mislukt; wachten tot Home Assistant de status bijwerkt.",
  "Vehicle locked successfully": "Voertuig succesvol vergrendeld",
  "Vehicle unlocked successfully": "Voertuig succesvol ontgrendeld",
  "Lock command was sent, but the vehicle did not report locked before the timeout.": "De vergrendelopdracht is verzonden, maar het voertuig meldde niet tijdig dat het vergrendeld was.",
  "Unlock command was sent, but the vehicle did not report unlocked before the timeout.": "De ontgrendelopdracht is verzonden, maar het voertuig meldde niet tijdig dat het ontgrendeld was.",
  "Vehicle is already locked": "Voertuig is al vergrendeld",
  "Vehicle is already unlocked": "Voertuig is al ontgrendeld",
  "Vehicle lock status is unavailable; command not sent.": "De vergrendelstatus van het voertuig is niet beschikbaar; opdracht niet verzonden.",
  "Vehicle lock controls are not enabled or not mapped to a lock entity.": "De vergrendelbediening is niet ingeschakeld of niet aan een lock-entiteit toegewezen.",
  "Remote lock controls require confirmation and verify the returned lock state. Other remote vehicle actions remain read-only.": "Vergrendeling op afstand vraagt bevestiging en controleert de teruggekoppelde status. Andere voertuigacties op afstand blijven alleen-lezen.",
  "Set vehicle_controls: true to enable reviewed lock controls. Other remote vehicle actions remain read-only.": "Stel vehicle_controls: true in om de gecontroleerde vergrendelbediening te activeren. Andere voertuigacties op afstand blijven alleen-lezen.",
  "Driving history": "Rijgeschiedenis",
  "Daily driving data": "Dagelijkse rijgegevens",
  "Official Kia totals for the latest": "Officiële Kia-totalen voor de laatste",
  "driving days.": "rijdagen.",
  "Distance": "Afstand",
  "Energy used": "Verbruikte energie",
  "Average consumption": "Gemiddeld verbruik",
  "Show daily details": "Toon dagelijkse details",
  "Hide daily details": "Verberg dagelijkse details",
  "Day": "Dag",
  "Consumption": "Verbruik",
  "Recorder analysis": "Recorder-analyse",
  "Trip history": "Ritgeschiedenis",
  "Trip reconstruction not configured": "Ritreconstructie niet geconfigureerd",
  "Map engine or ignition together with odometer to derive individual trips.": "Wijs motor of contact samen met de kilometerstand toe om afzonderlijke ritten af te leiden.",
  "Loading trip history": "Ritgeschiedenis laden",
  "Reading recent vehicle states from Home Assistant Recorder.": "Recente voertuigstatussen worden uit Home Assistant Recorder gelezen.",
  "Trip history unavailable": "Ritgeschiedenis niet beschikbaar",
  "No completed trips found": "Geen voltooide ritten gevonden",
  "Trips appear after Kia reports an engine or ignition cycle with a higher odometer value.": "Ritten verschijnen nadat Kia een motor- of contactcyclus met een hogere kilometerstand meldt.",
  "Duration": "Duur",
  "Average speed": "Gemiddelde snelheid",
  "Unknown location": "Onbekende locatie",
  "Away": "Onderweg",
  "Estimated from Kia engine, odometer, location, and battery updates over the latest": "Geschat op basis van Kia-updates voor motor, kilometerstand, locatie en batterij over de laatste",
  "days.": "dagen.",
  "Times, energy, and locations are estimates because Kia updates can arrive several minutes apart.": "Tijden, energie en locaties zijn schattingen omdat Kia-updates meerdere minuten uit elkaar kunnen liggen.",
  "Persistent calendar": "Permanente kalender",
  "Stored trip history": "Opgeslagen ritgeschiedenis",
  "Trips are stored locally in Home Assistant and remain available beyond Recorder retention.": "Ritten worden lokaal in Home Assistant opgeslagen en blijven beschikbaar na de Recorder-bewaartermijn.",
  "Day": "Dag",
  "Overview": "Overzicht",
  "Previous month": "Vorige maand",
  "Next month": "Volgende maand",
  "Today": "Vandaag",
  "Refresh calendar": "Kalender vernieuwen",
  "Loading calendar history": "Kalendergeschiedenis laden",
  "Reading stored trips from the mapped Home Assistant calendar.": "Opgeslagen ritten worden uit de toegewezen Home Assistant-kalender gelezen.",
  "Calendar history unavailable": "Kalendergeschiedenis niet beschikbaar",
  "No stored trips for this day": "Geen opgeslagen ritten voor deze dag",
  "Choose another date or switch to Overview.": "Kies een andere datum of schakel naar Overzicht.",
  "No stored trips found": "Geen opgeslagen ritten gevonden",
  "New trips appear after the Home Assistant trip package records a completed drive.": "Nieuwe ritten verschijnen nadat het Home Assistant-rittenpackage een voltooide rit opslaat.",
  "Stored trips": "Opgeslagen ritten",
  "Route overview": "Routeoverzicht",
  "Available Kia location points": "Beschikbare Kia-locatiepunten",
  "Phone-assisted route points": "Routepunten via telefoon",
  "Loading route data": "Routegegevens laden",
  "No route points available for this day": "Geen routepunten beschikbaar voor deze dag",
  "Approximate routes": "Benaderde routes",
  "Approximate trip route": "Benaderde ritroute",
  "Approximate route": "Benaderde route",
  "points": "punten",
  "Start": "Start",
  "End": "Einde",
  "Used": "Verbruikt",
  "Avg speed": "Gem. snelheid",
  "Odometer": "Kilometerstand",
  "Route": "Route",
  "Drive": "Aandrijving",
  "Electronics": "Boordelektronica",
  "Battery care": "Batterijbeheer",
  "Recovered": "Teruggewonnen",
  "recovered": "teruggewonnen",
  "Detailed energy breakdown is available for newly recorded trips.": "De gedetailleerde energie-uitsplitsing is beschikbaar voor nieuw opgeslagen ritten.",
  "Selected day": "Geselecteerde dag",
  "All calendar history": "Volledige kalendergeschiedenis",
  "Calendar events are the persistent source; Recorder analysis remains the fallback when no trip calendar is mapped.": "Kalenderitems zijn de permanente bron; Recorder-analyse blijft de fallback wanneer geen rittenkalender is toegewezen.",
  "Showing the latest": "De laatste",
  "trips. Increase trip_calendar_limit to show more.": "ritten worden getoond. Verhoog trip_calendar_limit om er meer te tonen."
};

class KiaDashboardCard extends HTMLElement {
  constructor() {
    super();
    this.attachShadow({ mode: "open" });
    this._config = {};
    this._notice = "";
    this._activeTab = "overview";
    this._chargerModeBeforePause = "";
    this._tripHistory = [];
    this._tripHistoryState = "idle";
    this._tripHistoryError = "";
    this._tripHistoryRequestKey = "";
    this._tripHistoryRequestToken = 0;
    this._tripHistoryAttemptedAt = 0;
    this._calendarTrips = [];
    this._tripCalendarState = "idle";
    this._tripCalendarError = "";
    this._tripCalendarRequestKey = "";
    this._tripCalendarRequestToken = 0;
    this._tripCalendarCache = new Map();
    this._tripViewMode = "day";
    this._tripSelectedDate = this._dateKey(new Date());
    this._tripCalendarMonth = this._tripSelectedDate.slice(0, 7);
    this._dailyHistoryExpanded = false;
    this._chargerHistory = [];
    this._chargerHistoryState = "idle";
    this._chargerHistoryError = "";
    this._chargerHistoryRequestKey = "";
    this._chargerHistoryRequestToken = 0;
    this._chargerHistoryPeriod = 90;
    this._chargerHistoryExpanded = false;
  }

  setConfig(config) {
    const previousModeEntity = this._entity("charger_mode");
    const previousHistoryEntities = this._historyEntityIds().join("|");
    const previousTripCalendar = this._entity("trip_calendar");
    const previousChargerTotal = this._entity("charger_total_energy");
    this._config = config || {};
    if (previousModeEntity !== this._entity("charger_mode")) this._chargerModeBeforePause = "";
    if (previousHistoryEntities !== this._historyEntityIds().join("|")) {
      this._tripHistoryRequestToken += 1;
      this._tripHistoryRequestKey = "";
      this._tripHistoryState = "idle";
      this._tripHistory = [];
    }
    if (previousTripCalendar !== this._entity("trip_calendar")) {
      this._tripCalendarRequestToken += 1;
      this._tripCalendarRequestKey = "";
      this._tripCalendarState = "idle";
      this._calendarTrips = [];
      this._tripCalendarCache.clear();
    }
    if (previousChargerTotal !== this._entity("charger_total_energy")) {
      this._chargerHistoryRequestToken += 1;
      this._chargerHistoryRequestKey = "";
      this._chargerHistoryState = "idle";
      this._chargerHistory = [];
    }
    this._render();
    if (this._activeTab === "location") this._loadLocationTrips();
    if (this._activeTab === "energy") this._loadChargerHistory();
  }

  set hass(hass) {
    this._hass = hass;
    this._render();
    if (this._activeTab === "location") this._loadLocationTrips();
    if (this._activeTab === "energy") this._loadChargerHistory();
  }

  getCardSize() {
    return 12;
  }

  _entity(key) {
    return this._config.entities?.[key];
  }

  _obj(key) {
    const entityId = this._entity(key);
    return entityId && this._hass ? this._hass.states[entityId] : undefined;
  }

  _state(key, fallback = "--") {
    const obj = this._obj(key);
    if (!obj || obj.state === "unknown" || obj.state === "unavailable") return fallback;
    return obj.state;
  }

  _number(key, fallback = "--", precision = 1) {
    const value = Number.parseFloat(this._state(key, ""));
    const factor = 10 ** precision;
    return Number.isFinite(value) ? Math.round(value * factor) / factor : fallback;
  }

  _unit(key, fallback = "") {
    return this._obj(key)?.attributes?.unit_of_measurement || fallback;
  }

  _drivingHistory() {
    const attributes = this._obj("daily_driving_stats")?.attributes || {};
    return Object.entries(attributes)
      .filter(([date, value]) => /^\d{4}-\d{2}-\d{2}$/.test(date) && value && typeof value === "object")
      .map(([date, value]) => ({
        date,
        distance: Number.parseFloat(value.distance) || 0,
        consumed: Number.parseFloat(value.total_consumed) || 0,
        regenerated: Number.parseFloat(value.regenerated_energy) || 0,
        engineConsumed: Number.parseFloat(value.engine_consumption) || 0,
        climateConsumed: Number.parseFloat(value.climate_consumption) || 0,
        electronicsConsumed: Number.parseFloat(value.onboard_electronics_consumption) || 0,
        batteryCareConsumed: Number.parseFloat(value.battery_care_consumption) || 0,
      }))
      .sort((left, right) => left.date.localeCompare(right.date));
  }

  _todayDriving() {
    const mapped = this._obj("today_driving_stats");
    if (mapped && !["unknown", "unavailable"].includes(mapped.state)) {
      const value = mapped.attributes || {};
      return {
        date: value.today_date || mapped.state,
        distance: Number.parseFloat(value.distance) || 0,
        consumed: Number.parseFloat(value.total_consumed) || 0,
        regenerated: Number.parseFloat(value.regenerated_energy) || 0,
        engineConsumed: Number.parseFloat(value.engine_consumption) || 0,
        climateConsumed: Number.parseFloat(value.climate_consumption) || 0,
        electronicsConsumed: Number.parseFloat(value.onboard_electronics_consumption) || 0,
        batteryCareConsumed: Number.parseFloat(value.battery_care_consumption) || 0,
      };
    }
    return this._drivingHistory().at(-1) || null;
  }

  _drivingDays() {
    const history = this._drivingHistory();
    const today = this._todayDriving();
    if (!today?.date) return history;
    const existing = history.findIndex((day) => day.date === today.date);
    if (existing >= 0) history[existing] = { ...history[existing], ...today };
    else history.push(today);
    return history.sort((left, right) => left.date.localeCompare(right.date));
  }

  _tripHistoryDays() {
    const configured = Number.parseInt(this._config.trip_history_days ?? 7, 10);
    return Math.max(1, Math.min(14, Number.isFinite(configured) ? configured : 7));
  }

  _tripHistoryLimit() {
    const configured = Number.parseInt(this._config.trip_history_limit ?? 12, 10);
    return Math.max(1, Math.min(30, Number.isFinite(configured) ? configured : 12));
  }

  _dateKey(value) {
    const date = value instanceof Date ? value : new Date(value);
    if (Number.isNaN(date.getTime())) return "";
    return `${date.getFullYear()}-${String(date.getMonth() + 1).padStart(2, "0")}-${String(date.getDate()).padStart(2, "0")}`;
  }

  _tripCalendarLimit() {
    const configured = Number.parseInt(this._config.trip_calendar_limit ?? 250, 10);
    return Math.max(10, Math.min(1000, Number.isFinite(configured) ? configured : 250));
  }

  _tripCalendarRange() {
    const now = new Date();
    if (this._tripViewMode === "overview") {
      const configured = String(this._config.trip_calendar_start || "");
      const fallback = new Date(now.getFullYear() - 10, 0, 1);
      const start = /^\d{4}-\d{2}-\d{2}$/.test(configured) ? new Date(`${configured}T00:00:00`) : fallback;
      const end = new Date(now.getFullYear(), now.getMonth(), now.getDate() + 1);
      return { start, end };
    }
    const [year, month] = this._tripCalendarMonth.split("-").map(Number);
    return {
      start: new Date(year, month - 1, 1),
      end: new Date(year, month, 1),
    };
  }

  _tripCalendarKey() {
    const range = this._tripCalendarRange();
    const calendar = this._obj("trip_calendar");
    return [
      this._entity("trip_calendar") || "",
      this._tripViewMode,
      range.start.toISOString(),
      range.end.toISOString(),
      calendar?.last_changed || "",
      calendar?.last_updated || "",
    ].join("|");
  }

  _calendarEventDate(value) {
    const raw = value && typeof value === "object" ? value.dateTime || value.date : value;
    const date = new Date(raw);
    return Number.isNaN(date.getTime()) ? null : date;
  }

  _calendarNumber(value) {
    const number = Number.parseFloat(value);
    return Number.isFinite(number) ? number : null;
  }

  _calendarTrip(event) {
    const startDate = this._calendarEventDate(event?.start);
    const endDate = this._calendarEventDate(event?.end);
    if (!startDate || !endDate) return null;
    let data = {};
    try {
      data = JSON.parse(String(event.description || "{}"));
    } catch (_error) {
      return null;
    }
    if (!["kia_trip_v1", "kia_trip_v2"].includes(data.schema)) return null;
    const start = startDate.getTime();
    const end = endDate.getTime();
    const durationMinutes = this._calendarNumber(data.duration_minutes) ?? Math.max(1, Math.round((end - start) / 60000));
    const distance = this._calendarNumber(data.distance_km);
    const usedEnergy = this._calendarNumber(data.energy_kwh);
    const averageSpeed = this._calendarNumber(data.average_speed_kmh) ?? (distance !== null && durationMinutes > 0 ? distance / (durationMinutes / 60) : null);
    const consumption = this._calendarNumber(data.consumption_kwh_100km) ?? (distance > 0 && usedEnergy !== null ? (usedEnergy / distance) * 100 : null);
    const coordinate = (value) => {
      const [latRaw, lonRaw] = String(value || "").split(",");
      const lat = this._calendarNumber(latRaw);
      const lon = this._calendarNumber(lonRaw);
      return lat !== null && lon !== null && Math.abs(lat) <= 90 && Math.abs(lon) <= 180 ? { lat, lon } : null;
    };
    const routePoints = (Array.isArray(data.route_points) ? data.route_points : [])
      .map((point) => Array.isArray(point) ? coordinate(`${point[0]},${point[1]}`) : coordinate(point))
      .filter(Boolean);
    const originCoordinates = coordinate(data.origin_coordinates);
    const destinationCoordinates = coordinate(data.destination_coordinates);
    if (originCoordinates && !routePoints.length) routePoints.push(originCoordinates);
    if (destinationCoordinates && (!routePoints.length || routePoints.at(-1).lat !== destinationCoordinates.lat || routePoints.at(-1).lon !== destinationCoordinates.lon)) routePoints.push(destinationCoordinates);
    return {
      source: "calendar",
      id: String(data.trip_id || `${start}`),
      start,
      end,
      durationMinutes,
      distance,
      origin: String(data.origin || "Unknown location"),
      destination: String(data.destination || event.location || "Unknown location"),
      startBattery: this._calendarNumber(data.soc_start),
      endBattery: this._calendarNumber(data.soc_end),
      usedEnergy,
      consumption,
      averageSpeed,
      startOdometer: this._calendarNumber(data.odometer_start),
      endOdometer: this._calendarNumber(data.odometer_end),
      driveEnergy: this._calendarNumber(data.drive_energy_kwh),
      climateEnergy: this._calendarNumber(data.climate_energy_kwh),
      electronicsEnergy: this._calendarNumber(data.electronics_energy_kwh),
      batteryCareEnergy: this._calendarNumber(data.battery_care_energy_kwh),
      regeneratedEnergy: this._calendarNumber(data.regenerated_energy_kwh),
      routeSource: String(data.route_source || "kia"),
      routePoints,
    };
  }

  async _loadTripCalendar(force = false) {
    const entityId = this._entity("trip_calendar");
    if (!entityId || !this._hass?.callApi) return;
    const requestKey = this._tripCalendarKey();
    if (!force && (this._tripCalendarState === "loading" || (this._tripCalendarState === "ready" && requestKey === this._tripCalendarRequestKey))) return;

    if (!force && this._tripCalendarCache.has(requestKey)) {
      this._tripCalendarRequestKey = requestKey;
      this._calendarTrips = this._tripCalendarCache.get(requestKey);
      this._tripCalendarState = "ready";
      this._tripCalendarError = "";
      if (this._activeTab === "location") this._render();
      return;
    }

    const token = ++this._tripCalendarRequestToken;
    this._tripCalendarRequestKey = requestKey;
    this._tripCalendarState = "loading";
    this._tripCalendarError = "";
    this._render();
    const { start, end } = this._tripCalendarRange();
    const path = `calendars/${encodeURIComponent(entityId)}?start=${encodeURIComponent(start.toISOString())}&end=${encodeURIComponent(end.toISOString())}`;
    try {
      const events = await this._hass.callApi("GET", path);
      if (token !== this._tripCalendarRequestToken) return;
      this._calendarTrips = (Array.isArray(events) ? events : [])
        .map((event) => this._calendarTrip(event))
        .filter(Boolean)
        .sort((left, right) => right.start - left.start);
      this._tripCalendarCache.set(requestKey, this._calendarTrips);
      while (this._tripCalendarCache.size > 18) {
        this._tripCalendarCache.delete(this._tripCalendarCache.keys().next().value);
      }
      this._tripCalendarState = "ready";
    } catch (error) {
      if (token !== this._tripCalendarRequestToken) return;
      this._calendarTrips = [];
      this._tripCalendarState = "error";
      this._tripCalendarError = error?.message || String(error || "Calendar unavailable");
    }
    if (this._activeTab === "location") this._render();
  }

  _loadLocationTrips(force = false) {
    if (this._entity("trip_calendar")) return this._loadTripCalendar(force);
    return this._loadTripHistory();
  }

  _setTripView(mode) {
    if (!["day", "overview"].includes(mode) || mode === this._tripViewMode) return;
    this._tripViewMode = mode;
    this._tripCalendarRequestKey = "";
    this._loadTripCalendar();
  }

  _shiftTripMonth(offset) {
    const [year, month] = this._tripCalendarMonth.split("-").map(Number);
    const date = new Date(year, month - 1 + offset, 1);
    this._tripCalendarMonth = this._dateKey(date).slice(0, 7);
    this._tripSelectedDate = this._dateKey(date);
    this._tripCalendarRequestKey = "";
    this._loadTripCalendar();
  }

  _selectTripDate(value) {
    if (!/^\d{4}-\d{2}-\d{2}$/.test(value)) return;
    const month = value.slice(0, 7);
    this._tripSelectedDate = value;
    if (month !== this._tripCalendarMonth) {
      this._tripCalendarMonth = month;
      this._tripCalendarRequestKey = "";
      this._loadTripCalendar();
    } else {
      this._render();
    }
  }

  _historyEntityIds() {
    return ["engine", "ignition", "odometer", "location", "battery_level", "battery_remaining_energy"]
      .map((key) => this._entity(key))
      .filter((entityId, index, values) => entityId && values.indexOf(entityId) === index);
  }

  _tripHistoryKey() {
    const source = this._obj("engine") || this._obj("ignition");
    const odometer = this._obj("odometer");
    return [
      this._tripHistoryDays(),
      ...this._historyEntityIds(),
      source?.last_changed || source?.last_updated || "",
      odometer?.last_changed || odometer?.last_updated || "",
    ].join("|");
  }

  async _loadTripHistory() {
    if (this._entity("trip_calendar")) return;
    if (!this._hass?.callApi || !this._entity("odometer") || (!this._entity("engine") && !this._entity("ignition"))) return;
    const requestKey = this._tripHistoryKey();
    if (this._tripHistoryState === "loading" || (this._tripHistoryState === "ready" && requestKey === this._tripHistoryRequestKey)) return;
    if (this._tripHistoryState === "error" && requestKey === this._tripHistoryRequestKey && Date.now() - this._tripHistoryAttemptedAt < 300000) return;

    const token = ++this._tripHistoryRequestToken;
    this._tripHistoryRequestKey = requestKey;
    this._tripHistoryAttemptedAt = Date.now();
    this._tripHistoryState = "loading";
    this._tripHistoryError = "";
    this._render();

    const end = new Date();
    const start = new Date(end.getTime() - this._tripHistoryDays() * 86400000);
    const entityIds = this._historyEntityIds();
    const path = `history/period/${encodeURIComponent(start.toISOString())}?filter_entity_id=${encodeURIComponent(entityIds.join(","))}&end_time=${encodeURIComponent(end.toISOString())}`;

    try {
      const history = await this._hass.callApi("GET", path);
      if (token !== this._tripHistoryRequestToken) return;
      this._tripHistory = this._deriveTripHistory(history).slice(0, this._tripHistoryLimit());
      this._tripHistoryState = "ready";
    } catch (error) {
      if (token !== this._tripHistoryRequestToken) return;
      this._tripHistory = [];
      this._tripHistoryState = "error";
      this._tripHistoryError = error?.message || String(error || "History unavailable");
    }
    if (this._activeTab === "location") this._render();
  }

  _historySeries(history, entityId) {
    if (!entityId || !Array.isArray(history)) return [];
    const series = history.find((items) => Array.isArray(items) && items.some((item) => item?.entity_id === entityId)) || [];
    return series
      .filter((item) => item && !["unknown", "unavailable"].includes(String(item.state).toLowerCase()))
      .map((item) => ({ ...item, timestamp: new Date(item.last_changed || item.last_updated).getTime() }))
      .filter((item) => Number.isFinite(item.timestamp))
      .sort((left, right) => left.timestamp - right.timestamp);
  }

  _historySample(series, timestamp, preferPrevious = false) {
    if (!series.length) return null;
    const before = series.filter((item) => item.timestamp <= timestamp - (preferPrevious ? 1000 : 0)).at(-1);
    if (preferPrevious && before) return before;
    const after = series.find((item) => item.timestamp >= timestamp && item.timestamp - timestamp <= 120000);
    return after || series.filter((item) => item.timestamp <= timestamp + 120000).at(-1) || null;
  }

  _historyNumber(sample) {
    const value = Number.parseFloat(sample?.state);
    return Number.isFinite(value) ? value : null;
  }

  _historyEnergy(sample) {
    const value = this._historyNumber(sample);
    if (value === null) return null;
    const unit = String(sample?.attributes?.unit_of_measurement || "kWh").toLowerCase();
    if (unit === "kj") return value / 3600;
    if (unit === "wh") return value / 1000;
    return value;
  }

  _historyLocation(sample) {
    if (!sample) return "Unknown location";
    const state = String(sample.state || "").trim();
    if (state && !["not_home", "unknown", "unavailable"].includes(state.toLowerCase())) return state;
    const latitude = Number.parseFloat(sample.attributes?.latitude);
    const longitude = Number.parseFloat(sample.attributes?.longitude);
    return Number.isFinite(latitude) && Number.isFinite(longitude) ? `${latitude.toFixed(4)}, ${longitude.toFixed(4)}` : "Away";
  }

  _deriveTripHistory(history) {
    const engineSeries = this._historySeries(history, this._entity("engine"));
    const ignitionSeries = this._historySeries(history, this._entity("ignition"));
    const engine = engineSeries.length ? engineSeries : ignitionSeries;
    const odometer = this._historySeries(history, this._entity("odometer"));
    const locations = this._historySeries(history, this._entity("location"));
    const battery = this._historySeries(history, this._entity("battery_level"));
    const remainingEnergy = this._historySeries(history, this._entity("battery_remaining_energy"));
    const active = (state) => ["on", "running", "true", "1"].includes(String(state).toLowerCase());
    const trips = [];
    let started = null;
    let wasActive = false;

    for (const state of engine) {
      const isActive = active(state.state);
      if (isActive && !wasActive) started = state;
      if (!isActive && wasActive && started) {
        const startOdometer = this._historyNumber(this._historySample(odometer, started.timestamp, true));
        const endOdometer = this._historyNumber(this._historySample(odometer, state.timestamp));
        const distance = startOdometer !== null && endOdometer !== null ? endOdometer - startOdometer : null;
        if (distance !== null && distance >= 0.2) {
          const startBattery = this._historyNumber(this._historySample(battery, started.timestamp, true));
          const endBattery = this._historyNumber(this._historySample(battery, state.timestamp));
          const startEnergy = this._historyEnergy(this._historySample(remainingEnergy, started.timestamp, true));
          const endEnergy = this._historyEnergy(this._historySample(remainingEnergy, state.timestamp));
          const usedEnergy = startEnergy !== null && endEnergy !== null && startEnergy >= endEnergy ? startEnergy - endEnergy : null;
          const durationMinutes = Math.max(1, Math.round((state.timestamp - started.timestamp) / 60000));
          trips.push({
            start: started.timestamp,
            end: state.timestamp,
            durationMinutes,
            distance,
            origin: this._historyLocation(this._historySample(locations, started.timestamp, true)),
            destination: this._historyLocation(this._historySample(locations, state.timestamp)),
            startBattery,
            endBattery,
            usedEnergy,
            consumption: usedEnergy !== null && distance > 0 ? (usedEnergy / distance) * 100 : null,
            averageSpeed: durationMinutes > 0 ? distance / (durationMinutes / 60) : null,
          });
        }
        started = null;
      }
      wasActive = isActive;
    }
    return trips.sort((left, right) => right.start - left.start);
  }

  _safe(value) {
    return String(value ?? "").replace(/[&<>"]/g, (char) => ({ "&": "&amp;", "<": "&lt;", ">": "&gt;", '"': "&quot;" })[char]);
  }

  _language() {
    const language = String(this._hass?.locale?.language || "").toLowerCase();
    return ["nl", "nl-be", "nl-nl"].includes(language) ? "nl" : "en";
  }

  _localize(value) {
    const text = String(value ?? "");
    if (this._language() !== "nl") return text;
    const pattern = Object.keys(KIA_DASHBOARD_NL)
      .sort((left, right) => right.length - left.length)
      .map((key) => key.replace(/[.*+?^$\{}()|[\]\\]/g, "\\$&"))
      .join("|");
    return text
      .replace(new RegExp(pattern, "g"), (english) => KIA_DASHBOARD_NL[english])
      .replace(/(\d+) of (\d+) available/g, "$1 van $2 beschikbaar")
      .replace(/(\d+) items? to check/g, "$1 punten te controleren")
      .replace(/(\d+) signals? (?:is|are) missing or unavailable/g, "$1 signalen ontbreken of zijn niet beschikbaar");
  }

  _active(key) {
    const value = String(this._state(key, "off")).toLowerCase();
    return ["on", "open", "unlocked", "charging", "connected", "heat", "cool", "heat_cool", "dry", "fan_only"].includes(value);
  }

  _locked() {
    return String(this._state("door_lock", "locked")).toLowerCase() === "locked";
  }

  _charging() {
    const value = String(this._state("charging_state", "off")).toLowerCase();
    return this._active("charging_state") || value.includes("charging") || value.includes("opladen");
  }

  _climateOn() {
    const value = String(this._state("climate", "off")).toLowerCase();
    return value && !["off", "uit", "idle", "unknown", "unavailable", "--"].includes(value);
  }

  _formatDate(value) {
    if (!value || value === "--") return "--";
    const normalized = String(value).replace(/(\d{4}-\d{2}-\d{2})T/, "$1T");
    const date = new Date(normalized);
    if (Number.isNaN(date.getTime())) return value;
    return new Intl.DateTimeFormat(this._hass?.locale?.language || navigator.language || "nl-BE", {
      day: "2-digit",
      month: "short",
      year: "numeric",
      hour: "2-digit",
      minute: "2-digit",
    }).format(date);
  }

  _connectionThreshold(key, fallback) {
    const value = Number(this._config?.[key]);
    return Number.isFinite(value) && value > 0 ? value : fallback;
  }

  _connectionAgeLabel(ageMinutes) {
    if (!Number.isFinite(ageMinutes)) return "No timestamp available";
    if (ageMinutes < 1) return "Just now";
    if (ageMinutes < 60) return `${Math.round(ageMinutes)} min ago`;
    if (ageMinutes < 1440) return `${Math.round(ageMinutes / 60)} h ago`;
    return `${Math.round(ageMinutes / 1440)} d ago`;
  }

  _vehicleConnectionHealth(now = Date.now()) {
    if (!this._entity("last_updated")) {
      return {
        state: "not-configured",
        label: "Not configured",
        tone: "muted",
        icon: "mdi:link-variant-off",
        ageLabel: "No timestamp available",
        updatedLabel: "--",
        message: "Map last_updated to monitor Kia data freshness.",
      };
    }

    const entity = this._obj("last_updated");
    const rawState = String(entity?.state || "").toLowerCase();
    if (!entity || ["unknown", "unavailable", ""].includes(rawState)) {
      return {
        state: "unavailable",
        label: "Unavailable",
        tone: "critical",
        icon: "mdi:cloud-alert-outline",
        ageLabel: "No timestamp available",
        updatedLabel: "--",
        message: "The last-updated entity is unavailable. Check the Kia integration and card mapping.",
      };
    }

    const timestamp = new Date(entity.state).getTime();
    if (!Number.isFinite(timestamp)) {
      return {
        state: "unavailable",
        label: "Unavailable",
        tone: "critical",
        icon: "mdi:clock-alert-outline",
        ageLabel: "No timestamp available",
        updatedLabel: this._formatDate(entity.state),
        message: "The last-updated entity is unavailable. Check the Kia integration and card mapping.",
      };
    }

    const ageMinutes = Math.max(0, (now - timestamp) / 60000);
    const freshMinutes = this._connectionThreshold("vehicle_fresh_minutes", 30);
    const staleMinutes = Math.max(freshMinutes, this._connectionThreshold("vehicle_stale_minutes", 180));
    const common = {
      ageMinutes,
      ageLabel: this._connectionAgeLabel(ageMinutes),
      updatedLabel: this._formatDate(entity.state),
    };
    if (ageMinutes <= freshMinutes) {
      return { ...common, state: "current", label: "Data current", tone: "healthy", icon: "mdi:cloud-check-outline", message: "Vehicle data updated within the expected interval." };
    }
    if (ageMinutes <= staleMinutes) {
      return { ...common, state: "delayed", label: "Data delayed", tone: "attention", icon: "mdi:cloud-clock-outline", message: "Vehicle data is arriving later than expected; wait for the next Kia scan." };
    }
    return { ...common, state: "stale", label: "Data stale", tone: "critical", icon: "mdi:cloud-alert-outline", message: "No recent Kia data. Check Kia Connect authentication, integration repairs, or a Kia cloud outage." };
  }

  _chargerConnectionHealth() {
    if (!this._entity("charger_online")) {
      return { state: "not-configured", label: "Not configured", tone: "muted", icon: "mdi:ev-station-off", message: "Map charger_online to monitor the home charger separately." };
    }
    const entity = this._obj("charger_online");
    const state = String(entity?.state || "").toLowerCase();
    if (!entity || ["unknown", "unavailable", ""].includes(state)) {
      return { state: "unavailable", label: "Unavailable", tone: "critical", icon: "mdi:ev-station-off", message: "The charger connection entity is unavailable." };
    }
    if (["on", "online", "connected", "true", "1"].includes(state)) {
      return { state: "online", label: "Online", tone: "healthy", icon: "mdi:ev-station", message: "Home charger reports an active connection." };
    }
    if (["off", "offline", "disconnected", "false", "0"].includes(state)) {
      return { state: "offline", label: "Offline", tone: "critical", icon: "mdi:ev-station-off", message: "Home charger reports that it is offline." };
    }
    return { state: "unavailable", label: "Unavailable", tone: "attention", icon: "mdi:ev-station-off", message: "The charger connection entity is unavailable." };
  }

  _tireStatus(key) {
    const value = String(this._state(key, "OK")).toLowerCase();
    if (["off", "ok", "normal", "closed", "false", "0"].includes(value)) return "OK";
    if (["on", "problem", "warning", "low", "true", "1"].includes(value)) return "Check";
    return this._safe(this._state(key, "OK"));
  }

  _asset(name) {
    if (!name) return "";
    if (name.startsWith("/")) return name;
    return `${this._config.asset_base || "/local/vehicles/"}${name}`;
  }

  _carImage() {
    const images = this._config.images || {};
    if (this._charging()) return this._asset(images.charging || "ev6_charging.png");
    if (this._climateOn()) return this._asset(images.climate || "ev6_climate.png");
    return this._asset(images.normal || "ev6_front_right.png");
  }

  _coord(value) {
    const parsed = Number.parseFloat(value);
    return Number.isFinite(parsed) ? parsed : null;
  }

  _trackerCoords() {
    const configuredLat = this._coord(this._config.location?.latitude ?? this._config.latitude);
    const configuredLon = this._coord(this._config.location?.longitude ?? this._config.longitude);
    if (configuredLat !== null && configuredLon !== null) return { lat: configuredLat, lon: configuredLon };

    const entityLat = this._coord(this._state("latitude", ""));
    const entityLon = this._coord(this._state("longitude", ""));
    if (entityLat !== null && entityLon !== null) return { lat: entityLat, lon: entityLon };

    const obj = this._obj("location");
    const attrs = obj?.attributes || {};
    const gps = Array.isArray(attrs.gps) ? attrs.gps : [];
    const lat = this._coord(attrs.latitude ?? attrs.lat ?? gps[0]);
    const lon = this._coord(attrs.longitude ?? attrs.lon ?? attrs.lng ?? gps[1]);
    return lat !== null && lon !== null ? { lat, lon } : null;
  }

  _mapTileGrid() {
    const coords = this._trackerCoords();
    if (!coords) return null;
    const configuredZoom = Number.parseInt(this._config.map_zoom ?? this._config.map?.zoom ?? 16, 10);
    const zoom = Math.max(3, Math.min(18, Number.isFinite(configuredZoom) ? configuredZoom : 16));
    const tileSize = 256;
    const radius = 2;
    const gridSize = radius * 2 + 1;
    const scale = 2 ** zoom;
    const latRad = (coords.lat * Math.PI) / 180;
    const xFloat = ((coords.lon + 180) / 360) * scale;
    const yFloat = ((1 - Math.log(Math.tan(latRad) + 1 / Math.cos(latRad)) / Math.PI) / 2) * scale;
    const xTile = Math.floor(xFloat);
    const yTile = Math.floor(yFloat);
    const xOffset = (xFloat - xTile) * tileSize;
    const yOffset = (yFloat - yTile) * tileSize;
    const centerOffset = radius * tileSize;
    const tiles = [];

    for (let y = -radius; y <= radius; y += 1) {
      for (let x = -radius; x <= radius; x += 1) {
        const tileX = ((xTile + x) % scale + scale) % scale;
        const tileY = yTile + y;
        if (tileY < 0 || tileY >= scale) {
          tiles.push('<span class="map-tile-empty"></span>');
        } else {
          tiles.push(`<img src="https://tile.openstreetmap.org/${zoom}/${tileX}/${tileY}.png" alt="">`);
        }
      }
    }

    return {
      style: `--map-grid:${gridSize};--map-size:${gridSize * tileSize}px;left:50%;top:50%;transform:translate(-${Math.round(centerOffset + xOffset)}px, -${Math.round(centerOffset + yOffset)}px)`,
      tiles: tiles.join(""),
    };
  }

  _rangeAttrs(key, fallbackMin = 0, fallbackMax = 100, fallbackStep = 1) {
    const attrs = this._obj(key)?.attributes || {};
    const min = Number.parseFloat(attrs.min);
    const max = Number.parseFloat(attrs.max);
    const step = Number.parseFloat(attrs.step);
    return {
      min: Number.isFinite(min) ? min : fallbackMin,
      max: Number.isFinite(max) ? max : fallbackMax,
      step: Number.isFinite(step) && step > 0 ? step : fallbackStep,
    };
  }

  _navigate(section) {
    const tabs = ["overview", "battery", "vehicle", "climate", "energy", "location", "settings"];
    if (!tabs.includes(section) || section === this._activeTab) return;
    this._activeTab = section;
    this._render();
    if (section === "location") this._loadLocationTrips(true);
    if (section === "energy") this._loadChargerHistory(true);
  }

  _moreInfo(key) {
    const entityId = this._entity(key);
    if (!entityId) return;
    this.dispatchEvent(new CustomEvent("hass-more-info", { bubbles: true, composed: true, detail: { entityId } }));
  }

  _confirm(message) {
    if (this._config.confirm_actions === false) return true;
    return window.confirm(this._localize(message));
  }

  _noticeMessage(message) {
    this._notice = message;
    this._render();
    window.setTimeout(() => {
      if (this._notice === message) {
        this._notice = "";
        this._render();
      }
    }, 6500);
  }

  _actionErrorMessage(error, entityId) {
    const raw = error?.message || error?.body?.message || String(error || "");
    if (raw.toLowerCase().includes("pin")) return `Action failed for ${entityId}: PIN verification failed in the Kia integration.`;
    return raw || `Action failed for ${entityId}`;
  }

  _chargerModes() {
    const modeOptions = this._config.charger_modes || this._config.charger?.modes || {};
    return [
      [modeOptions.standard || "standard", "mdi:flash", "Standard"],
      [modeOptions.smart || "smart", "mdi:brain", "Smart"],
      [modeOptions.solar || "solar", "mdi:solar-power", "Solar"],
    ];
  }

  _chargerModeStorageKey() {
    return `kia-dashboard:charger-mode:${this._entity("charger_mode") || "default"}`;
  }

  _validChargerMode(value) {
    const mode = String(value || "").trim();
    const match = this._chargerModes().find(([option]) => String(option).toLowerCase() === mode.toLowerCase());
    return match ? String(match[0]) : "";
  }

  _rememberChargerMode(value = this._state("charger_mode", "")) {
    const mode = this._validChargerMode(value);
    if (!mode) return "";
    this._chargerModeBeforePause = mode;
    try {
      window.sessionStorage?.setItem(this._chargerModeStorageKey(), mode);
    } catch (_error) {
      // Session storage is optional; the in-memory value still covers this card instance.
    }
    return mode;
  }

  _rememberedChargerMode() {
    let mode = this._validChargerMode(this._chargerModeBeforePause);
    if (mode) return mode;
    try {
      mode = this._validChargerMode(window.sessionStorage?.getItem(this._chargerModeStorageKey()));
    } catch (_error) {
      mode = "";
    }
    if (mode) this._chargerModeBeforePause = mode;
    return mode;
  }

  _chargerStatusLabel(value) {
    const labels = {
      initialize: "Initializing",
      available: "Available",
      cable_connected: "Cable connected",
      charging: "Charging",
      charging_finished: "Charging finished",
      error: "Charger error",
      started: "Charging",
      suspended: "Paused",
      suspended_evse: "Paused by charger",
      stopped: "Stopped",
    };
    const normalized = String(value || "").trim().toLowerCase();
    return this._localize(labels[normalized] || String(value || "Not mapped").replaceAll("_", " "));
  }

  async _callEntity(entityKey, service, message) {
    const entityId = this._entity(entityKey);
    if (!entityId || !this._hass) {
      this._noticeMessage(`Action entity missing: ${entityKey}`);
      this._moreInfo(entityKey);
      return;
    }

    if (message && !this._confirm(message)) {
      this._noticeMessage("Action cancelled");
      return;
    }

    const domain = entityId.split(".")[0];
    let targetDomain = domain;
    let targetService = service;

    if (domain === "button") targetService = "press";
    if (domain === "lock") targetService = service === "turn_off" ? "unlock" : "lock";
    if (domain === "cover") targetService = service === "close" ? "close_cover" : "open_cover";
    if (["switch", "input_boolean"].includes(domain) && ["open", "close"].includes(service)) targetService = "turn_on";

    if (!["button", "cover", "lock", "switch", "input_boolean", "climate"].includes(domain)) {
      this._noticeMessage(`Unsupported action entity: ${entityId}`);
      this._moreInfo(entityKey);
      return;
    }

    try {
      await this._hass.callService(targetDomain, targetService, { entity_id: entityId });
      this._noticeMessage(`Sent ${targetDomain}.${targetService} to ${entityId}`);
    } catch (error) {
      this._noticeMessage(this._actionErrorMessage(error, entityId));
    }
  }

  async _waitForEntityState(entityKey, expectedState, timeoutMs = 90000) {
    const expected = String(expectedState).toLowerCase();
    const matches = () => String(this._obj(entityKey)?.state || "").toLowerCase() === expected;
    if (matches()) return true;
    const started = Date.now();
    return new Promise((resolve) => {
      const check = () => {
        if (matches()) return resolve(true);
        if (Date.now() - started >= timeoutMs) return resolve(false);
        window.setTimeout(check, 1000);
      };
      window.setTimeout(check, 1000);
    });
  }

  async _refreshVehicleStateAfterAction(expectedState, timeoutMs = 90000) {
    const refreshEntityId = this._entity("refresh");
    if (!this._hass || !refreshEntityId?.startsWith("button.")) return false;
    const configuredDelay = Number(this._config.vehicle_action_refresh_delay);
    const maximumDelay = Math.max(0, Math.min(30000, timeoutMs - 1000));
    const delay = Number.isFinite(configuredDelay) ? Math.max(0, Math.min(maximumDelay, configuredDelay)) : Math.min(5000, maximumDelay);
    if (delay > 0) {
      await new Promise((resolve) => window.setTimeout(resolve, delay));
    }
    if (String(this._obj("door_lock")?.state || "").toLowerCase() === String(expectedState).toLowerCase()) return false;
    try {
      await this._hass.callService("button", "press", { entity_id: refreshEntityId });
      this._noticeMessage("Vehicle status refresh requested; waiting for the updated lock state.");
      return true;
    } catch (_error) {
      this._noticeMessage("Automatic vehicle status refresh failed; waiting for Home Assistant to update.");
      return false;
    }
  }

  async _setVehicleLock(locked) {
    const entityId = this._entity("door_lock");
    const controlsEnabled = this._config.vehicle_controls === true || this._config.vehicle?.controls === true;
    if (!this._hass || !controlsEnabled || !entityId?.startsWith("lock.")) {
      this._noticeMessage("Vehicle lock controls are not enabled or not mapped to a lock entity.");
      return;
    }
    const currentState = String(this._obj("door_lock")?.state || "").toLowerCase();
    if (!["locked", "unlocked"].includes(currentState)) {
      this._noticeMessage("Vehicle lock status is unavailable; command not sent.");
      return;
    }
    if (this._locked() === locked) {
      this._noticeMessage(locked ? "Vehicle is already locked" : "Vehicle is already unlocked");
      return;
    }
    const action = locked ? "lock" : "unlock";
    if (!this._confirm(locked ? "Lock the vehicle now?" : "Unlock the vehicle now?")) {
      this._noticeMessage("Action cancelled");
      return;
    }
    try {
      await this._hass.callService("lock", action, { entity_id: entityId });
      this._noticeMessage(locked ? "Lock command sent; waiting for vehicle status." : "Unlock command sent; waiting for vehicle status.");
      const timeout = Math.max(5000, Number(this._config.vehicle_action_timeout) || 90000);
      const expectedState = locked ? "locked" : "unlocked";
      void this._refreshVehicleStateAfterAction(expectedState, timeout);
      const confirmed = await this._waitForEntityState("door_lock", expectedState, timeout);
      this._noticeMessage(confirmed
        ? (locked ? "Vehicle locked successfully" : "Vehicle unlocked successfully")
        : (locked
          ? "Lock command was sent, but the vehicle did not report locked before the timeout."
          : "Unlock command was sent, but the vehicle did not report unlocked before the timeout."));
    } catch (error) {
      this._noticeMessage(this._actionErrorMessage(error, entityId));
    }
  }

  async _setNumber(entityKey, value, confirmation = "") {
    const entityId = this._entity(entityKey);
    if (!entityId || !this._hass) return;
    if (!entityId.startsWith("number.")) {
      this._noticeMessage(`Cannot adjust ${entityId}; expected number entity.`);
      return;
    }
    if (confirmation && !this._confirm(confirmation)) {
      this._noticeMessage("Action cancelled");
      return;
    }
    try {
      await this._hass.callService("number", "set_value", { entity_id: entityId, value: Number(value) });
      const unit = this._unit(entityKey);
      this._noticeMessage(`Set ${entityId} to ${value}${unit ? ` ${unit}` : ""}`);
    } catch (error) {
      this._noticeMessage(this._actionErrorMessage(error, entityId));
    }
  }

  async _setSelect(entityKey, option, confirmation = `Change charger mode to ${option}?`) {
    const entityId = this._entity(entityKey);
    if (!entityId || !this._hass) return;
    const domain = entityId.split(".")[0];
    if (!["select", "input_select"].includes(domain)) {
      this._noticeMessage(`Cannot select an option on ${entityId}; expected select entity.`);
      return;
    }
    if (confirmation && !this._confirm(confirmation)) {
      this._noticeMessage("Action cancelled");
      return;
    }
    try {
      await this._hass.callService(domain, "select_option", { entity_id: entityId, option });
      this._rememberChargerMode(option);
      this._noticeMessage(`Set ${entityId} to ${option}`);
    } catch (error) {
      this._noticeMessage(this._actionErrorMessage(error, entityId));
    }
  }

  _handleAction(action) {
    if (action === "refresh") this._callEntity("refresh", "press", "");
    if (action === "start_climate") this._callEntity(this._entity("start_climate") ? "start_climate" : "climate", "turn_on", "Start climate now?");
    if (action === "stop_climate") this._callEntity(this._entity("stop_climate") ? "stop_climate" : "climate", "turn_off", "Stop climate now?");
    if (action === "start_charging") this._callEntity("start_charging", "turn_on", "Start charging now?");
    if (action === "stop_charging") this._callEntity("stop_charging", "turn_off", "Stop charging now?");
    if (action === "lock_vehicle") this._setVehicleLock(true);
    if (action === "unlock_vehicle") this._setVehicleLock(false);
    if (action === "charger_start") this._callEntity("charger_start", "turn_on", "Start the home charger now?");
    if (action === "charger_pause") {
      this._rememberChargerMode();
      this._callEntity("charger_pause", "turn_off", "Pause the home charger now?");
    }
    if (action === "charger_resume") {
      if (this._config.charger_resume_via_mode === true) {
        const mode = this._rememberedChargerMode();
        if (!mode) {
          this._noticeMessage("Previous charger mode is unknown. Choose Standard, Smart, or Solar to resume safely.");
        } else {
          this._setSelect("charger_mode", mode, `Resume the home charger in ${mode}?`);
        }
      } else {
        this._callEntity("charger_resume", "turn_on", "Resume the home charger now?");
      }
    }
    if (action === "charger_stop") this._callEntity("charger_stop", "turn_off", "Stop the home charging session now?");
  }

  _nav(icon, label, section) {
    const active = this._activeTab === section;
    return `<button class="nav-tile${active ? " active" : ""}" data-nav="${section}" aria-current="${active ? "page" : "false"}"><ha-icon icon="${icon}"></ha-icon><span>${label}</span></button>`;
  }

  _renderPlaceholder(icon, label) {
    return `<main class="detail-placeholder card" aria-live="polite"><ha-icon icon="${icon}"></ha-icon><div><span>Coming in 2.0.0</span><h2>${label}</h2><p>The ${label} detail view will be added here. The vehicle hero and section navigation remain available above.</p></div></main>`;
  }

  _renderBatteryTab(context = {}) {
    const state = (key) => {
      const obj = this._obj(key);
      const value = String(obj?.state ?? "").trim();
      return obj && value && !["unknown", "unavailable"].includes(value.toLowerCase()) ? { obj, value } : null;
    };
    const measurement = (key, fallbackUnit) => {
      const current = state(key);
      if (!current) return { available: false, text: "Unavailable", value: null };
      const value = Number.parseFloat(current.value);
      if (!Number.isFinite(value)) return { available: false, text: "Unavailable", value: null };
      const rounded = Math.round(value * 10) / 10;
      const unit = current.obj.attributes?.unit_of_measurement || fallbackUnit;
      return { available: true, text: `${rounded}${unit ? ` ${unit}` : ""}`, value: rounded, unit };
    };
    const binary = (key, activeStates, inactiveStates) => {
      const current = state(key);
      if (!current) return null;
      const value = current.value.toLowerCase();
      if (activeStates.includes(value)) return true;
      if (inactiveStates.includes(value)) return false;
      return null;
    };
    const limit = (key, label) => {
      const current = measurement(key, "%");
      if (!current.available) return `<div class="battery-detail-limit unavailable"><span>${label}</span><strong>Unavailable</strong></div>`;
      return `<div class="battery-detail-limit"><span>${label}</span>${this._numberControl(key, current.value, current.unit)}</div>`;
    };
    const battery = measurement("battery_level", "%");
    const pct = battery.available ? Math.max(0, Math.min(100, battery.value)) : 0;
    const range = measurement("battery_range", "km");
    const power = measurement("charging_power", "kW");
    const charging = binary("charging_state", ["on", "charging", "active", "opladen"], ["off", "not charging", "idle", "inactive"]);
    const plugged = binary("plug_connected", ["on", "connected", "true", "1"], ["off", "disconnected", "false", "0"]);
    const chargingText = charging === null ? "Unavailable" : charging ? "Charging" : "Not charging";
    const plugText = plugged === null ? "Unavailable" : plugged ? "Yes" : "No";
    const sessionText = charging === null || plugged === null ? "Unavailable" : charging ? "Charging" : plugged ? "Ready to charge" : "Not connected";
    const hasDc = Boolean(this._entity("dc_charging_limit"));
    const title = (icon, heading, caption) => `<div class="battery-detail-title"><ha-icon icon="${icon}"></ha-icon><div><h3>${heading}</h3><span>${caption}</span></div></div>`;
    const energyMeasurement = (key) => {
      const current = measurement(key, "kWh");
      if (!current.available || String(current.unit).toLowerCase() !== "kj") return current;
      const value = Math.round((current.value / 3600) * 10) / 10;
      return { available: true, value, unit: "kWh", text: `${value} kWh` };
    };
    const statusText = (key, activeLabel, inactiveLabel) => {
      const current = binary(key, ["on", "true", "1", "active"], ["off", "false", "0", "inactive"]);
      return current === null ? "Unavailable" : current ? activeLabel : inactiveLabel;
    };
    const health = measurement("battery_state_of_health", "%");
    const voltage = measurement("battery_pack_voltage", "V");
    const remainingEnergy = energyMeasurement("battery_remaining_energy");
    const capacity = energyMeasurement("battery_capacity");
    const temperatureMin = measurement("battery_temperature_min", "\u00b0C");
    const temperatureMax = measurement("battery_temperature_max", "\u00b0C");
    const waterTemperature = measurement("battery_water_temperature", "\u00b0C");
    const heaterPower = measurement("battery_heater_power", "W");
    const battery12v = measurement("battery_12v_level", "%");
    const estimatedDuration = state("estimated_charge_duration");


    return `<main class="battery-detail" aria-label="Battery details">
      <section class="battery-detail-card battery-detail-hero">
        <div class="battery-detail-heading"><span>EV battery overview</span><h2>Battery</h2><p>Range, charging state, limits, and cable status in one focused view.</p></div>
        <div class="battery-detail-gauge${battery.available ? "" : " unavailable"}"${battery.available ? ` style="--level:${pct}%"` : ""}><div><strong>${battery.available ? `${this._safe(battery.value)}<small>%</small>` : "Unavailable"}</strong><span>State of charge</span></div></div>
        <div class="battery-detail-highlights">
          <div><ha-icon icon="mdi:map-marker-distance"></ha-icon><span>Estimated range</span><strong class="${range.available ? "" : "unavailable"}">${this._safe(range.text)}</strong></div>
          <div><ha-icon icon="mdi:battery-charging"></ha-icon><span>Charging state</span><strong class="${charging === true ? "good" : charging === null ? "unavailable" : ""}">${chargingText}</strong></div>
          <div><ha-icon icon="mdi:power-plug"></ha-icon><span>Plug connected</span><strong class="${plugged === null ? "unavailable" : ""}">${plugText}</strong></div>
        </div>
      </section>
      <div class="battery-detail-grid">
        <section class="battery-detail-card battery-detail-condition">${title("mdi:heart-pulse", "Battery condition", "Traction battery and auxiliary system")}<div class="battery-detail-list"><div><span>State of health</span><strong class="${health.available ? "good" : "unavailable"}">${this._safe(health.text)}</strong></div><div><span>Remaining energy</span><strong class="${remainingEnergy.available ? "" : "unavailable"}">${this._safe(remainingEnergy.text)}</strong></div><div><span>Nominal capacity</span><strong class="${capacity.available ? "" : "unavailable"}">${this._safe(capacity.text)}</strong></div><div><span>Pack voltage</span><strong class="${voltage.available ? "" : "unavailable"}">${this._safe(voltage.text)}</strong></div></div><div class="battery-detail-subsection"><ha-icon icon="mdi:car-battery"></ha-icon><div><strong>12V battery</strong><span>Auxiliary battery condition</span></div></div><div class="battery-detail-list"><div><span>Battery level</span><strong class="${battery12v.available ? "" : "unavailable"}">${this._safe(battery12v.text)}</strong></div><div><span>Fault state</span><strong>${statusText("battery_12v_fault", "Fault detected", "No fault")}</strong></div></div></section>
        <section class="battery-detail-card battery-detail-thermal">${title("mdi:thermometer-lines", "Thermal management", "Battery temperatures and conditioning")}<div class="battery-detail-list"><div><span>Minimum temperature</span><strong>${this._safe(temperatureMin.text)}</strong></div><div><span>Maximum temperature</span><strong>${this._safe(temperatureMax.text)}</strong></div><div><span>Coolant temperature</span><strong>${this._safe(waterTemperature.text)}</strong></div><div><span>Battery heating</span><strong>${statusText("battery_heating", "Active", "Inactive")}</strong></div><div><span>Preconditioning</span><strong>${statusText("battery_precondition", "Active", "Inactive")}</strong></div><div><span>Winter mode</span><strong>${statusText("battery_winter_mode", "Active", "Inactive")}</strong></div><div><span>Heater power</span><strong>${this._safe(heaterPower.text)}</strong></div></div></section>
        <section class="battery-detail-card">${title("mdi:battery-sync", "Charge limits", "Configured charging targets")}${limit("charging_limit", "AC target")}${hasDc ? limit("dc_charging_limit", "DC target") : ""}</section>
        <section class="battery-detail-card">${title("mdi:map-marker-distance", "Range context", "Current driving estimate")}<div class="battery-detail-stat"><strong class="${range.available ? "" : "unavailable"}">${this._safe(range.text)}</strong><span>Estimated remaining range</span></div>${range.available && battery.available ? `<div class="battery-detail-progress"><span style="width:${pct}%"></span></div>` : ""}<p>Range changes with temperature, driving style, speed, and climate use.</p></section>
        <section class="battery-detail-card battery-detail-session">${title("mdi:flash", "Charging session", "Live connection context")}<div class="battery-detail-session-body"><ha-icon class="${charging === true ? "active" : ""}" icon="mdi:ev-plug-type2"></ha-icon><div><span>Charging power</span><strong>${this._safe(power.text)}</strong></div><div><span>Current state</span><strong>${sessionText}</strong></div><div><span>Estimated charge duration</span><strong>${this._safe(estimatedDuration?.value || "Unavailable")}</strong></div></div><p>Charging history remains on Energy; this panel focuses on the current battery session.</p></section>
      </div>
    </main>`;
  }

  _renderVehicleTab() {
    const read = (key) => {
      const entity = this._obj(key);
      if (!entity) return { value: "Not configured", available: false };
      if (["unknown", "unavailable"].includes(entity.state)) return { value: "Unavailable", available: false };
      return { value: entity.state, available: true };
    };
    const tile = (key, icon, label, context, warning = false, displayValue) => {
      const current = read(key);
      const tone = !current.available ? " unavailable" : warning ? " warning" : "";
      const value = current.available && displayValue ? displayValue : current.value;
      return `<button class="vehicle-state${tone}" data-info="${key}" ${this._entity(key) ? "" : "disabled"}><ha-icon icon="${icon}"></ha-icon><span><small>${context}</small><strong>${label}</strong></span><b>${this._safe(value)}</b><ha-icon class="vehicle-state-indicator" icon="${!current.available ? "mdi:help-circle-outline" : warning ? "mdi:alert-circle-outline" : "mdi:check-circle-outline"}"></ha-icon></button>`;
    };
    const binaryLabel = (key, active, inactive) => read(key).available ? (this._active(key) ? active : inactive) : undefined;
    const tireLabel = (key) => read(key).available ? (this._tireStatus(key) === "OK" ? "Normal" : "Attention") : undefined;
    const openings = [
      ["front_left_door", "Front left", "Door"], ["front_right_door", "Front right", "Door"], ["rear_left_door", "Rear left", "Door"], ["rear_right_door", "Rear right", "Door"],
      ["trunk", "Trunk", "Cargo opening"], ["hood", "Hood", "Service opening"],
      ["front_left_window", "Front left", "Window"], ["front_right_window", "Front right", "Window"], ["rear_left_window", "Rear left", "Window"], ["rear_right_window", "Rear right", "Window"],
    ];
    const perimeterOpenings = openings.slice(0, 6);
    const windows = [["front_left", "Front left"], ["front_right", "Front right"], ["rear_left", "Rear left"], ["rear_right", "Rear right"]];
    const tires = [["tire_front_left", "Front left"], ["tire_front_right", "Front right"], ["tire_rear_left", "Rear left"], ["tire_rear_right", "Rear right"]];
    const windowTiles = windows.map(([position, label]) => {
      const stateKey = `${position}_window`;
      const openKey = `${position}_window_open`;
      const closeKey = `${position}_window_close`;
      const current = read(stateKey);
      const warning = current.available && this._active(stateKey);
      const value = current.available ? (warning ? "Open" : "Closed") : current.value;
      return `<div class="vehicle-window-tile${!current.available ? " unavailable" : warning ? " warning" : ""}"><button class="vehicle-window-info" data-info="${stateKey}" ${this._entity(stateKey) ? "" : "disabled"}><ha-icon icon="mdi:car-door"></ha-icon><span><small>Window</small><strong>${label}</strong><b>${this._safe(value)}</b></span></button><div class="vehicle-window-actions"><button data-entity-action="${openKey}" data-service="open" data-confirm="Open ${label.toLowerCase()} window?" aria-label="Open ${label.toLowerCase()} window" title="Open" ${this._entity(openKey) ? "" : "disabled"}><ha-icon icon="mdi:arrow-up"></ha-icon></button><button data-entity-action="${closeKey}" data-service="close" data-confirm="Close ${label.toLowerCase()} window?" aria-label="Close ${label.toLowerCase()} window" title="Close" ${this._entity(closeKey) ? "" : "disabled"}><ha-icon icon="mdi:arrow-down"></ha-icon></button></div></div>`;
    }).join("");
    const vin = read("vin");
    const openWarnings = openings.filter(([key]) => this._obj(key) && this._active(key));
    const tireWarnings = tires.filter(([key]) => this._obj(key) && this._tireStatus(key) !== "OK");
    const unlocked = Boolean(this._obj("door_lock")) && !this._locked();
    const lightsOn = Boolean(this._obj("lights")) && this._active("lights");
    const smartKeyWarning = Boolean(this._obj("smart_key_battery_warning")) && this._active("smart_key_battery_warning");
    const warnings = [...(unlocked ? ["Vehicle doors are unlocked."] : []), ...(lightsOn ? ["Headlights are on."] : []), ...(smartKeyWarning ? ["Smart key battery needs attention."] : []), ...openWarnings.map(([, label, context]) => `${context} ${label.toLowerCase()} is open.`), ...tireWarnings.map(([, label]) => `${label} tire pressure needs attention.`)];
    const missingKeys = ["door_lock", "charge_port", "lights", ...openings.map(([key]) => key), ...tires.map(([key]) => key)].filter((key) => !read(key).available);
    const missing = missingKeys.length > 0;
    const summaryTone = warnings.length ? "warning" : missing ? "partial" : "";
    const summaryIcon = warnings.length ? "mdi:alert-outline" : missing ? "mdi:help-circle-outline" : "mdi:shield-check-outline";
    const summaryText = warnings.length ? `${warnings.length} item${warnings.length === 1 ? "" : "s"} to check${missing ? "; data incomplete" : ""}` : missing ? "Vehicle data incomplete" : "No active warnings";
    const heading = `<div class="vehicle-section-title"><ha-icon icon="mdi:car-door"></ha-icon><div><span>Perimeter</span><h2>Doors &amp; openings</h2></div></div>`;
    const lockState = String(this._obj("door_lock")?.state || "").toLowerCase();
    const lockStateReady = ["locked", "unlocked"].includes(lockState);
    const lockControlsEnabled = (this._config.vehicle_controls === true || this._config.vehicle?.controls === true) && this._entity("door_lock")?.startsWith("lock.") && lockStateReady;
    const lockControls = `<div class="vehicle-lock-actions"><button data-action="lock_vehicle" ${!lockControlsEnabled || lockState === "locked" ? "disabled" : ""}><ha-icon icon="mdi:lock"></ha-icon>Lock vehicle</button><button class="unlock" data-action="unlock_vehicle" ${!lockControlsEnabled || lockState === "unlocked" ? "disabled" : ""}><ha-icon icon="mdi:lock-open-variant"></ha-icon>Unlock vehicle</button></div>`;
    return `<main class="vehicle-detail"><header class="vehicle-page-heading card"><div><span>Vehicle detail</span><h2>Ready-state check</h2><p>Review access points, exterior lights, and tire status before departure.</p></div><div class="vehicle-page-meta"><div class="vehicle-page-status ${summaryTone}"><ha-icon icon="${summaryIcon}"></ha-icon><strong>${summaryText}</strong></div>${this._entity("vin") ? `<button class="vehicle-vin${vin.available ? "" : " unavailable"}" data-info="vin"><ha-icon icon="mdi:identifier"></ha-icon><span><small>VIN</small><strong>${this._safe(vin.value)}</strong></span></button>` : ""}</div></header><section class="vehicle-detail-grid">
      <article class="vehicle-section card vehicle-security"><div class="vehicle-section-title"><ha-icon icon="mdi:shield-car"></ha-icon><div><span>Access</span><h2>Locks &amp; lights</h2></div></div><div class="vehicle-state-list">${tile("door_lock", "mdi:car-door-lock", "Door lock", "Vehicle security", unlocked, read("door_lock").available ? (unlocked ? "Unlocked" : "Locked") : undefined)}${tile("charge_port", "mdi:ev-plug-type2", "Charge port", "Port state", this._active("charge_port"), binaryLabel("charge_port", "Open", "Closed"))}${tile("lights", "mdi:car-light-high", "Headlights", "Exterior lights", lightsOn, binaryLabel("lights", "On", "Off"))}${this._entity("smart_key_battery_warning") ? tile("smart_key_battery_warning", "mdi:key-alert", "Smart key battery", "Key status", smartKeyWarning, smartKeyWarning ? "Replace battery" : "Normal") : ""}</div>${lockControls}<p class="vehicle-section-note"><ha-icon icon="mdi:information-outline"></ha-icon>${lockControlsEnabled ? "Remote lock controls require confirmation and verify the returned lock state. Other remote vehicle actions remain read-only." : "Set vehicle_controls: true to enable reviewed lock controls. Other remote vehicle actions remain read-only."}</p>${this._notice ? `<p class="notice">${this._safe(this._notice)}</p>` : ""}</article>
      <article class="vehicle-section card vehicle-openings">${heading}<div class="vehicle-opening-grid">${perimeterOpenings.map(([key, label, context]) => tile(key, key === "trunk" ? "mdi:car-back" : key === "hood" ? "mdi:car-cog" : "mdi:car-door", label, context, this._active(key), binaryLabel(key, "Open", "Closed"))).join("")}</div><div class="vehicle-window-grid">${windowTiles}</div></article>
      <article class="vehicle-section card vehicle-tires"><div class="vehicle-section-title"><ha-icon icon="mdi:car-tire-alert"></ha-icon><div><span>Road contact</span><h2>Tire status</h2></div></div><div class="vehicle-tire-layout"><div>${tile("tire_front_left", "mdi:tire", "Front left", "Tire pressure", this._tireStatus("tire_front_left") !== "OK", tireLabel("tire_front_left"))}${tile("tire_rear_left", "mdi:tire", "Rear left", "Tire pressure", this._tireStatus("tire_rear_left") !== "OK", tireLabel("tire_rear_left"))}</div><img src="${this._asset(this._config.images?.top || "ev6_top.png")}" alt="Top view of vehicle" onerror="this.style.display='none'"><div>${tile("tire_front_right", "mdi:tire", "Front right", "Tire pressure", this._tireStatus("tire_front_right") !== "OK", tireLabel("tire_front_right"))}${tile("tire_rear_right", "mdi:tire", "Rear right", "Tire pressure", this._tireStatus("tire_rear_right") !== "OK", tireLabel("tire_rear_right"))}</div></div></article>
      <article class="vehicle-section card vehicle-warnings"><div class="vehicle-section-title"><ha-icon icon="${warnings.length ? "mdi:alert-circle-outline" : missing ? "mdi:help-circle-outline" : "mdi:check-decagram-outline"}"></ha-icon><div><span>Summary</span><h2>${warnings.length ? "Items to review" : missing ? "Vehicle data incomplete" : "Vehicle checks normal"}</h2></div></div>${warnings.length ? `<ul>${warnings.map((warning) => `<li>${this._safe(warning)}</li>`).join("")}</ul>${missing ? `<p class="vehicle-mapping-note">Vehicle data is incomplete: ${missingKeys.length} signal${missingKeys.length === 1 ? " is" : "s are"} missing or unavailable. Check Settings for vehicle mappings.</p>` : ""}` : `<p>${missing ? `No active warnings in available data, but ${missingKeys.length} signal${missingKeys.length === 1 ? " is" : "s are"} missing or unavailable. Check Settings for vehicle mappings.` : "Locks, openings, lights, charge port, and tire pressure report normal states."}</p>`}</article>
    </section></main>`;
  }

  _renderClimateTab() {
    const climate = this._obj("climate");
    const attrs = climate?.attributes || {};
    const on = this._climateOn();
    const unit = this._hass?.config?.unit_system?.temperature || "°C";
    const target = this._entity("set_temperature") ? this._number("set_temperature") : attrs.temperature ?? "--";
    const cabin = this._entity("cabin_temperature") ? this._number("cabin_temperature") : attrs.current_temperature ?? "--";
    const outside = this._entity("outside_temperature") ? this._number("outside_temperature") : "--";
    const temp = (value) => `${this._safe(value)}${value === "--" ? "" : ` ${this._safe(unit)}`}`;
    const mappedState = (key) => {
      const obj = this._obj(key);
      if (!this._entity(key)) return "Not mapped";
      if (!obj || ["unknown", "unavailable"].includes(obj.state)) return "Unavailable";
      return this._active(key) ? "On" : "Off";
    };
    const comfortTile = (key, icon, label) => {
      const entityId = this._entity(key);
      if (!entityId) return "";
      const domain = entityId.split(".")[0];
      const obj = this._obj(key);
      const unavailable = !obj || obj.state === "unavailable" || (domain !== "button" && obj.state === "unknown");
      const active = !unavailable && this._active(key);
      const value = domain === "button" && !unavailable ? "Ready" : unavailable ? "Unavailable" : String(obj.state).replaceAll("_", " ");
      const actionable = ["button", "switch", "input_boolean"].includes(domain);
      const service = domain === "button" || !active ? "turn_on" : "turn_off";
      const confirmation = "Change climate comfort setting?";
      const interaction = actionable ? `data-entity-action="${key}" data-service="${service}" data-confirm="${confirmation}"` : `data-info="${key}"`;
      return `<button class="climate-comfort-tile${active ? " active" : ""}${unavailable ? " unavailable" : ""}" ${interaction}><ha-icon icon="${icon}"></ha-icon><span>${label}</span><strong>${this._safe(value)}</strong><small>${actionable ? (active ? "Turn off" : "Turn on") : "Open details"}</small></button>`;
    };
    const seatComfort = [
      ["driver_seat", "Driver seat", "driver_seat_heating", "Driver seat heating", "driver_seat_ventilation", "Driver seat ventilation"],
      ["passenger_seat", "Passenger seat", "passenger_seat_heating", "Passenger seat heating", "passenger_seat_ventilation", "Passenger seat ventilation"],
      ["rear_left_seat", "Rear left seat", "rear_left_seat_heating", "Rear left seat heating", "rear_left_seat_ventilation", "Rear left seat ventilation"],
      ["rear_right_seat", "Rear right seat", "rear_right_seat_heating", "Rear right seat heating", "rear_right_seat_ventilation", "Rear right seat ventilation"],
    ].flatMap(([combinedKey, combinedLabel, heatingKey, heatingLabel, ventilationKey, ventilationLabel]) => {
      if (this._entity(combinedKey)) return [comfortTile(combinedKey, "mdi:car-seat", combinedLabel)];
      return [
        comfortTile(heatingKey, "mdi:car-seat-heater", heatingLabel),
        comfortTile(ventilationKey, "mdi:car-seat-cooler", ventilationLabel),
      ];
    }).filter(Boolean).join("");
    const scheduleTiles = [
      ["climate_schedule", "Climate schedule"],
      ["climate_departure_time", "Next departure"],
      ["climate_schedule_1", "Departure schedule 1"],
      ["climate_departure_time_1", "Departure time 1"],
      ["climate_departure_days_1", "Departure days 1"],
      ["climate_schedule_2", "Departure schedule 2"],
      ["climate_departure_time_2", "Departure time 2"],
      ["climate_departure_days_2", "Departure days 2"],
    ].filter(([key]) => this._entity(key)).map(([key, label]) => `<button data-info="${key}"><span>${label}</span><strong>${this._safe(this._state(key, "Unavailable"))}</strong></button>`).join("");
    return `<main class="climate-detail" aria-label="Climate details">
      <section class="climate-intro card"><div><span class="climate-eyebrow">Cabin comfort</span><h2>Climate</h2><p>Review temperature, HVAC state, comfort features, and remote controls.</p></div><div class="climate-state ${on ? "is-on" : ""}"><ha-icon icon="${on ? "mdi:fan" : "mdi:fan-off"}"></ha-icon><span>Climate system</span><strong>${this._safe(on ? "On" : this._state("climate", "Unavailable"))}</strong></div></section>
      <section class="climate-temperature card"><div class="climate-heading"><ha-icon icon="mdi:thermometer"></ha-icon><div><span>Temperature</span><h2>Cabin target</h2></div></div><strong class="climate-target">${temp(target)}</strong><div class="climate-readings"><button data-info="cabin_temperature"><span>Cabin</span><strong>${temp(cabin)}</strong></button><button data-info="outside_temperature"><span>Outside</span><strong>${temp(outside)}</strong></button></div></section>
      <section class="climate-controls card"><div class="climate-heading"><ha-icon icon="mdi:remote"></ha-icon><div><span>Remote controls</span><h2>Prepare the cabin</h2></div></div><div class="climate-actions"><button data-action="start_climate" ${this._entity("start_climate") || this._entity("climate") ? "" : "disabled"}><ha-icon icon="mdi:fan"></ha-icon>Start Climate</button><button class="stop" data-action="stop_climate" ${this._entity("stop_climate") || this._entity("climate") ? "" : "disabled"}><ha-icon icon="mdi:fan-off"></ha-icon>Stop Climate</button><button data-entity-action="vent_windows" data-service="turn_on" data-confirm="Vent all windows now?" ${this._entity("vent_windows") ? "" : "disabled"}><ha-icon icon="mdi:car-door"></ha-icon>Vent all windows</button></div>${this._notice ? `<p class="notice">${this._safe(this._notice)}</p>` : ""}</section>
      <section class="climate-comfort card"><div class="climate-heading"><ha-icon icon="mdi:car-seat"></ha-icon><div><span>Comfort</span><h2>System state</h2></div></div><div class="climate-readings climate-system-grid"><button data-info="climate"><span>HVAC mode</span><strong>${this._safe(attrs.hvac_action || this._state("climate"))}</strong></button><button data-info="defrost"><span>Defrost</span><strong>${mappedState("defrost")}</strong></button><button data-info="steering_wheel_heater"><span>Steering wheel heat</span><strong>${mappedState("steering_wheel_heater")}</strong></button>${comfortTile("rear_window_heater", "mdi:car-defrost-rear", "Rear window heat")}</div>${seatComfort ? `<div class="climate-subheading"><span>Seat comfort</span></div><div class="climate-comfort-grid">${seatComfort}</div>` : ""}</section>
      <section class="climate-session card"><ha-icon icon="mdi:timer-outline"></ha-icon><div class="climate-session-copy"><span>Remote climate session</span><h2>${this._safe(this._entity("last_climate_result") ? this._state("last_climate_result") : "No command result mapped")}</h2><p>${scheduleTiles ? "Schedule and departure context" : "Timer and requested mode will appear when mapped state is available."}</p>${scheduleTiles ? `<div class="climate-readings climate-schedule-grid">${scheduleTiles}</div>` : ""}</div></section>
    </main>`;
  }
  _chargerHistoryDays() {
    return this._chargerHistoryPeriod;
  }

  _chargerHistoryKey() {
    return [this._entity("charger_total_energy") || "", this._chargerHistoryDays(), this._dateKey(new Date())].join("|");
  }

  async _loadChargerHistory(force = false) {
    const entityId = this._entity("charger_total_energy");
    if (!this._hass?.callWS || !entityId) return;
    const requestKey = this._chargerHistoryKey();
    if (!force && this._chargerHistoryState === "loading") return;
    if (!force && this._chargerHistoryState === "ready" && requestKey === this._chargerHistoryRequestKey) return;

    const token = ++this._chargerHistoryRequestToken;
    this._chargerHistoryRequestKey = requestKey;
    this._chargerHistoryState = "loading";
    this._chargerHistoryError = "";
    this._render();
    const end = new Date();
    const start = new Date(end.getTime() - this._chargerHistoryDays() * 86400000);
    try {
      const response = await this._hass.callWS({
        type: "recorder/statistics_during_period",
        start_time: start.toISOString(),
        end_time: end.toISOString(),
        statistic_ids: [entityId],
        period: "day",
        types: ["change", "state"],
      });
      if (token !== this._chargerHistoryRequestToken) return;
      const rows = response?.[entityId] || response?.statistics?.[entityId] || [];
      this._chargerHistory = (Array.isArray(rows) ? rows : [])
        .map((row) => {
          const timestamp = typeof row.start === "number" ? row.start : new Date(row.start).getTime();
          const energy = Number.parseFloat(row.change);
          return { date: this._dateKey(timestamp), energy: Number.isFinite(energy) ? Math.max(0, energy) : 0 };
        })
        .filter((row) => row.date)
        .sort((left, right) => left.date.localeCompare(right.date));
      this._chargerHistoryState = "ready";
    } catch (error) {
      if (token !== this._chargerHistoryRequestToken) return;
      this._chargerHistory = [];
      this._chargerHistoryState = "error";
      this._chargerHistoryError = error?.message || String(error || "Charging history unavailable");
    }
    if (this._activeTab === "energy") this._render();
  }

  _setChargerHistoryPeriod(days) {
    if (![30, 90, 365].includes(days) || days === this._chargerHistoryPeriod) return;
    this._chargerHistoryPeriod = days;
    this._chargerHistoryRequestKey = "";
    return this._loadChargerHistory(true);
  }

  _toggleChargerHistory() {
    this._chargerHistoryExpanded = !this._chargerHistoryExpanded;
    this._render();
  }

  _renderChargerHistoryStatistics(tariff) {
    const days = this._chargerHistoryDays();
    if (!this._entity("charger_total_energy")) {
      return `<div class="charger-history-empty"><ha-icon icon="mdi:chart-timeline-variant"></ha-icon><div><strong>History helpers ready</strong><span>Map charger_total_energy to load permanent Home Assistant energy statistics.</span></div></div>`;
    }
    const controls = `<div class="charger-history-periods" aria-label="Charging history period">${[30, 90, 365].map((period) => `<button data-charger-history-period="${period}" class="${days === period ? "active" : ""}">${period === 365 ? "1 year" : `${period} days`}</button>`).join("")}<button class="refresh" data-charger-history-refresh aria-label="Refresh charging history"><ha-icon icon="mdi:refresh"></ha-icon></button></div>`;
    if (this._chargerHistoryState === "loading" || this._chargerHistoryState === "idle") {
      return `${controls}<div class="charger-history-empty"><ha-icon icon="mdi:loading"></ha-icon><div><strong>Loading charging history</strong><span>Reading permanent Home Assistant energy statistics.</span></div></div>`;
    }
    if (this._chargerHistoryState === "error") {
      return `${controls}<div class="charger-history-empty warning"><ha-icon icon="mdi:alert-circle-outline"></ha-icon><div><strong>Charging history unavailable</strong><span>${this._safe(this._chargerHistoryError)}</span></div></div>`;
    }
    const threshold = Math.max(0, Number.parseFloat(this._config.charger_history_min_kwh ?? 0.2) || 0);
    const rows = this._chargerHistory;
    const chargingDays = rows.filter((row) => row.energy >= threshold);
    const total = rows.reduce((sum, row) => sum + row.energy, 0);
    const average = chargingDays.length ? chargingDays.reduce((sum, row) => sum + row.energy, 0) / chargingDays.length : 0;
    const cost = Number.isFinite(tariff) ? total * tariff : null;
    const chartRows = rows.slice(-31);
    const maximum = Math.max(0.1, ...chartRows.map((row) => row.energy));
    const chart = chartRows.length ? `<div class="charger-history-chart" role="img" aria-label="Daily charger energy over the latest ${chartRows.length} days"><div class="charger-history-bars">${chartRows.map((row) => `<div title="${this._safe(`${this._formatHistoryDay(row.date)}: ${row.energy.toFixed(2)} kWh`)}"><i class="${row.energy >= threshold ? "charging" : "idle"}" style="height:${Math.max(2, Math.round((row.energy / maximum) * 100))}%"></i><span>${this._safe(row.date.slice(5).replace("-", "/"))}</span></div>`).join("")}</div></div>` : "";
    const details = chargingDays.slice().reverse().map((row) => `<div role="row"><strong role="cell">${this._safe(this._formatHistoryDay(row.date))}</strong><span role="cell">${row.energy.toFixed(2)} kWh</span><span role="cell">${Number.isFinite(tariff) ? `EUR ${(row.energy * tariff).toFixed(2)}` : "--"}</span></div>`).join("");
    return `${controls}<div class="charger-history-summary"><div><span>Energy used</span><strong>${total.toFixed(1)} kWh</strong></div><div><span>Charging days</span><strong>${chargingDays.length}</strong></div><div><span>Average per charging day</span><strong>${average.toFixed(1)} kWh</strong></div><div><span>Estimated period cost</span><strong>${cost === null ? "--" : `EUR ${cost.toFixed(2)}`}</strong></div></div>${chart}${details ? `<button class="charger-history-toggle" data-charger-history-toggle aria-expanded="${this._chargerHistoryExpanded}"><span>${this._chargerHistoryExpanded ? "Hide daily charging data" : "Show daily charging data"}</span><ha-icon icon="mdi:chevron-${this._chargerHistoryExpanded ? "up" : "down"}"></ha-icon></button><div class="charger-history-table" role="table" ${this._chargerHistoryExpanded ? "" : "hidden"}><div class="header" role="row"><strong role="columnheader">Day</strong><strong role="columnheader">Energy</strong><strong role="columnheader">Cost</strong></div>${details}</div>` : `<div class="charger-history-empty"><ha-icon icon="mdi:calendar-blank-outline"></ha-icon><div><strong>No charging days in this period</strong><span>Days below ${threshold} kWh are treated as charger standby consumption.</span></div></div>`}`;
  }

  _renderEnergyTab() {
    const battery = this._number("battery_level");
    const batteryUnit = this._unit("battery_level", "%") || "%";
    const range = this._number("battery_range");
    const rangeUnit = this._unit("battery_range", "km") || "km";
    const chargeTarget = this._number("charging_limit");
    const chargeTargetUnit = this._unit("charging_limit", "%") || "%";
    const chargingPower = this._number("charging_power");
    const chargingPowerUnit = this._unit("charging_power", "kW") || "kW";
    const averageConsumption = this._number("average_energy_consumption");
    const averageConsumptionUnit = this._unit("average_energy_consumption", "kWh/100 km");
    const consumption90d = this._number("energy_consumption_90d");
    const consumption90dUnit = this._unit("energy_consumption_90d", "kWh/100 km");
    const chargingState = this._charging() ? "Charging" : this._state("charging_state");
    const plugObject = this._obj("plug_connected");
    const plugAvailable = Boolean(plugObject) && !["unknown", "unavailable"].includes(plugObject.state);
    const plugConnected = plugAvailable && this._active("plug_connected");
    const plugState = plugAvailable ? (plugConnected ? "Connected" : "Disconnected") : "--";
    const todayDriving = this._todayDriving();
    const drivingHistory = this._drivingHistory().slice(-14);
    const totalRegeneration = this._number("total_energy_regeneration");
    const totalRegenerationUnit = this._unit("total_energy_regeneration", "Wh");
    const displayEnergy = (value, unit = "Wh") => {
      if (!Number.isFinite(Number(value))) return "--";
      const kwh = String(unit).toLowerCase() === "kwh" ? Number(value) : Number(value) / 1000;
      return `${Math.round(kwh * 10) / 10} kWh`;
    };
    const todayEfficiency = todayDriving?.distance > 0 ? Math.round((todayDriving.consumed / 1000 / todayDriving.distance) * 1000) / 10 : "--";
    const todayRegenRatio = todayDriving?.consumed > 0 ? Math.round((todayDriving.regenerated / todayDriving.consumed) * 100) : "--";
    const chartMaximum = Math.max(1, ...drivingHistory.flatMap((day) => [day.consumed, day.regenerated]));
    const drivingBars = drivingHistory.map((day) => {
      const consumedHeight = Math.max(3, Math.round((day.consumed / chartMaximum) * 100));
      const regeneratedHeight = Math.max(3, Math.round((day.regenerated / chartMaximum) * 100));
      const label = day.date.slice(5).replace("-", "/");
      const title = `${day.date}: ${displayEnergy(day.consumed)} consumed, ${displayEnergy(day.regenerated)} regenerated, ${Math.round(day.distance * 10) / 10} km`;
      return `<div class="driving-day" title="${this._safe(title)}"><div><i class="consumed" style="height:${consumedHeight}%"></i><i class="regenerated" style="height:${regeneratedHeight}%"></i></div><span>${this._safe(label)}</span></div>`;
    }).join("");
    const drivingAnalytics = todayDriving || drivingHistory.length || totalRegeneration !== "--" ? `
      <section class="energy-section driving-analytics card"><div class="energy-heading"><span><ha-icon icon="mdi:chart-box-outline"></ha-icon></span><div><small>Driving analytics</small><h2>Daily driving history</h2></div></div>
        <div class="driving-summary">
          <button data-info="today_driving_stats"><span>Distance today</span><strong>${this._safe(todayDriving ? Math.round(todayDriving.distance * 10) / 10 : "--")} <em>km</em></strong></button>
          <button data-info="today_driving_stats"><span>Average energy consumption</span><strong>${this._safe(todayEfficiency)} <em>kWh/100 km</em></strong></button>
          <button data-info="today_driving_stats"><span>Energy regenerated</span><strong>${this._safe(todayDriving ? displayEnergy(todayDriving.regenerated) : "--")}</strong><small>${this._safe(todayRegenRatio)}${todayRegenRatio === "--" ? "" : "%"} of consumption</small></button>
          <button data-info="total_energy_regeneration"><span>Total regeneration</span><strong>${this._safe(totalRegeneration === "--" ? "--" : displayEnergy(totalRegeneration, totalRegenerationUnit))}</strong></button>
        </div>
        ${drivingBars ? `<div class="driving-chart" role="img" aria-label="Daily consumption and regenerated energy over the latest ${drivingHistory.length} driving days"><div class="driving-chart-legend"><span><i class="consumed"></i>Consumed</span><span><i class="regenerated"></i>Regenerated</span></div><div class="driving-chart-bars">${drivingBars}</div></div>` : ""}
      </section>` : "";

    const stat = (key, icon, label, value, unit = "", status = "") => `<button class="energy-stat${status ? ` ${status}` : ""}" data-info="${key}"><span class="energy-stat-icon"><ha-icon icon="${icon}"></ha-icon></span><span class="energy-stat-copy"><small>${label}</small><strong>${this._safe(value)}${unit && value !== "--" ? ` <em>${this._safe(unit)}</em>` : ""}</strong></span><ha-icon class="energy-stat-link" icon="mdi:chevron-right"></ha-icon></button>`;
    const usable = (key) => {
      const obj = this._obj(key);
      return Boolean(obj) && !["unknown", "unavailable", ""].includes(String(obj.state).toLowerCase());
    };
    const chargerState = this._state("charger_status", "Not mapped");
    const chargerActive = ["charging", "started"].includes(String(chargerState).toLowerCase());
    const chargerOnline = this._active("charger_online");
    const chargerPower = this._number("charger_power");
    const chargerCurrent = this._number("charger_current", "--", 3);
    const sessionEnergy = this._number("charger_session_energy", "--", 3);
    const totalEnergy = this._number("charger_total_energy", "--", 3);
    const chargerMode = this._state("charger_mode", "Not mapped");
    const controlsEnabled = this._config.charger_controls === true || this._config.charger?.controls === true;
    const modes = this._chargerModes();
    const modeEntity = this._entity("charger_mode");
    const modeDomainSupported = modeEntity && ["select", "input_select"].includes(modeEntity.split(".")[0]);
    const modeButtons = modes.map(([option, icon, label]) => {
      const active = chargerMode.toLowerCase() === String(option).toLowerCase();
      return `<button class="charger-mode${active ? " active" : ""}" data-select="charger_mode" data-option="${this._safe(option)}" ${!controlsEnabled || !modeDomainSupported ? "disabled" : ""}><ha-icon icon="${icon}"></ha-icon><span>${label}</span></button>`;
    }).join("");
    const actionButton = (action, key, icon, label, tone = "") => `<button class="charger-action${tone ? ` ${tone}` : ""}" data-action="${action}" ${!controlsEnabled || !this._entity(key) ? "disabled" : ""}><ha-icon icon="${icon}"></ha-icon><span>${label}</span></button>`;
    const resumeEntityKey = this._config.charger_resume_via_mode === true ? "charger_mode" : "charger_resume";
    const currentAttrs = this._rangeAttrs("charger_current_limit", 6, 32, 1);
    const currentLimit = this._number("charger_current_limit");
    const session = this._obj("charger_session_energy");
    const sessionAttrs = session?.attributes || {};
    const rawSessionDate = sessionAttrs.end_time ?? sessionAttrs.endTime ?? sessionAttrs.start_time ?? sessionAttrs.startTime ?? sessionAttrs.syncDate;
    const sessionDate = rawSessionDate ? this._formatDate(Number.isFinite(Number(rawSessionDate)) ? new Date(Number(rawSessionDate)).toISOString() : rawSessionDate) : "No timestamp available";
    const explicitCost = usable("charger_session_cost") ? this._number("charger_session_cost", "--", 2) : null;
    const mappedPrice = usable("charger_energy_price") ? this._number("charger_energy_price", "--", 5) : null;
    const mappedPriceUnit = this._unit("charger_energy_price", "EUR/kWh");
    const mappedPriceInEur = mappedPrice !== null && mappedPrice !== "--"
      ? mappedPrice / (/(?:ct|cent|câ‚¬)\s*\/\s*kwh/i.test(mappedPriceUnit) ? 100 : 1)
      : null;
    const tariffItems = Array.isArray(sessionAttrs.hostTariffComponents) ? sessionAttrs.hostTariffComponents : [];
    const sessionTariff = Number.parseFloat(tariffItems.find((item) => String(item?.unit).toLowerCase() === "kwh")?.cost);
    const tariff = Number.isFinite(mappedPriceInEur) ? mappedPriceInEur : sessionTariff;
    const estimatedCost = explicitCost !== null ? explicitCost : Number.isFinite(tariff) && sessionEnergy !== "--" ? Math.round(tariff * sessionEnergy * 100) / 100 : "--";
    const tariffContext = explicitCost !== null
      ? "From mapped session cost"
      : Number.isFinite(tariff)
        ? `Based on EUR ${this._safe(tariff)} / kWh${Number.isFinite(mappedPriceInEur) ? " from mapped energy price" : ""}`
        : "Map charger_energy_price, charger_session_cost, or expose a tariff attribute";
    const monthEnergy = usable("charger_energy_month") ? this._number("charger_energy_month", "--", 3) : null;
    const explicitMonthCost = usable("charger_cost_month") ? this._number("charger_cost_month", "--", 2) : null;
    const monthCost = explicitMonthCost !== null
      ? explicitMonthCost
      : Number.isFinite(mappedPriceInEur) && monthEnergy !== null && monthEnergy !== "--"
        ? Math.round(mappedPriceInEur * monthEnergy * 100) / 100
        : null;
    const historyItems = [
      ["charger_energy_today", "mdi:calendar-today", "Today", "kWh"],
      ["charger_energy_week", "mdi:calendar-week", "This week", "kWh"],
      ["charger_energy_month", "mdi:calendar-month", "This month", "kWh"],
      ["charger_energy_year", "mdi:calendar-range", "This year", "kWh"],
    ];
    const historyCards = [
      ...historyItems.filter(([key]) => usable(key)).map(([key, icon, label, unit]) => stat(key, icon, label, this._number(key), this._unit(key, unit))),
      ...(mappedPrice !== null && mappedPrice !== "--" ? [stat("charger_energy_price", "mdi:currency-eur", "Energy price", mappedPrice, mappedPriceUnit)] : []),
      ...(monthCost !== null ? [stat(explicitMonthCost !== null ? "charger_cost_month" : "charger_energy_month", "mdi:cash", "Monthly cost", monthCost, "EUR")] : []),
    ].join("");

    return `<main class="energy-detail" aria-label="Energy details">
      <section class="energy-intro card"><div><span class="energy-eyebrow">Home charging</span><h2>Vehicle and charger in one energy view</h2><p>Follow the live charging session, choose a charging strategy, and compare home energy flows without coupling the dashboard to one charger brand.</p></div><div class="energy-orbit" aria-hidden="true"><ha-icon icon="mdi:ev-station"></ha-icon></div></section>
      <section class="energy-section vehicle-energy card"><div class="energy-heading"><span><ha-icon icon="mdi:map-marker-distance"></ha-icon></span><div><small>Vehicle energy</small><h2>Range and connection</h2></div></div><div class="energy-stats">${stat("battery_range", "mdi:map-marker-distance", "Estimated range", range, rangeUnit)}${stat("battery_level", "mdi:battery-high", "Battery level", battery, batteryUnit, battery === "--" ? "" : "positive")}${stat("charging_limit", "mdi:battery-charging-80", "AC charge target", chargeTarget, chargeTargetUnit)}${stat("charging_state", "mdi:ev-station", "Vehicle charging state", chargingState, "", this._charging() ? "positive" : "")}${stat("charging_power", "mdi:flash", "Vehicle charging power", chargingPower, chargingPowerUnit, this._charging() ? "positive" : "")}${stat("plug_connected", "mdi:power-plug", "Vehicle plug", plugState, "", plugConnected ? "positive" : "")}${stat("average_energy_consumption", "mdi:gauge", "Average energy consumption", averageConsumption, averageConsumptionUnit)}${stat("energy_consumption_90d", "mdi:chart-timeline-variant", "90-day energy consumption", consumption90d, consumption90dUnit)}</div><p class="energy-note"><ha-icon icon="mdi:information-outline"></ha-icon><span>Vehicle data remains available when no separate home charger is configured. The Battery page owns vehicle charge limits.</span></p></section>
      ${drivingAnalytics}
      <section class="energy-section charger-live card"><div class="energy-heading"><span><ha-icon icon="mdi:ev-station"></ha-icon></span><div><small>Home charger</small><h2>Live session</h2></div><span class="charger-health ${chargerOnline ? "online" : ""}"><i></i>${chargerOnline ? "Online" : usable("charger_online") ? "Offline" : "Not mapped"}</span></div><div class="charger-live-hero"><div><span>EVSE status</span><strong class="${chargerActive ? "positive-text" : ""}">${this._safe(this._chargerStatusLabel(chargerState))}</strong><small>${this._safe(chargerMode)}</small></div><ha-icon icon="${chargerActive ? "mdi:battery-charging" : "mdi:ev-plug-type2"}"></ha-icon></div><div class="energy-stats compact">${stat("charger_power", "mdi:flash", "Charger power", chargerPower, this._unit("charger_power", "W"), chargerActive ? "positive" : "")}${stat("charger_session_energy", "mdi:counter", "Session energy", sessionEnergy, this._unit("charger_session_energy", "kWh"))}${stat("charger_current", "mdi:current-ac", "Connector current", chargerCurrent, this._unit("charger_current", "A"))}${stat("charger_total_energy", "mdi:transmission-tower-import", "Total imported", totalEnergy, this._unit("charger_total_energy", "kWh"))}</div></section>
      <section class="energy-section charger-control card"><div class="energy-heading"><span><ha-icon icon="mdi:tune-variant"></ha-icon></span><div><small>Charging strategy</small><h2>Charger control</h2></div><span class="charger-safety"><ha-icon icon="mdi:shield-check-outline"></ha-icon>${controlsEnabled ? "Enabled" : "Read only"}</span></div><div class="charger-modes">${modeButtons}</div><div class="charger-current"><div><span>Charging current</span><strong>${this._safe(currentLimit)}${currentLimit !== "--" ? ` ${this._safe(this._unit("charger_current_limit", "A"))}` : ""}</strong></div><input type="range" min="${currentAttrs.min}" max="${currentAttrs.max}" step="${currentAttrs.step}" value="${currentLimit === "--" ? currentAttrs.min : currentLimit}" data-number="charger_current_limit" data-confirm="Change the home charger current limit?" ${!controlsEnabled || !this._entity("charger_current_limit") ? "disabled" : ""}></div><div class="charger-actions">${actionButton("charger_start", "charger_start", "mdi:play", "Start", "start")}${actionButton("charger_pause", "charger_pause", "mdi:pause", "Pause")}${actionButton("charger_resume", resumeEntityKey, "mdi:play-pause", "Resume")}${actionButton("charger_stop", "charger_stop", "mdi:stop", "Stop", "stop")}</div>${this._notice ? `<p class="energy-notice">${this._safe(this._notice)}</p>` : ""}<p class="energy-note"><ha-icon icon="mdi:information-outline"></ha-icon><span>${controlsEnabled ? "Commands use the mapped Home Assistant entities and ask for confirmation." : "Set charger_controls: true after reviewing the mapped action entities to enable controls."}</span></p></section>
      <section class="energy-section charger-flow card"><div class="energy-heading"><span><ha-icon icon="mdi:home-lightning-bolt-outline"></ha-icon></span><div><small>Home energy</small><h2>Power flow context</h2></div></div><div class="energy-stats compact">${stat("charger_pv_power", "mdi:solar-power", "Solar production", this._number("charger_pv_power"), this._unit("charger_pv_power", "W"), "positive")}${stat("charger_house_power", "mdi:home-lightning-bolt", "Home consumption", this._number("charger_house_power"), this._unit("charger_house_power", "W"))}${stat("charger_grid_power", "mdi:transmission-tower", "Grid power", this._number("charger_grid_power"), this._unit("charger_grid_power", "W"))}${stat("charger_grid_support", "mdi:transmission-tower-export", "Grid support", this._number("charger_grid_support"), this._unit("charger_grid_support", "A"))}</div><p class="energy-note"><ha-icon icon="mdi:leaf"></ha-icon><span>Smart and Solar remain charger-defined strategies; this card only reflects and selects the mapped Home Assistant mode.</span></p></section>
      <section class="energy-section charger-history card"><div class="energy-heading"><span><ha-icon icon="mdi:history"></ha-icon></span><div><small>Charging history</small><h2>Latest session and totals</h2></div></div><div class="charger-session-summary"><div><span>Latest session</span><strong>${this._safe(sessionEnergy)}${sessionEnergy !== "--" ? ` ${this._safe(this._unit("charger_session_energy", "kWh"))}` : ""}</strong><small>${this._safe(sessionDate)}</small></div><div><span>Estimated cost</span><strong>${estimatedCost === "--" ? "--" : `EUR ${this._safe(Number(estimatedCost).toFixed(2))}`}</strong><small>${tariffContext}</small></div></div>${historyCards ? `<div class="charger-history-grid">${historyCards}</div>` : ""}<div class="charger-long-term"><div class="charger-long-term-heading"><div><span>Long-term statistics</span><h3>Energy history</h3></div><small>From Home Assistant Recorder</small></div>${this._renderChargerHistoryStatistics(tariff)}</div><button class="energy-back" data-nav="overview"><ha-icon icon="mdi:arrow-left"></ha-icon><span>Back to Overview</span></button></section>
    </main>`;
  }

  _formatHistoryDay(value) {
    const date = new Date(`${value}T12:00:00`);
    if (Number.isNaN(date.getTime())) return value;
    return new Intl.DateTimeFormat(this._hass?.locale?.language || navigator.language || "nl-BE", {
      weekday: "short",
      day: "2-digit",
      month: "short",
    }).format(date);
  }

  _formatHistoryTime(value) {
    const date = new Date(value);
    if (Number.isNaN(date.getTime())) return "--";
    return new Intl.DateTimeFormat(this._hass?.locale?.language || navigator.language || "nl-BE", {
      hour: "2-digit",
      minute: "2-digit",
    }).format(date);
  }

  _formatTripDuration(minutes) {
    const hours = Math.floor(minutes / 60);
    const remainder = minutes % 60;
    return hours ? `${hours} h ${remainder} min` : `${remainder} min`;
  }

  _toggleDailyHistory() {
    this._dailyHistoryExpanded = !this._dailyHistoryExpanded;
    this._render();
  }

  _renderLocationDailyHistory() {
    const configured = Number.parseInt(this._config.daily_history_limit ?? 30, 10);
    const limit = Math.max(7, Math.min(31, Number.isFinite(configured) ? configured : 30));
    const days = this._drivingDays().filter((day) => day.distance > 0).slice(-limit).reverse();
    if (!days.length) return "";
    const totalDistance = days.reduce((sum, day) => sum + day.distance, 0);
    const totalConsumed = days.reduce((sum, day) => sum + day.consumed, 0) / 1000;
    const totalRegenerated = days.reduce((sum, day) => sum + day.regenerated, 0) / 1000;
    const weightedConsumption = totalDistance > 0 ? (totalConsumed / totalDistance) * 100 : 0;
    const rows = days.map((day) => {
      const consumption = day.distance > 0 ? (day.consumed / 1000 / day.distance) * 100 : 0;
      return `<div class="location-daily-row" role="row">
        <strong role="cell">${this._safe(this._formatHistoryDay(day.date))}</strong>
        <span role="cell">${this._safe(day.distance.toFixed(1))} km</span>
        <span role="cell">${this._safe(consumption.toFixed(1))} kWh/100 km</span>
        <span role="cell">${this._safe((day.consumed / 1000).toFixed(1))} kWh</span>
        <span role="cell">${this._safe((day.regenerated / 1000).toFixed(1))} kWh</span>
        <span role="cell">${this._safe((day.climateConsumed / 1000).toFixed(1))} kWh</span>
      </div>`;
    }).join("");
    return `<section class="location-history-section location-daily card">
      <div class="location-history-heading"><ha-icon icon="mdi:chart-timeline-variant"></ha-icon><div><span>Driving history</span><h2>Daily driving data</h2><p>Official Kia totals for the latest ${days.length} driving days.</p></div></div>
      <div class="location-period-summary">
        <div><span>Distance</span><strong>${this._safe(totalDistance.toFixed(1))} km</strong></div>
        <div><span>Energy used</span><strong>${this._safe(totalConsumed.toFixed(1))} kWh</strong></div>
        <div><span>Average consumption</span><strong>${this._safe(weightedConsumption.toFixed(1))} kWh/100 km</strong></div>
        <div><span>Regenerated</span><strong>${this._safe(totalRegenerated.toFixed(1))} kWh</strong></div>
      </div>
      <button class="location-daily-toggle" data-daily-history-toggle aria-expanded="${this._dailyHistoryExpanded}" aria-controls="location-daily-details"><span>${this._dailyHistoryExpanded ? "Hide daily details" : "Show daily details"}</span><ha-icon icon="mdi:chevron-${this._dailyHistoryExpanded ? "up" : "down"}"></ha-icon></button>
      <div id="location-daily-details" class="location-daily-table" role="table" aria-label="Daily driving data" ${this._dailyHistoryExpanded ? "" : "hidden"}>
        <div class="location-daily-row location-daily-header" role="row"><span role="columnheader">Day</span><span role="columnheader">Distance</span><span role="columnheader">Consumption</span><span role="columnheader">Energy</span><span role="columnheader">Regenerated</span><span role="columnheader">Climate</span></div>
        ${rows}
      </div>
    </section>`;
  }

  _tripTotals(trips) {
    const totalDistance = trips.reduce((sum, trip) => sum + (trip.distance || 0), 0);
    const totalEnergy = trips.reduce((sum, trip) => sum + (trip.usedEnergy || 0), 0);
    const regeneratedEnergy = trips.reduce((sum, trip) => sum + (trip.regeneratedEnergy || 0), 0);
    const totalMinutes = trips.reduce((sum, trip) => sum + (trip.durationMinutes || 0), 0);
    return {
      count: trips.length,
      distance: totalDistance,
      energy: totalEnergy,
      regeneratedEnergy,
      durationMinutes: totalMinutes,
      consumption: totalDistance > 0 && totalEnergy > 0 ? (totalEnergy / totalDistance) * 100 : null,
    };
  }

  _renderTripSummary(trips, label) {
    const totals = this._tripTotals(trips);
    return `<div class="location-trip-summary" aria-label="${this._safe(label)}">
      <div><span>Stored trips</span><strong>${totals.count}</strong></div>
      <div><span>Distance</span><strong>${totals.distance.toFixed(1)} km</strong></div>
      <div><span>Energy used</span><strong>${totals.energy.toFixed(1)} kWh</strong>${totals.regeneratedEnergy > 0 ? `<small>${totals.regeneratedEnergy.toFixed(1)} kWh recovered</small>` : ""}</div>
      <div><span>Average consumption</span><strong>${totals.consumption === null ? "--" : `${totals.consumption.toFixed(1)} kWh/100 km`}</strong></div>
    </div>`;
  }

  _renderTripRouteMap(trips) {
    const routes = trips.map((trip) => Array.isArray(trip.routePoints) ? trip.routePoints.filter((point) => Number.isFinite(point.lat) && Number.isFinite(point.lon)) : []).filter((route) => route.length >= 2);
    if (!routes.length) return "";
    const width = 1200;
    const height = 430;
    const padding = 48;
    const project = (point, zoom) => {
      const scale = 256 * (2 ** zoom);
      const latitude = Math.max(-85.05112878, Math.min(85.05112878, point.lat));
      const sine = Math.sin((latitude * Math.PI) / 180);
      return {
        x: ((point.lon + 180) / 360) * scale,
        y: (0.5 - Math.log((1 + sine) / (1 - sine)) / (4 * Math.PI)) * scale,
      };
    };
    const allPoints = routes.flat();
    let zoom = 16;
    for (let candidate = 17; candidate >= 3; candidate -= 1) {
      const candidatePoints = allPoints.map((point) => project(point, candidate));
      const xs = candidatePoints.map((point) => point.x);
      const ys = candidatePoints.map((point) => point.y);
      if (Math.max(...xs) - Math.min(...xs) <= width - padding * 2 && Math.max(...ys) - Math.min(...ys) <= height - padding * 2) {
        zoom = candidate;
        break;
      }
    }
    const projected = allPoints.map((point) => project(point, zoom));
    const xs = projected.map((point) => point.x);
    const ys = projected.map((point) => point.y);
    const center = { x: (Math.min(...xs) + Math.max(...xs)) / 2, y: (Math.min(...ys) + Math.max(...ys)) / 2 };
    const left = center.x - width / 2;
    const top = center.y - height / 2;
    const scale = 2 ** zoom;
    const firstTileX = Math.floor(left / 256);
    const lastTileX = Math.floor((left + width) / 256);
    const firstTileY = Math.floor(top / 256);
    const lastTileY = Math.floor((top + height) / 256);
    const tiles = [];
    for (let tileY = firstTileY; tileY <= lastTileY; tileY += 1) {
      if (tileY < 0 || tileY >= scale) continue;
      for (let tileX = firstTileX; tileX <= lastTileX; tileX += 1) {
        const wrappedX = ((tileX % scale) + scale) % scale;
        tiles.push(`<image href="https://tile.openstreetmap.org/${zoom}/${wrappedX}/${tileY}.png" x="${tileX * 256 - left}" y="${tileY * 256 - top}" width="256" height="256"></image>`);
      }
    }
    const lines = routes.map((route, index) => {
      const points = route.map((point) => {
        const value = project(point, zoom);
        return `${(value.x - left).toFixed(1)},${(value.y - top).toFixed(1)}`;
      }).join(" ");
      const start = project(route[0], zoom);
      const end = project(route.at(-1), zoom);
      return `<polyline class="trip-route-line route-${index % 4}" points="${points}"></polyline><circle class="trip-route-point start" cx="${(start.x - left).toFixed(1)}" cy="${(start.y - top).toFixed(1)}" r="8"></circle><circle class="trip-route-point end" cx="${(end.x - left).toFixed(1)}" cy="${(end.y - top).toFixed(1)}" r="8"></circle>`;
    }).join("");
    const pointCount = routes.reduce((sum, route) => sum + route.length, 0);
    const phoneAssisted = trips.some((trip) => /^(phone|driver)/.test(String(trip.routeSource || "").toLowerCase()));
    const sourceLabel = phoneAssisted ? "Phone-assisted route points" : "Available Kia location points";
    return `<section class="trip-route-map" aria-label="Approximate routes"><div class="trip-route-map-heading"><div><span>Route overview</span><strong>${sourceLabel}</strong></div><small>${pointCount} points</small></div><div class="trip-route-map-canvas"><svg viewBox="0 0 ${width} ${height}" preserveAspectRatio="xMidYMid slice" role="img" aria-label="Approximate trip route">${tiles.join("")}<g>${lines}</g></svg><span class="trip-route-quality">Approximate route</span><a href="https://www.openstreetmap.org/copyright" target="_blank" rel="noopener">© OpenStreetMap</a></div></section>`;
  }

  _renderTripRoutePlaceholder(message = "No route points available for this day") {
    return `<div class="trip-route-placeholder"><ha-icon icon="mdi:map-marker-path"></ha-icon><div><strong>Route overview</strong><span>${message}</span></div></div>`;
  }

  _renderTripCards(trips) {
    const rows = trips.map((trip) => {
      const battery = trip.startBattery !== null && trip.endBattery !== null ? `${trip.startBattery.toFixed(1)}% → ${trip.endBattery.toFixed(1)}%` : "--";
      const distance = trip.distance !== null ? `${trip.distance.toFixed(1)} km` : "--";
      const energy = trip.usedEnergy !== null ? `${trip.usedEnergy.toFixed(1)} kWh` : "--";
      const consumption = trip.consumption !== null ? `${trip.consumption.toFixed(1)} kWh/100 km` : "--";
      const speed = trip.averageSpeed !== null ? `${trip.averageSpeed.toFixed(0)} km/h` : "--";
      const odometer = trip.endOdometer !== null ? `${trip.endOdometer.toFixed(1)} km` : "--";
      const breakdown = [["Drive", trip.driveEnergy], ["Climate", trip.climateEnergy], ["Electronics", trip.electronicsEnergy], ["Battery care", trip.batteryCareEnergy], ["Recovered", trip.regeneratedEnergy]].filter(([, value]) => value !== null);
      return `<details class="location-trip-row"><summary><span class="trip-cell trip-start"><small>${this._safe(this._formatHistoryDay(this._dateKey(trip.start)))}</small><strong>${this._safe(this._formatHistoryTime(trip.start))}</strong></span><span class="trip-cell"><small>End</small><strong>${this._safe(this._formatHistoryTime(trip.end))}</strong></span><span class="trip-cell"><small>Duration</small><strong>${this._safe(this._formatTripDuration(trip.durationMinutes))}</strong></span><span class="trip-cell"><small>Distance</small><strong>${this._safe(distance)}</strong></span><span class="trip-cell"><small>Used</small><strong>${this._safe(energy)}</strong></span><span class="trip-cell"><small>Consumption</small><strong>${this._safe(consumption)}</strong></span><span class="trip-cell"><small>Average speed</small><strong>${this._safe(speed)}</strong></span><span class="trip-cell trip-odometer"><small>Odometer</small><strong>${this._safe(odometer)}</strong></span><ha-icon icon="mdi:chevron-down"></ha-icon></summary><div class="location-trip-detail"><div class="location-trip-route"><span>Route</span><h3>${this._safe(trip.origin)} <ha-icon icon="mdi:arrow-right"></ha-icon> ${this._safe(trip.destination)}</h3><small>${this._safe(battery)}</small></div>${breakdown.length ? `<div class="trip-energy-breakdown">${breakdown.map(([label, value]) => `<span><small>${label}</small><strong>${value.toFixed(2)} kWh</strong></span>`).join("")}</div>` : `<p>Detailed energy breakdown is available for newly recorded trips.</p>`}</div></details>`;
    }).join("");
    return `<div class="location-trip-table"><div class="location-trip-table-head"><span>Start</span><span>End</span><span>Duration</span><span>Distance</span><span>Used</span><span>Consumption</span><span>Average speed</span><span>Odometer</span><span></span></div>${rows}</div>`;
  }

  _renderTripCalendar() {
    const [year, month] = this._tripCalendarMonth.split("-").map(Number);
    const first = new Date(year, month - 1, 1);
    const offset = (first.getDay() + 6) % 7;
    const gridStart = new Date(year, month - 1, 1 - offset);
    const locale = this._hass?.locale?.language || navigator.language || "en";
    const monthLabel = new Intl.DateTimeFormat(locale, { month: "long", year: "numeric" }).format(first);
    const tripCounts = new Map();
    this._calendarTrips.forEach((trip) => {
      const key = this._dateKey(trip.start);
      tripCounts.set(key, (tripCounts.get(key) || 0) + 1);
    });
    const weekdays = Array.from({ length: 7 }, (_, index) => new Intl.DateTimeFormat(locale, { weekday: "short" }).format(new Date(2024, 0, 1 + index)));
    const cells = Array.from({ length: 42 }, (_, index) => {
      const date = new Date(gridStart.getFullYear(), gridStart.getMonth(), gridStart.getDate() + index);
      const key = this._dateKey(date);
      const count = tripCounts.get(key) || 0;
      const classes = [date.getMonth() !== month - 1 ? "outside" : "", key === this._tripSelectedDate ? "selected" : "", key === this._dateKey(new Date()) ? "today" : "", count ? "has-trips" : ""].filter(Boolean).join(" ");
      return `<button class="${classes}" data-trip-date="${key}" aria-label="${this._safe(this._formatHistoryDay(key))}${count ? `, ${count} trips` : ""}"><span>${date.getDate()}</span>${count ? `<small>${count}</small>` : ""}</button>`;
    }).join("");
    return `<div class="trip-calendar">
      <div class="trip-calendar-toolbar"><button data-trip-month="-1" aria-label="Previous month"><ha-icon icon="mdi:chevron-left"></ha-icon></button><strong>${this._safe(monthLabel)}</strong><button data-trip-month="1" aria-label="Next month"><ha-icon icon="mdi:chevron-right"></ha-icon></button></div>
      <div class="trip-calendar-weekdays">${weekdays.map((day) => `<span>${this._safe(day)}</span>`).join("")}</div>
      <div class="trip-calendar-grid">${cells}</div>
      <button class="trip-calendar-today" data-trip-date="${this._dateKey(new Date())}">Today</button>
    </div>`;
  }

  _renderPersistentTripHistory() {
    const isDay = this._tripViewMode === "day";
    const selectedTrips = isDay ? this._calendarTrips.filter((trip) => this._dateKey(trip.start) === this._tripSelectedDate) : this._calendarTrips;
    const renderLimit = this._tripCalendarLimit();
    const shownTrips = selectedTrips.slice(0, renderLimit);
    const routeMap = this._renderTripRouteMap(shownTrips);
    let content = "";
    if (this._tripCalendarState === "loading" || this._tripCalendarState === "idle") {
      content = `<div class="location-history-empty"><ha-icon icon="mdi:loading"></ha-icon><div><strong>Loading calendar history</strong><span>Reading stored trips from the mapped Home Assistant calendar.</span></div></div>`;
    } else if (this._tripCalendarState === "error") {
      content = `<div class="location-history-empty warning"><ha-icon icon="mdi:calendar-alert"></ha-icon><div><strong>Calendar history unavailable</strong><span>${this._safe(this._tripCalendarError)}</span></div></div>`;
    } else if (!selectedTrips.length) {
      content = `<div class="location-history-empty"><ha-icon icon="mdi:calendar-blank-outline"></ha-icon><div><strong>${isDay ? "No stored trips for this day" : "No stored trips found"}</strong><span>${isDay ? "Choose another date or switch to Overview." : "New trips appear after the Home Assistant trip package records a completed drive."}</span></div></div>`;
    } else {
      content = `${this._renderTripSummary(selectedTrips, isDay ? "Selected day" : "All calendar history")}${isDay ? "" : routeMap}${this._renderTripCards(shownTrips)}${selectedTrips.length > shownTrips.length ? `<p class="location-history-limit">Showing the latest ${shownTrips.length} of ${selectedTrips.length} trips. Increase trip_calendar_limit to show more.</p>` : ""}`;
    }
    const dayRoute = routeMap || this._renderTripRoutePlaceholder(this._tripCalendarState === "loading" || this._tripCalendarState === "idle" ? "Loading route data" : "No route points available for this day");
    return `<section class="location-history-section location-history card">
      <div class="location-history-top"><div class="location-history-heading"><ha-icon icon="mdi:calendar-road"></ha-icon><div><span>Persistent calendar</span><h2>Stored trip history</h2><p>Trips are stored locally in Home Assistant and remain available beyond Recorder retention.</p></div></div><button class="trip-calendar-refresh" data-trip-refresh aria-label="Refresh calendar"><ha-icon icon="mdi:refresh"></ha-icon></button></div>
      <div class="trip-view-toggle" role="group" aria-label="Trip history view"><button class="${isDay ? "active" : ""}" data-trip-view="day">Day</button><button class="${isDay ? "" : "active"}" data-trip-view="overview">Overview</button></div>
      ${isDay ? `<div class="trip-calendar-layout">${this._renderTripCalendar()}${dayRoute}</div>` : ""}
      <div class="trip-history-content"><h3>${isDay ? `${this._formatHistoryDay(this._tripSelectedDate)}` : "All calendar history"}</h3>${content}</div>
      <p class="location-history-note"><ha-icon icon="mdi:information-outline"></ha-icon>Calendar events are the persistent source; Recorder analysis remains the fallback when no trip calendar is mapped.</p>
    </section>`;
  }

  _renderLocationTripHistory() {
    if (this._entity("trip_calendar")) return this._renderPersistentTripHistory();
    const mapped = this._entity("odometer") && (this._entity("engine") || this._entity("ignition"));
    let content = "";
    if (!mapped) {
      content = `<div class="location-history-empty"><ha-icon icon="mdi:map-marker-alert-outline"></ha-icon><div><strong>Trip reconstruction not configured</strong><span>Map engine or ignition together with odometer to derive individual trips.</span></div></div>`;
    } else if (this._tripHistoryState === "loading" || this._tripHistoryState === "idle") {
      content = `<div class="location-history-empty"><ha-icon icon="mdi:loading"></ha-icon><div><strong>Loading trip history</strong><span>Reading recent vehicle states from Home Assistant Recorder.</span></div></div>`;
    } else if (this._tripHistoryState === "error") {
      content = `<div class="location-history-empty warning"><ha-icon icon="mdi:alert-circle-outline"></ha-icon><div><strong>Trip history unavailable</strong><span>${this._safe(this._tripHistoryError)}</span></div></div>`;
    } else if (!this._tripHistory.length) {
      content = `<div class="location-history-empty"><ha-icon icon="mdi:road-variant"></ha-icon><div><strong>No completed trips found</strong><span>Trips appear after Kia reports an engine or ignition cycle with a higher odometer value.</span></div></div>`;
    } else {
      content = `<div class="location-trip-list">${this._tripHistory.map((trip) => {
        const battery = trip.startBattery !== null && trip.endBattery !== null ? `${trip.startBattery.toFixed(1)}% → ${trip.endBattery.toFixed(1)}%` : "--";
        const energy = trip.usedEnergy !== null ? `${trip.usedEnergy.toFixed(1)} kWh` : "--";
        const consumption = trip.consumption !== null ? `${trip.consumption.toFixed(1)} kWh/100 km` : "--";
        return `<article class="location-trip-item">
          <div class="location-trip-route"><span>${this._safe(this._formatHistoryDay(new Date(trip.start).toISOString().slice(0, 10)))}</span><h3>${this._safe(trip.origin)} <ha-icon icon="mdi:arrow-right"></ha-icon> ${this._safe(trip.destination)}</h3><small>${this._safe(this._formatHistoryTime(trip.start))}–${this._safe(this._formatHistoryTime(trip.end))}</small></div>
          <div class="location-trip-metrics"><span><small>Distance</small><strong>${this._safe(trip.distance.toFixed(1))} km</strong></span><span><small>Duration</small><strong>${this._safe(this._formatTripDuration(trip.durationMinutes))}</strong></span><span><small>Battery</small><strong>${this._safe(battery)}</strong></span><span><small>Energy used</small><strong>${this._safe(energy)}</strong></span><span><small>Consumption</small><strong>${this._safe(consumption)}</strong></span><span><small>Average speed</small><strong>${this._safe(trip.averageSpeed.toFixed(0))} km/h</strong></span></div>
        </article>`;
      }).join("")}</div>`;
    }
    return `<section class="location-history-section location-history card">
      <div class="location-history-heading"><ha-icon icon="mdi:routes"></ha-icon><div><span>Recorder analysis</span><h2>Trip history</h2><p>Estimated from Kia engine, odometer, location, and battery updates over the latest ${this._tripHistoryDays()} days.</p></div></div>
      ${content}
      <p class="location-history-note"><ha-icon icon="mdi:information-outline"></ha-icon>Times, energy, and locations are estimates because Kia updates can arrive several minutes apart.</p>
    </section>`;
  }

  _renderLocationTab(context) {
    const { lastUpdated, mapTiles, markerImage } = context;
    const trackerState = this._state("location", "Location unavailable");
    const odometer = `${this._number("odometer")} ${this._unit("odometer", "km")}`;
    const coords = this._trackerCoords();
    const coordinateLabel = coords ? `${coords.lat.toFixed(5)}, ${coords.lon.toFixed(5)}` : "Coordinates unavailable";
    const trackerEntity = this._entity("location");
    const todayDriving = this._todayDriving();
    const driveMode = this._state("drive_mode", "--");
    const drivingContextAvailable = Boolean(todayDriving) || driveMode !== "--";
    const todayEfficiency = todayDriving?.distance > 0 ? Math.round((todayDriving.consumed / 1000 / todayDriving.distance) * 1000) / 10 : "--";
    const drivingContext = drivingContextAvailable ? `
          <ha-icon icon="mdi:road-variant"></ha-icon>
          <div><span>Driving context</span><h2>Today&apos;s driving</h2><div class="location-trip-stats">
            <button data-info="today_driving_stats"><span>Distance today</span><strong>${this._safe(todayDriving ? Math.round(todayDriving.distance * 10) / 10 : "--")} km</strong></button>
            <button data-info="today_driving_stats"><span>Average energy consumption</span><strong>${this._safe(todayEfficiency)} kWh/100 km</strong></button>
            <button data-info="drive_mode"><span>Drive mode</span><strong>${this._safe(driveMode)}</strong></button>
          </div></div>` : `
          <ha-icon icon="mdi:map-marker-path"></ha-icon>
          <div><span>Trip context</span><h2>Ready for future trip data</h2><p>Route, destination, and movement details remain read-only placeholders until mapped entities are defined.</p></div>`;

    return `
      <main class="location-detail" aria-label="Location details">
        <section class="location-detail-map card">
          <div class="location-detail-heading">
            <div><span>Position context</span><h2>Last known location</h2></div>
            <button class="location-info" data-info="location" ${trackerEntity ? "" : "disabled"} aria-label="${trackerEntity ? "Open tracker details" : "Tracker entity not configured"}"><ha-icon icon="mdi:information-outline"></ha-icon></button>
          </div>
          <div class="location-detail-map-canvas">
            ${mapTiles ? `<div class="map-tiles" style="${mapTiles.style}">${mapTiles.tiles}</div>` : `<div class="location-map-empty"><ha-icon icon="mdi:map-marker-off-outline"></ha-icon><span>Map preview needs tracker coordinates</span></div>`}
            ${coords ? `<span class="map-marker"><img src="${markerImage}" alt="Vehicle position" onerror="this.style.display='none';this.nextElementSibling.style.display='block'"><ha-icon icon="mdi:car-sports"></ha-icon></span>` : ""}
          </div>
          <div class="location-detail-caption"><ha-icon icon="mdi:crosshairs-gps"></ha-icon><span>${this._safe(coordinateLabel)}</span></div>
        </section>

        <section class="location-detail-summary card">
          <div class="location-detail-heading"><div><span>Tracker</span><h2>${this._safe(trackerState)}</h2></div><ha-icon icon="mdi:map-marker-outline"></ha-icon></div>
          <div class="location-stat-grid">
            <button class="location-stat" data-info="odometer"><ha-icon icon="mdi:counter"></ha-icon><span>Odometer</span><strong>${this._safe(odometer)}</strong></button>
            <div class="location-stat"><ha-icon icon="mdi:clock-outline"></ha-icon><span>Last updated</span><strong>${this._safe(lastUpdated)}</strong></div>
          </div>
        </section>

        <section class="location-context-card location-parking card">
          <ha-icon icon="mdi:parking"></ha-icon>
          <div><span>Parking context</span><h2>${this._safe(trackerState)}</h2><p>The tracker state is the latest known parking area. Update freshness stays visible alongside it.</p></div>
        </section>

        <section class="location-context-card location-trip card${drivingContextAvailable ? "" : " muted"}">${drivingContext}</section>

        ${this._renderLocationDailyHistory()}
        ${this._renderLocationTripHistory()}

        <button class="location-back card" data-nav="overview"><ha-icon icon="mdi:arrow-left"></ha-icon><span>Back to Overview</span></button>
      </main>`;
  }

  _renderSettingsTab() {
    const configured = Object.entries(this._config.entities || {});
    const entityAvailable = (entityId) => {
      const state = this._hass?.states?.[entityId]?.state;
      if (!state || state === "unavailable") return false;
      return entityId.startsWith("button.") || state !== "unknown";
    };
    const available = configured.filter(([, entityId]) => entityAvailable(entityId));
    const unavailable = configured.filter(([, entityId]) => !entityAvailable(entityId));
    const mappingState = configured.length ? `${available.length} of ${configured.length} available` : "No entities configured";
    const mappingOk = configured.length > 0 && unavailable.length === 0;
    const unavailableText = unavailable.length ? unavailable.map(([key]) => key.replaceAll("_", " ")).join(", ") : "All configured entities are reporting usable states.";
    const themeName = this._hass?.themes?.theme || "Home Assistant theme";
    const themeMode = this._hass?.themes?.darkMode ? "Dark mode" : "Light mode";
    const lastUpdated = this._formatDate(this._state("last_updated", "--"));
    const climateResult = this._state("last_climate_result", "Not configured");
    const chargingResult = this._state("last_charging_result", "Not configured");
    const vehicleConnection = this._vehicleConnectionHealth();
    const chargerConnection = this._chargerConnectionHealth();
    const chargerStatus = this._entity("charger_status") ? this._chargerStatusLabel(this._state("charger_status", "Unavailable")) : "Not mapped";
    const connectionTones = [vehicleConnection.tone, chargerConnection.tone];
    const connectionTone = connectionTones.includes("critical") ? "critical" : connectionTones.includes("attention") ? "attention" : connectionTones.includes("healthy") ? "healthy" : "muted";
    const connectionLabel = connectionTone === "critical" ? "Attention" : connectionTone === "attention" ? "Data delayed" : connectionTone === "healthy" ? "Data current" : "Not configured";
    const dashboardVersionEntity = this._obj("dashboard_version");
    const dashboardVersion = this._entity("dashboard_version")
      ? dashboardVersionEntity?.attributes?.installed_version ?? dashboardVersionEntity?.attributes?.version ?? this._state("dashboard_version", "Unavailable")
      : KIA_DASHBOARD_CARD_VERSION;
    const dashboardVersionAvailable = !["unknown", "unavailable"].includes(String(dashboardVersion).toLowerCase());
    return `<main class="settings-detail" aria-label="Settings detail">
      <section class="settings-heading card"><div class="settings-heading-icon"><ha-icon icon="mdi:tune-variant"></ha-icon></div><div><span class="settings-eyebrow">Dashboard administration</span><h2>Settings</h2><p>Review entity mapping, theme context, safe actions, and integration feedback for this card.</p></div><span class="settings-health ${mappingOk ? "healthy" : "attention"}"><ha-icon icon="${mappingOk ? "mdi:check-circle-outline" : "mdi:alert-circle-outline"}"></ha-icon>${mappingOk ? "Mapping healthy" : "Mapping needs attention"}</span></section>
      <div class="settings-grid">
        <section class="settings-panel card settings-connectivity"><div class="settings-panel-title"><ha-icon icon="mdi:access-point-network"></ha-icon><div><span>Connection diagnostics</span><h2>Connection health</h2></div><strong class="${connectionTone}-text">${connectionLabel}</strong></div><div class="settings-connection-grid">
          <article class="settings-connection ${vehicleConnection.tone}"><ha-icon icon="${vehicleConnection.icon}"></ha-icon><div><span>Vehicle connection</span><h3>${vehicleConnection.label}</h3><dl><div><dt>Last vehicle update</dt><dd>${this._safe(vehicleConnection.updatedLabel)}</dd></div><div><dt>Freshness</dt><dd>${this._safe(vehicleConnection.ageLabel)}</dd></div></dl><p>${vehicleConnection.message}</p></div></article>
          <article class="settings-connection ${chargerConnection.tone}"><ha-icon icon="${chargerConnection.icon}"></ha-icon><div><span>Home charger connection</span><h3>${chargerConnection.label}</h3><dl><div><dt>Charger status</dt><dd>${this._safe(chargerStatus)}</dd></div></dl><p>${chargerConnection.message}</p></div></article>
        </div><p class="settings-note"><ha-icon icon="mdi:information-outline"></ha-icon><span>Kia authentication state is not exposed to dashboard cards. Stale data can indicate reauthentication or a Kia cloud outage.</span></p></section>
        <section class="settings-panel card settings-mapping"><div class="settings-panel-title"><ha-icon icon="mdi:transit-connection-variant"></ha-icon><div><span>Configuration</span><h2>Entity mapping</h2></div><strong class="${mappingOk ? "healthy-text" : "attention-text"}">${mappingState}</strong></div><p>Entity IDs stay in this card's <code>entities</code> configuration. Missing or unavailable mappings are listed here before card changes are considered.</p><div class="settings-diagnostic ${mappingOk ? "healthy" : "attention"}"><ha-icon icon="${mappingOk ? "mdi:shield-check-outline" : "mdi:alert-outline"}"></ha-icon><div><strong>${mappingOk ? "No mapping issues detected" : "Check configured entities"}</strong><span>${this._safe(unavailableText)}</span></div></div></section>
        <section class="settings-panel card settings-theme"><div class="settings-panel-title"><ha-icon icon="mdi:palette-outline"></ha-icon><div><span>Appearance</span><h2>Theme</h2></div></div><div class="settings-theme-preview"><span class="theme-swatch brand"></span><span class="theme-swatch success"></span><span class="theme-swatch warning"></span><span class="theme-swatch surface"></span></div><dl><div><dt>Active theme</dt><dd>${this._safe(themeName)}</dd></div><div><dt>Color mode</dt><dd>${themeMode}</dd></div><div><dt>Card styling</dt><dd>Semantic Home Assistant tokens</dd></div></dl></section>
        <section class="settings-panel card settings-actions-panel"><div class="settings-panel-title"><ha-icon icon="mdi:gesture-tap-button"></ha-icon><div><span>Low risk</span><h2>Dashboard actions</h2></div></div><p>Refresh requests use the configured refresh entity. Vehicle details remain a local, read-only Home Assistant surface.</p><div class="settings-actions"><button data-action="refresh"><ha-icon icon="mdi:refresh"></ha-icon><span><strong>Refresh vehicle</strong><small>Request current Kia data</small></span></button><button data-info="vehicle_data" ${this._entity("vehicle_data") ? "" : "disabled"}><ha-icon icon="mdi:database-eye-outline"></ha-icon><span><strong>Vehicle data details</strong><small>Open the mapped raw-data entity</small></span></button></div>${this._notice ? `<p class="settings-notice">${this._safe(this._notice)}</p>` : ""}</section>
        <section class="settings-panel card settings-maintenance"><div class="settings-panel-title"><ha-icon icon="mdi:tools"></ha-icon><div><span>Read only</span><h2>Maintenance feedback</h2></div></div><div class="settings-feedback">${this._row("mdi:calendar-clock", "Latest scan", lastUpdated, lastUpdated !== "--")}${this._row("mdi:fan", "Climate command state", climateResult, climateResult !== "Not configured")}${this._row("mdi:ev-station", "Charging command state", chargingResult, chargingResult !== "Not configured")}${this._row("mdi:code-json", "Dashboard version", dashboardVersion, dashboardVersionAvailable)}</div><p class="settings-note"><ha-icon icon="mdi:information-outline"></ha-icon><span>For unavailable data, verify the entity in Home Assistant and this card's entity mapping before troubleshooting the layout.</span></p></section>
      </div>
    </main>`;
  }

  _batteryTabStyles() {
    return `
      .battery-detail{margin-top:12px;display:grid;gap:12px}.battery-detail-card{background:linear-gradient(145deg,var(--kia-panel),var(--kia-card));border:1px solid var(--kia-line);border-radius:8px;box-shadow:var(--ha-card-box-shadow,0 8px 22px rgba(0,0,0,.14));padding:clamp(18px,2vw,28px);min-width:0}.battery-detail-card>p,.battery-detail-heading p{margin-top:12px;color:var(--kia-muted);line-height:1.5}
      .battery-detail-hero{min-height:250px;display:grid;grid-template-columns:minmax(220px,.8fr) minmax(190px,.55fr) minmax(300px,1fr);gap:clamp(22px,3vw,54px);align-items:center}.battery-detail-heading>span{color:var(--blue);font-size:12px;font-weight:800;letter-spacing:.09em;text-transform:uppercase}.battery-detail-heading h2{margin-top:7px;font-size:clamp(30px,3vw,46px);line-height:1}
      .battery-detail-gauge{width:min(190px,100%);aspect-ratio:1;border-radius:50%;display:grid;place-items:center;justify-self:center;background:conic-gradient(var(--green) var(--level),var(--kia-recessed) 0);position:relative}.battery-detail-gauge:before{content:"";position:absolute;inset:12px;border-radius:50%;background:var(--kia-card);border:1px solid var(--kia-line)}.battery-detail-gauge div{position:relative;text-align:center}.battery-detail-gauge strong{display:block;font-size:clamp(34px,3vw,48px)}.battery-detail-gauge small{font-size:.48em}.battery-detail-gauge span,.battery-detail-title span,.battery-detail-highlights span,.battery-detail-stat span,.battery-detail-session-body span{color:var(--kia-muted);font-size:13px}
      .battery-detail-highlights{display:grid;gap:12px}.battery-detail-highlights>div{display:grid;grid-template-columns:34px 1fr;align-items:center;gap:2px 10px;padding:12px 14px;border-radius:8px;background:var(--kia-control);border:1px solid var(--kia-line)}.battery-detail-highlights ha-icon{grid-row:1/3;color:var(--blue);--mdc-icon-size:26px}.battery-detail-highlights strong{font-size:17px}.battery-detail .good{color:var(--green)}.battery-detail .unavailable{color:var(--kia-muted);font-style:italic}.battery-detail-gauge.unavailable{background:var(--kia-recessed);box-shadow:none}.battery-detail-gauge.unavailable strong{font-size:18px}
      .battery-detail-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:12px}.battery-detail-title{display:flex;gap:12px;align-items:center;margin-bottom:20px}.battery-detail-title>ha-icon{color:var(--blue);--mdc-icon-size:28px}.battery-detail-title h3{margin:0 0 2px;font-size:20px}.battery-detail-actions{display:grid;grid-template-columns:1fr 1fr;gap:10px}.battery-detail-actions button{min-height:54px;display:flex;align-items:center;justify-content:center;gap:9px;border:1px solid var(--kia-line);border-radius:8px;background:var(--kia-control);color:var(--kia-muted);font-weight:800;cursor:not-allowed;opacity:.72}.battery-detail-actions .stop{border-color:var(--kia-line);background:var(--kia-control)}
      .battery-detail-limit+.battery-detail-limit{margin-top:18px}.battery-detail-limit>span{display:block;margin-bottom:7px;color:var(--kia-muted);font-size:13px}.battery-detail-limit .limit-control{grid-template-columns:minmax(90px,auto) minmax(120px,1fr)}.battery-detail-stat strong{display:block;font-size:clamp(28px,3vw,42px)}.battery-detail-progress{height:16px;margin-top:20px;padding:3px;border-radius:8px;background:var(--kia-recessed);border:1px solid var(--kia-line)}.battery-detail-progress span{display:block;height:100%;border-radius:5px;background:linear-gradient(90deg,var(--red) 0 30%,var(--amber) 30% 55%,var(--green) 55% 100%)}
      .battery-detail-list{display:grid;gap:1px;border:1px solid var(--kia-line);border-radius:8px;overflow:hidden;background:var(--kia-line)}.battery-detail-subsection{display:flex;align-items:center;gap:10px;margin:22px 0 12px;padding-top:18px;border-top:1px solid var(--kia-line)}.battery-detail-subsection>ha-icon{color:var(--blue);--mdc-icon-size:24px}.battery-detail-subsection strong,.battery-detail-subsection span{display:block}.battery-detail-subsection span{margin-top:2px;color:var(--kia-muted);font-size:12px}.battery-detail-list>div{display:flex;justify-content:space-between;gap:18px;padding:12px 14px;background:var(--kia-control)}.battery-detail-list span{color:var(--kia-muted)}.battery-detail-session{grid-column:1/-1}.battery-detail-session-body{display:grid;grid-template-columns:64px repeat(2,minmax(0,1fr));align-items:center;gap:20px}.battery-detail-session-body>ha-icon{width:58px;height:58px;padding:14px;box-sizing:border-box;border-radius:50%;color:var(--kia-muted);background:var(--kia-control);border:1px solid var(--kia-line)}.battery-detail-session-body>ha-icon.active{color:var(--green)}.battery-detail-session-body strong{display:block;margin-top:4px;font-size:20px}
      @media(max-width:1180px){.battery-detail-hero{grid-template-columns:1fr 1fr}.battery-detail-heading{grid-column:1/-1}}@media(max-width:760px){.battery-detail-hero,.battery-detail-grid{grid-template-columns:1fr}.battery-detail-heading,.battery-detail-session{grid-column:auto}.battery-detail-actions{grid-template-columns:1fr}.battery-detail-session-body{grid-template-columns:58px 1fr}.battery-detail-session-body>div:last-child{grid-column:2}.battery-detail-limit .limit-control{grid-template-columns:1fr}}
    `;
  }

  _vehicleTabStyles() {
    return `
      .vehicle-detail { margin-top:12px; display:grid; gap:12px; } .vehicle-page-heading { min-height:118px; padding:22px 26px; display:flex; align-items:center; justify-content:space-between; gap:24px; }
      .vehicle-page-heading>div:first-child>span,.vehicle-section-title span { color:var(--blue); font-size:12px; font-weight:800; letter-spacing:.08em; text-transform:uppercase; } .vehicle-page-heading h2 { margin-top:4px; font-size:clamp(24px,2.2vw,34px); } .vehicle-page-heading p { margin-top:7px; color:var(--kia-muted); } .vehicle-page-meta { min-width:250px; display:grid; gap:8px; }
      .vehicle-page-status { min-width:190px; padding:14px 16px; border:1px solid color-mix(in srgb,var(--green) 50%,var(--kia-line)); border-radius:8px; background:color-mix(in srgb,var(--green) 12%,var(--kia-card)); display:flex; align-items:center; gap:10px; } .vehicle-page-status ha-icon { color:var(--green); --mdc-icon-size:28px; } .vehicle-page-status.warning { border-color:var(--amber); background:color-mix(in srgb,var(--amber) 12%,var(--kia-card)); } .vehicle-page-status.warning ha-icon { color:var(--amber); }
      .vehicle-page-status.partial { border-color:var(--kia-line); background:var(--kia-control); } .vehicle-page-status.partial ha-icon { color:var(--kia-muted); } .vehicle-vin { width:100%; padding:10px 13px; border:1px solid var(--kia-line); border-radius:8px; background:var(--kia-control); display:grid; grid-template-columns:24px minmax(0,1fr); align-items:center; gap:10px; text-align:left; } .vehicle-vin:hover,.vehicle-vin:focus-visible { border-color:var(--blue); } .vehicle-vin>ha-icon { color:var(--blue); --mdc-icon-size:22px; } .vehicle-vin small,.vehicle-vin strong { display:block; } .vehicle-vin small { color:var(--kia-muted); font-size:10px; letter-spacing:.08em; } .vehicle-vin strong { margin-top:2px; font-size:12px; overflow-wrap:anywhere; } .vehicle-vin.unavailable { color:var(--kia-muted); }
      .vehicle-detail-grid { display:grid; grid-template-columns:minmax(280px,.84fr) minmax(420px,1.45fr); grid-template-areas:"security openings" "tires warnings"; gap:12px; } .vehicle-section { padding:20px 22px; min-width:0; } .vehicle-security { grid-area:security; } .vehicle-openings { grid-area:openings; } .vehicle-tires { grid-area:tires; } .vehicle-warnings { grid-area:warnings; }
      .vehicle-section-title { display:flex; align-items:center; gap:12px; margin-bottom:16px; } .vehicle-section-title>ha-icon { color:var(--blue); --mdc-icon-size:28px; } .vehicle-section-title h2 { margin-top:2px; font-size:20px; }
      .vehicle-state-list { display:grid; gap:9px; } .vehicle-state { width:100%; min-height:58px; padding:9px 11px; border:1px solid var(--kia-line); border-radius:8px; background:var(--kia-control); display:grid; grid-template-columns:26px minmax(100px,1fr) auto 22px; align-items:center; gap:10px; text-align:left; } .vehicle-state:hover:not(:disabled),.vehicle-state:focus-visible { border-color:var(--blue); } .vehicle-state:disabled { cursor:default; opacity:.72; }
      .vehicle-state>ha-icon:first-child { color:var(--kia-muted); --mdc-icon-size:21px; } .vehicle-state span { min-width:0; } .vehicle-state small { display:block; color:var(--kia-muted); font-size:11px; } .vehicle-state strong { display:block; margin-top:3px; overflow:hidden; text-overflow:ellipsis; white-space:nowrap; font-size:14px; } .vehicle-state b { max-width:116px; overflow:hidden; text-overflow:ellipsis; white-space:nowrap; font-size:13px; text-transform:capitalize; }
      .vehicle-state-indicator { color:var(--green); --mdc-icon-size:19px; } .vehicle-state.warning .vehicle-state-indicator { color:var(--amber); } .vehicle-state.unavailable .vehicle-state-indicator { color:var(--kia-muted); } .vehicle-lock-actions{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:9px;margin-top:12px}.vehicle-lock-actions button{min-height:44px;border:1px solid color-mix(in srgb,var(--green) 55%,var(--kia-line));border-radius:8px;background:var(--kia-control);color:var(--kia-text);display:flex;align-items:center;justify-content:center;gap:8px;font-weight:800}.vehicle-lock-actions button.unlock{border-color:color-mix(in srgb,var(--amber) 55%,var(--kia-line))}.vehicle-lock-actions button:disabled{cursor:not-allowed;opacity:.45}.vehicle-lock-actions ha-icon{color:var(--green);--mdc-icon-size:20px}.vehicle-lock-actions .unlock ha-icon{color:var(--amber)}.vehicle-section-note { margin-top:14px; padding-top:13px; border-top:1px solid var(--kia-line); display:flex; gap:8px; color:var(--kia-muted); font-size:12px; line-height:1.4; }
      .vehicle-window-grid{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:9px;margin-top:16px;padding-top:16px;border-top:1px solid var(--kia-line)}.vehicle-window-tile{display:grid;grid-template-columns:minmax(0,1fr) auto;align-items:center;gap:8px;min-width:0;padding:8px;border:1px solid var(--kia-line);border-radius:12px;background:var(--kia-control)}.vehicle-window-tile.warning{border-color:var(--amber)}.vehicle-window-tile.unavailable{opacity:.65}.vehicle-window-info{min-width:0;padding:2px;border:0;background:transparent;display:grid;grid-template-columns:24px minmax(0,1fr);align-items:center;gap:8px;text-align:left}.vehicle-window-info>ha-icon{color:var(--kia-muted);--mdc-icon-size:20px}.vehicle-window-info small,.vehicle-window-info strong,.vehicle-window-info b{display:block}.vehicle-window-info small{color:var(--kia-muted);font-size:10px}.vehicle-window-info strong{margin-top:2px;overflow:hidden;text-overflow:ellipsis;white-space:nowrap;font-size:13px}.vehicle-window-info b{margin-top:3px;font-size:11px;text-transform:capitalize}.vehicle-window-actions{display:grid;gap:4px}.vehicle-window-actions button{width:32px;height:28px;padding:0;border:1px solid var(--kia-line);border-radius:8px;background:var(--kia-card);display:grid;place-items:center}.vehicle-window-actions button:hover:not(:disabled),.vehicle-window-actions button:focus-visible{border-color:var(--blue);color:var(--blue)}.vehicle-window-actions button:disabled{cursor:not-allowed;opacity:.35}.vehicle-window-actions ha-icon{--mdc-icon-size:18px}
      .vehicle-opening-grid { display:grid; grid-template-columns:repeat(2,minmax(0,1fr)); gap:9px; } .vehicle-tire-layout { display:grid; grid-template-columns:minmax(125px,1fr) 92px minmax(125px,1fr); gap:12px; align-items:center; } .vehicle-tire-layout>div { display:grid; gap:10px; } .vehicle-tire-layout img { width:88px; height:154px; object-fit:contain; justify-self:center; filter:drop-shadow(0 10px 12px rgba(0,0,0,.34)); } .vehicle-tire-layout .vehicle-state { grid-template-columns:22px minmax(76px,1fr) auto 20px; padding-inline:9px; }
      .vehicle-warnings { display:flex; flex-direction:column; justify-content:center; } .vehicle-warnings p,.vehicle-warnings ul { color:var(--kia-muted); line-height:1.55; } .vehicle-warnings ul { margin:0; padding-left:20px; } .vehicle-mapping-note { margin-top:12px; }
      @media (max-width:1180px) { .vehicle-detail-grid { grid-template-columns:1fr; grid-template-areas:"security" "openings" "tires" "warnings"; } } @media (max-width:760px) { .vehicle-page-heading { align-items:flex-start; flex-direction:column; } .vehicle-page-meta,.vehicle-page-status { min-width:0; width:100%; box-sizing:border-box; } .vehicle-opening-grid,.vehicle-window-grid,.vehicle-tire-layout { grid-template-columns:1fr; } .vehicle-tire-layout img { grid-row:1; height:126px; } }@media(max-width:460px){.vehicle-lock-actions{grid-template-columns:1fr}}
    `;
  }

  _climateTabStyles() {
    return `
      .climate-detail{margin-top:12px;display:grid;grid-template-columns:1.15fr .85fr;grid-template-areas:"intro intro" "temperature controls" "comfort comfort" "session session";gap:12px}.climate-detail>.card{padding:clamp(20px,2.2vw,32px);min-width:0}.climate-intro{grid-area:intro;display:grid;grid-template-columns:1fr auto;align-items:center;gap:28px;min-height:150px;overflow:hidden}.climate-eyebrow,.climate-heading span,.climate-session-copy>span{color:var(--blue);font-size:12px;font-weight:800;letter-spacing:.09em;text-transform:uppercase}.climate-intro h2{margin-top:4px;font-size:clamp(30px,3vw,46px)}.climate-intro p,.climate-session p{margin-top:8px;color:var(--kia-muted);line-height:1.5}.climate-state{min-width:180px;padding:18px;border:1px solid var(--kia-line);border-radius:8px;background:var(--kia-control);display:grid;grid-template-columns:34px 1fr;gap:3px 12px;align-items:center}.climate-state ha-icon{grid-row:1/3;color:var(--kia-muted);--mdc-icon-size:30px}.climate-state span,.climate-readings span{color:var(--kia-muted);font-size:12px}.climate-state strong{font-size:20px;text-transform:capitalize}.climate-state.is-on{border-color:var(--blue);background:color-mix(in srgb,var(--blue) 14%,var(--kia-card))}.climate-state.is-on ha-icon,.climate-state.is-on strong{color:var(--blue)}.climate-temperature{grid-area:temperature}.climate-controls{grid-area:controls}.climate-comfort{grid-area:comfort}.climate-session{grid-area:session}.climate-heading{display:flex;gap:13px;align-items:center;margin-bottom:20px}.climate-heading>ha-icon{color:var(--blue);--mdc-icon-size:30px}.climate-heading h2{margin-top:2px;font-size:21px}.climate-target{min-height:120px;border-radius:8px;background:var(--kia-recessed);border:1px solid var(--kia-line);display:grid;place-items:center;font-size:clamp(38px,4vw,58px)}.climate-readings,.climate-actions{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:12px;margin-top:12px}.climate-readings.three{grid-template-columns:repeat(3,minmax(0,1fr))}.climate-readings button{border:1px solid var(--kia-line);background:var(--kia-control);border-radius:8px;min-height:78px;padding:14px;display:flex;flex-direction:column;text-align:left;justify-content:center}.climate-readings strong{margin-top:4px;overflow-wrap:anywhere;text-transform:capitalize}.climate-actions button{min-height:112px;border:1px solid var(--blue);border-radius:8px;background:color-mix(in srgb,var(--blue) 14%,var(--kia-card));display:flex;flex-direction:column;align-items:center;justify-content:center;gap:10px;font-weight:800}.climate-actions ha-icon{color:var(--blue);--mdc-icon-size:34px}.climate-actions button:disabled{cursor:not-allowed;opacity:.58}.climate-actions .stop{border-color:var(--amber)}.climate-actions .stop ha-icon{color:var(--amber)}.climate-actions{grid-template-columns:repeat(3,minmax(0,1fr))}.climate-session{display:flex;align-items:center;gap:20px}.climate-session>ha-icon{color:var(--blue);--mdc-icon-size:42px}.climate-session h2{margin-top:5px;font-size:clamp(18px,2vw,24px);overflow-wrap:anywhere}.climate-system-grid{grid-template-columns:repeat(auto-fit,minmax(180px,1fr))}.climate-subheading{margin-top:22px;padding-top:18px;border-top:1px solid var(--kia-line)}.climate-subheading span{color:var(--blue);font-size:12px;font-weight:800;letter-spacing:.09em;text-transform:uppercase}.climate-comfort-grid{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:12px;margin-top:12px}.climate-comfort-tile{min-height:112px;padding:14px;border:1px solid var(--kia-line);border-radius:8px;background:var(--kia-control);display:grid;grid-template-columns:34px 1fr;grid-template-rows:auto auto auto;gap:3px 10px;text-align:left;align-items:center}.climate-comfort-tile ha-icon{grid-row:1/4;color:var(--blue);--mdc-icon-size:28px}.climate-comfort-tile span,.climate-comfort-tile small{color:var(--kia-muted);font-size:12px}.climate-comfort-tile strong{overflow-wrap:anywhere;text-transform:capitalize}.climate-comfort-tile.active{border-color:var(--green);background:color-mix(in srgb,var(--green) 12%,var(--kia-card))}.climate-comfort-tile.active ha-icon,.climate-comfort-tile.active strong{color:var(--green)}.climate-comfort-tile.unavailable{opacity:.58}.climate-session-copy{min-width:0;flex:1}.climate-schedule-grid{grid-template-columns:repeat(2,minmax(0,1fr));max-width:760px}@media(max-width:1000px){.climate-comfort-grid{grid-template-columns:repeat(2,minmax(0,1fr))}}@media(max-width:760px){.climate-detail{grid-template-columns:1fr;grid-template-areas:"intro" "temperature" "controls" "comfort" "session"}.climate-intro{grid-template-columns:1fr}.climate-readings,.climate-readings.three,.climate-actions,.climate-comfort-grid,.climate-schedule-grid{grid-template-columns:1fr}.climate-session{align-items:flex-start}}
    `;
  }

  _energyTabStyles() {
    return `
      .energy-detail{margin-top:12px;display:grid;grid-template-columns:1.15fr 1fr;gap:12px}.energy-detail>.card{min-width:0;overflow:hidden}.energy-intro{grid-column:1/-1;min-height:150px;padding:clamp(24px,3vw,42px);display:flex;align-items:center;justify-content:space-between;gap:28px;position:relative}.energy-intro:before{content:"";position:absolute;inset:0;background:radial-gradient(circle at 82% 50%,color-mix(in srgb,var(--blue) 18%,transparent),transparent 28%);pointer-events:none}.energy-intro>div:first-child{position:relative;z-index:1;max-width:720px}.energy-eyebrow,.energy-heading small{color:var(--blue);font-size:12px;font-weight:800;letter-spacing:.09em;text-transform:uppercase}.energy-intro h2{margin-top:7px;font-size:clamp(25px,2.4vw,36px)}.energy-intro p,.energy-future>p{margin-top:10px;color:var(--kia-muted);line-height:1.5}.energy-orbit{flex:0 0 92px;width:92px;height:92px;border-radius:50%;display:grid;place-items:center;color:var(--blue);background:color-mix(in srgb,var(--blue) 12%,var(--kia-card));border:1px solid color-mix(in srgb,var(--blue) 48%,var(--kia-line));box-shadow:0 0 38px color-mix(in srgb,var(--blue) 18%,transparent)}.energy-orbit ha-icon{--mdc-icon-size:44px}.energy-section,.energy-future{padding:22px}.energy-heading{display:flex;align-items:center;gap:12px;margin-bottom:18px}.energy-heading>span{width:42px;height:42px;flex:0 0 42px;border-radius:8px;display:grid;place-items:center;color:var(--blue);background:color-mix(in srgb,var(--blue) 12%,var(--kia-card));border:1px solid var(--kia-line)}.energy-heading h2{margin-top:3px;font-size:20px}.energy-stats{display:grid;gap:10px}.energy-stat{width:100%;min-height:76px;padding:12px 14px;display:grid;grid-template-columns:42px minmax(0,1fr) 22px;align-items:center;gap:12px;color:var(--kia-text);text-align:left;border:1px solid var(--kia-line);border-radius:8px;background:var(--kia-control)}.energy-stat:hover,.energy-stat:focus-visible{border-color:var(--blue);outline:none}.energy-stat-icon{width:38px;height:38px;border-radius:50%;display:grid;place-items:center;color:var(--blue);background:color-mix(in srgb,var(--blue) 11%,var(--kia-card))}.energy-stat-icon ha-icon{--mdc-icon-size:22px}.energy-stat-copy{min-width:0}.energy-stat-copy small{display:block;color:var(--kia-muted);font-size:12px;font-weight:700}.energy-stat-copy strong{display:block;margin-top:3px;font-size:clamp(18px,1.7vw,24px);line-height:1.1;overflow-wrap:anywhere}.energy-stat-copy em{color:var(--kia-muted);font-size:.65em;font-style:normal;font-weight:700}.energy-stat-link{color:var(--kia-muted);--mdc-icon-size:20px}.energy-stat.positive .energy-stat-icon,.energy-stat.positive .energy-stat-copy strong{color:var(--green)}.energy-note{margin-top:14px;padding-top:14px;border-top:1px solid var(--kia-line);display:flex;align-items:flex-start;gap:9px;color:var(--kia-muted);font-size:12px;line-height:1.45}.energy-note ha-icon{flex:0 0 auto;color:var(--blue);--mdc-icon-size:18px}.energy-future{min-height:210px;position:relative}.energy-bars{height:64px;margin-top:20px;display:flex;align-items:flex-end;gap:8px;opacity:.65}.energy-bars i{flex:1;min-width:8px;height:38%;border-radius:4px 4px 0 0;background:color-mix(in srgb,var(--blue) 46%,var(--kia-line))}.energy-bars i:nth-child(2),.energy-bars i:nth-child(6){height:62%}.energy-bars i:nth-child(3){height:48%}.energy-bars i:nth-child(4){height:82%}.energy-bars i:nth-child(5){height:70%}.energy-bars i:nth-child(7){height:94%}.energy-back{margin-top:20px;min-height:46px;padding:0 16px;display:inline-flex;align-items:center;gap:9px;border:1px solid var(--kia-line);border-radius:8px;background:var(--kia-control);font-weight:800}.energy-back ha-icon{color:var(--blue);--mdc-icon-size:20px}@media(max-width:900px){.energy-detail{grid-template-columns:1fr}.energy-intro{grid-column:auto}}@media(max-width:560px){.energy-intro{align-items:flex-start}.energy-orbit{width:64px;height:64px;flex-basis:64px}.energy-orbit ha-icon{--mdc-icon-size:32px}.energy-section,.energy-future{padding:18px}}
      .energy-heading>div{min-width:0;flex:1}.energy-heading>.charger-health,.energy-heading>.charger-safety{width:auto;height:auto;flex:0 0 auto;border:0;background:transparent;display:flex;align-items:center;gap:7px;color:var(--kia-muted);font-size:11px;font-weight:800;white-space:nowrap}.charger-health i{width:8px;height:8px;border-radius:50%;background:var(--kia-muted);box-shadow:0 0 7px currentColor}.charger-health.online{color:var(--green)}.charger-health.online i{background:var(--green)}.charger-safety ha-icon{color:var(--blue);--mdc-icon-size:17px}.charger-live-hero{min-height:105px;margin-bottom:12px;padding:18px 20px;border:1px solid var(--kia-line);border-radius:8px;background:var(--kia-recessed);display:flex;align-items:center;justify-content:space-between;gap:20px}.charger-live-hero span,.charger-live-hero small,.charger-current span,.charger-session-summary span,.charger-session-summary small,.charger-history-empty span{display:block;color:var(--kia-muted);font-size:12px}.charger-live-hero strong{display:block;margin:4px 0;font-size:clamp(24px,2.4vw,34px);text-transform:capitalize}.charger-live-hero>ha-icon{color:var(--blue);--mdc-icon-size:48px}.positive-text{color:var(--green)}.energy-stats.compact{grid-template-columns:repeat(2,minmax(0,1fr))}.charger-modes{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:9px}.charger-mode{min-height:76px;border:1px solid var(--kia-line);border-radius:8px;background:var(--kia-control);display:flex;flex-direction:column;align-items:center;justify-content:center;gap:8px;font-weight:800}.charger-mode ha-icon{color:var(--blue);--mdc-icon-size:26px}.charger-mode.active{border-color:var(--green);background:color-mix(in srgb,var(--green) 12%,var(--kia-card))}.charger-mode.active ha-icon,.charger-mode.active span{color:var(--green)}.charger-mode:disabled,.charger-action:disabled,.charger-current input:disabled{cursor:not-allowed;opacity:.5}.charger-current{margin-top:14px;padding:14px;border:1px solid var(--kia-line);border-radius:8px;background:var(--kia-recessed);display:grid;grid-template-columns:auto minmax(120px,1fr);align-items:center;gap:18px}.charger-current strong{display:block;margin-top:3px}.charger-current input{width:100%;accent-color:var(--blue)}.charger-actions{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:9px;margin-top:12px}.charger-action{min-height:48px;border:1px solid var(--kia-line);border-radius:8px;background:var(--kia-control);display:flex;align-items:center;justify-content:center;gap:7px;font-weight:800}.charger-action ha-icon{color:var(--blue);--mdc-icon-size:20px}.charger-action.start{border-color:color-mix(in srgb,var(--green) 55%,var(--kia-line))}.charger-action.start ha-icon{color:var(--green)}.charger-action.stop{border-color:color-mix(in srgb,var(--red) 45%,var(--kia-line))}.charger-action.stop ha-icon{color:var(--red)}.energy-notice{margin-top:12px;padding:10px 12px;border:1px solid color-mix(in srgb,var(--blue) 38%,var(--kia-line));border-radius:8px;background:color-mix(in srgb,var(--blue) 10%,var(--kia-card));color:var(--blue);font-size:12px}.charger-history{grid-column:1/-1}.charger-session-summary{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:10px}.charger-session-summary>div{padding:18px;border:1px solid var(--kia-line);border-radius:8px;background:var(--kia-recessed)}.charger-session-summary strong{display:block;margin:5px 0;font-size:clamp(24px,2.2vw,34px)}.charger-history-grid{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:10px;margin-top:10px}.charger-history-empty{min-height:92px;margin-top:10px;padding:18px;border:1px dashed var(--kia-line);border-radius:8px;background:var(--kia-recessed);display:flex;align-items:center;gap:15px}.charger-history-empty ha-icon{color:var(--blue);--mdc-icon-size:32px}.charger-history-empty strong{display:block;margin-bottom:4px}.charger-history-grid .energy-stat{grid-template-columns:36px minmax(0,1fr) 18px}.charger-history-grid .energy-stat-icon{width:34px;height:34px}@media(max-width:1050px){.energy-stats.compact,.charger-history-grid{grid-template-columns:1fr 1fr}.charger-actions{grid-template-columns:1fr 1fr}}@media(max-width:560px){.energy-stats.compact,.charger-history-grid,.charger-session-summary,.charger-current{grid-template-columns:1fr}.charger-modes{grid-template-columns:1fr}.energy-heading>.charger-health,.energy-heading>.charger-safety{grid-column:2;justify-self:start}.charger-actions{grid-template-columns:1fr 1fr}}
      .charger-long-term{margin-top:22px;padding-top:20px;border-top:1px solid var(--kia-line)}.charger-long-term-heading{display:flex;align-items:end;justify-content:space-between;gap:16px}.charger-long-term-heading span,.charger-long-term-heading>small{color:var(--kia-muted);font-size:12px}.charger-long-term-heading h3{margin-top:3px;font-size:19px}.charger-history-periods{margin-top:14px;display:flex;gap:8px;flex-wrap:wrap}.charger-history-periods button{min-height:38px;padding:0 14px;border:1px solid var(--kia-line);border-radius:8px;background:var(--kia-control);color:var(--kia-text);font-weight:800}.charger-history-periods button.active{border-color:var(--green);color:var(--green);background:color-mix(in srgb,var(--green) 11%,var(--kia-card))}.charger-history-periods button.refresh{width:40px;padding:0;display:grid;place-items:center;margin-left:auto}.charger-history-periods ha-icon{--mdc-icon-size:19px}.charger-history-summary{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:10px;margin-top:12px}.charger-history-summary>div{padding:14px;border:1px solid var(--kia-line);border-radius:8px;background:var(--kia-control)}.charger-history-summary span{display:block;color:var(--kia-muted);font-size:12px}.charger-history-summary strong{display:block;margin-top:5px;font-size:clamp(18px,1.5vw,23px)}.charger-history-chart{height:180px;margin-top:12px;padding:16px 14px 10px;border:1px solid var(--kia-line);border-radius:8px;background:var(--kia-recessed);overflow:hidden}.charger-history-bars{height:100%;display:flex;align-items:stretch;gap:4px}.charger-history-bars>div{min-width:0;flex:1;display:grid;grid-template-rows:minmax(0,1fr) auto;gap:6px}.charger-history-bars>div>i{align-self:end;width:72%;max-width:18px;min-height:2px;justify-self:center;border-radius:4px 4px 0 0;background:var(--kia-line)}.charger-history-bars>div>i.charging{background:var(--green)}.charger-history-bars span{overflow:hidden;color:var(--kia-muted);font-size:8px;text-align:center}.charger-history-toggle{width:100%;min-height:44px;margin-top:12px;padding:0 14px;border:1px solid var(--kia-line);border-radius:8px;background:var(--kia-control);color:var(--kia-text);display:flex;align-items:center;justify-content:space-between;font-weight:800}.charger-history-table{margin-top:8px;border:1px solid var(--kia-line);border-radius:8px;overflow:hidden}.charger-history-table[hidden]{display:none}.charger-history-table>div{min-height:44px;padding:9px 14px;display:grid;grid-template-columns:1fr 1fr 1fr;align-items:center;gap:12px;border-top:1px solid var(--kia-line);color:var(--kia-muted);font-size:12px}.charger-history-table>div:first-child{border-top:0}.charger-history-table>.header{background:var(--kia-recessed);color:var(--kia-text)}.charger-history-table>div strong{color:var(--kia-text)}.charger-history-empty.warning ha-icon{color:var(--red)}@media(max-width:760px){.charger-history-summary{grid-template-columns:1fr 1fr}.charger-history-chart{height:150px}.charger-history-bars{gap:2px}.charger-history-bars span{display:none}}@media(max-width:460px){.charger-history-summary{grid-template-columns:1fr}.charger-long-term-heading{align-items:start;flex-direction:column}.charger-history-table>div{grid-template-columns:1.3fr 1fr 1fr;padding-inline:10px}}
      .driving-analytics{grid-column:1/-1}.driving-summary{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:10px}.driving-summary button{min-height:92px;padding:15px;border:1px solid var(--kia-line);border-radius:8px;background:var(--kia-control);color:var(--kia-text);text-align:left}.driving-summary button:hover,.driving-summary button:focus-visible{border-color:var(--blue)}.driving-summary span,.driving-summary small{display:block;color:var(--kia-muted);font-size:12px}.driving-summary strong{display:block;margin-top:6px;font-size:clamp(18px,1.6vw,24px)}.driving-summary em{color:var(--kia-muted);font-size:.65em;font-style:normal}.driving-summary small{margin-top:4px}.driving-chart{margin-top:18px;padding:16px 16px 10px;border:1px solid var(--kia-line);border-radius:8px;background:var(--kia-recessed)}.driving-chart-legend{display:flex;justify-content:flex-end;gap:16px;color:var(--kia-muted);font-size:11px}.driving-chart-legend span{display:flex;align-items:center;gap:5px}.driving-chart-legend i{width:9px;height:9px;border-radius:2px}.driving-chart i.consumed{background:var(--blue)}.driving-chart i.regenerated{background:var(--green)}.driving-chart-bars{height:150px;margin-top:12px;display:flex;align-items:stretch;gap:7px}.driving-day{min-width:0;flex:1;display:grid;grid-template-rows:minmax(0,1fr) auto;gap:7px}.driving-day>div{min-height:0;display:flex;align-items:flex-end;justify-content:center;gap:2px;border-bottom:1px solid var(--kia-line)}.driving-day>div i{width:min(11px,44%);min-height:3px;border-radius:3px 3px 0 0}.driving-day>span{overflow:hidden;color:var(--kia-muted);font-size:9px;text-align:center}@media(max-width:760px){.driving-summary{grid-template-columns:1fr 1fr}.driving-chart-bars{height:120px;gap:3px}.driving-day>span{font-size:8px}}@media(max-width:460px){.driving-summary{grid-template-columns:1fr}.driving-chart{padding-inline:10px}.driving-chart-legend{justify-content:flex-start}}
      .vehicle-energy,.charger-flow{grid-column:1/-1}.vehicle-energy .energy-stats,.charger-flow .energy-stats.compact{grid-template-columns:repeat(4,minmax(0,1fr))}@media(max-width:1200px){.vehicle-energy .energy-stats,.charger-flow .energy-stats.compact{grid-template-columns:repeat(2,minmax(0,1fr))}}@media(max-width:560px){.vehicle-energy .energy-stats,.charger-flow .energy-stats.compact{grid-template-columns:1fr}}
    `;
  }

  _locationTabStyles() {
    return `
      .location-detail { margin-top:12px; display:grid; grid-template-columns:minmax(0,1.45fr) minmax(300px,.75fr); grid-template-areas:"map summary" "map parking" "map trip" "daily daily" "history history" "back back"; gap:12px; align-items:stretch; }
      .location-detail-map { grid-area:map; min-height:560px; padding:22px; display:grid; grid-template-rows:auto minmax(360px,1fr) auto; gap:16px; min-width:0; }
      .location-detail-summary { grid-area:summary; padding:22px; display:grid; gap:22px; }
      .location-context-card { padding:22px; display:grid; grid-template-columns:42px 1fr; gap:16px; align-items:start; }
      .location-parking { grid-area:parking; } .location-trip { grid-area:trip; }
      .location-daily { grid-area:daily; } .location-history { grid-area:history; }
      .location-back { grid-area:back; min-height:52px; padding:0 20px; display:flex; align-items:center; justify-content:center; gap:10px; border-color:var(--blue); background:var(--kia-control); color:var(--kia-text); font-weight:800; }
      .location-back ha-icon { color:var(--blue); --mdc-icon-size:21px; }
      .location-detail-heading { display:flex; align-items:flex-start; justify-content:space-between; gap:18px; min-width:0; }
      .location-detail-heading span,.location-stat span,.location-context-card span,.location-detail-caption { color:var(--kia-muted); font-size:13px; }
      .location-detail-heading h2,.location-context-card h2 { margin-top:5px; font-size:clamp(19px,1.55vw,26px); overflow-wrap:anywhere; }
      .location-detail-heading>ha-icon,.location-context-card>ha-icon { color:var(--blue); --mdc-icon-size:34px; }
      .location-info { width:40px; height:40px; border:1px solid var(--kia-line); border-radius:8px; background:var(--kia-control); color:var(--blue); display:grid; place-items:center; }
      .location-info:disabled { cursor:default; color:var(--kia-muted); opacity:.55; }
      .location-detail-map-canvas { min-height:360px; border-radius:8px; background:color-mix(in srgb,var(--kia-control) 62%,var(--blue) 8%); display:grid; place-items:center; position:relative; overflow:hidden; isolation:isolate; }
      .location-detail-map-canvas:after { content:""; position:absolute; inset:0; border:1px solid color-mix(in srgb,var(--blue) 28%,transparent); border-radius:inherit; pointer-events:none; z-index:3; }
      .location-map-empty { display:grid; justify-items:center; gap:12px; color:var(--kia-muted); text-align:center; padding:24px; } .location-map-empty ha-icon { --mdc-icon-size:48px; color:var(--blue); }
      .location-detail-caption { display:flex; align-items:center; gap:8px; min-width:0; } .location-detail-caption ha-icon { --mdc-icon-size:18px; color:var(--blue); }
      .location-stat-grid { display:grid; grid-template-columns:repeat(2,minmax(0,1fr)); gap:10px; }
      .location-stat { min-width:0; min-height:116px; padding:16px; border:1px solid var(--kia-line); border-radius:8px; background:var(--kia-control); color:var(--kia-text); display:grid; grid-template-columns:28px 1fr; grid-template-rows:auto 1fr; gap:8px 10px; text-align:left; }
      button.location-stat { cursor:pointer; } .location-stat ha-icon { grid-row:1 / 3; color:var(--blue); --mdc-icon-size:24px; }
      .location-stat strong { align-self:end; font-size:clamp(15px,1vw,18px); line-height:1.25; overflow-wrap:anywhere; }
      .location-context-card p { margin-top:8px; color:var(--kia-muted); font-size:14px; line-height:1.45; } .location-context-card.muted>ha-icon { color:var(--kia-muted); }
      .location-trip-stats{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:8px;margin-top:14px}.location-trip-stats button{min-width:0;padding:10px;border:1px solid var(--kia-line);border-radius:8px;background:var(--kia-control);color:var(--kia-text);text-align:left}.location-trip-stats button:hover,.location-trip-stats button:focus-visible{border-color:var(--blue)}.location-trip-stats span{display:block;font-size:11px}.location-trip-stats strong{display:block;margin-top:4px;font-size:13px;overflow-wrap:anywhere}
      .location-history-section{padding:22px;min-width:0}.location-history-heading{display:grid;grid-template-columns:38px minmax(0,1fr);gap:12px;align-items:start}.location-history-heading>ha-icon{color:var(--blue);--mdc-icon-size:30px}.location-history-heading span{color:var(--blue);font-size:11px;font-weight:800;letter-spacing:.08em;text-transform:uppercase}.location-history-heading h2{margin-top:3px;font-size:clamp(19px,1.5vw,24px)}.location-history-heading p{margin-top:5px;color:var(--kia-muted);font-size:13px;line-height:1.45}
      .location-period-summary{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:9px;margin-top:18px}.location-period-summary>div{min-height:78px;padding:13px;border:1px solid var(--kia-line);border-radius:8px;background:var(--kia-control)}.location-period-summary span,.location-period-summary strong{display:block}.location-period-summary span{color:var(--kia-muted);font-size:11px}.location-period-summary strong{margin-top:6px;font-size:clamp(15px,1.2vw,19px)}
      .location-daily-toggle{width:100%;min-height:44px;margin-top:16px;padding:0 14px;border:1px solid var(--kia-line);border-radius:8px;background:var(--kia-control);color:var(--kia-text);display:flex;align-items:center;justify-content:space-between;gap:12px;font-weight:800;cursor:pointer}.location-daily-toggle:hover,.location-daily-toggle:focus-visible{border-color:var(--blue)}.location-daily-toggle ha-icon{color:var(--blue);--mdc-icon-size:22px}.location-daily-table{margin-top:10px;border:1px solid var(--kia-line);border-radius:8px;overflow:hidden}.location-daily-table[hidden]{display:none}.location-daily-row{display:grid;grid-template-columns:minmax(115px,1.15fr) repeat(5,minmax(105px,1fr));align-items:center}.location-daily-row>*{min-width:0;padding:11px 12px;border-top:1px solid var(--kia-line);overflow-wrap:anywhere}.location-daily-row:first-child>*{border-top:0}.location-daily-row strong{font-size:13px}.location-daily-row span{color:var(--kia-muted);font-size:12px}.location-daily-header{background:var(--kia-recessed)}.location-daily-header span{color:var(--kia-text);font-weight:800}
      .location-trip-list{display:grid;gap:10px;margin-top:18px}.location-trip-item{padding:16px;border:1px solid var(--kia-line);border-radius:8px;background:var(--kia-control);display:grid;grid-template-columns:minmax(220px,.85fr) minmax(0,1.65fr);gap:20px}.location-trip-route span,.location-trip-route small,.location-trip-metrics small{display:block;color:var(--kia-muted);font-size:11px}.location-trip-route h3{margin:5px 0;font-size:15px;line-height:1.35;overflow-wrap:anywhere}.location-trip-route h3 ha-icon{color:var(--blue);--mdc-icon-size:16px;vertical-align:middle}.location-trip-metrics{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:10px}.location-trip-metrics>span{min-width:0;padding:9px 10px;border-left:2px solid color-mix(in srgb,var(--blue) 45%,var(--kia-line))}.location-trip-metrics strong{display:block;margin-top:4px;font-size:13px;overflow-wrap:anywhere}.location-history-empty{min-height:100px;margin-top:18px;padding:18px;border:1px dashed var(--kia-line);border-radius:8px;background:var(--kia-recessed);display:flex;align-items:center;gap:14px}.location-history-empty>ha-icon{color:var(--blue);--mdc-icon-size:32px}.location-history-empty.warning>ha-icon{color:var(--amber)}.location-history-empty strong,.location-history-empty span{display:block}.location-history-empty span{margin-top:4px;color:var(--kia-muted);font-size:12px}.location-history-note{margin-top:14px;padding-top:13px;border-top:1px solid var(--kia-line);display:flex;align-items:flex-start;gap:8px;color:var(--kia-muted);font-size:12px;line-height:1.45}.location-history-note ha-icon{color:var(--blue);--mdc-icon-size:18px;flex:0 0 auto}
      .location-history-top{display:flex;align-items:flex-start;justify-content:space-between;gap:16px}.trip-calendar-refresh,.trip-calendar-toolbar button,.trip-calendar-today{border:1px solid var(--kia-line);border-radius:8px;background:var(--kia-control);color:var(--kia-text);cursor:pointer}.trip-calendar-refresh{width:42px;height:42px;display:grid;place-items:center;flex:0 0 auto}.trip-calendar-refresh:hover,.trip-calendar-refresh:focus-visible,.trip-calendar-toolbar button:hover,.trip-calendar-toolbar button:focus-visible,.trip-calendar-today:hover,.trip-calendar-today:focus-visible{border-color:var(--blue)}
      .trip-view-toggle{width:min(360px,100%);margin:18px 0 0;display:grid;grid-template-columns:1fr 1fr;padding:4px;border:1px solid var(--kia-line);border-radius:10px;background:var(--kia-recessed)}.trip-view-toggle button{min-height:40px;border:0;border-radius:7px;background:transparent;color:var(--kia-text);font-weight:800;cursor:pointer}.trip-view-toggle button.active{background:color-mix(in srgb,var(--blue) 14%,var(--kia-control));color:var(--blue);box-shadow:inset 0 0 0 1px color-mix(in srgb,var(--blue) 55%,transparent)}
      .trip-calendar-layout{display:grid;grid-template-columns:minmax(360px,.75fr) minmax(0,1.45fr);gap:16px;align-items:stretch;margin-top:18px}.trip-calendar{min-width:0;padding:14px;border:1px solid var(--kia-line);border-radius:10px;background:var(--kia-recessed)}.trip-calendar-toolbar{display:grid;grid-template-columns:40px 1fr 40px;align-items:center;gap:10px}.trip-calendar-toolbar strong{text-align:center;text-transform:capitalize}.trip-calendar-toolbar button{height:38px;display:grid;place-items:center}.trip-calendar-weekdays,.trip-calendar-grid{display:grid;grid-template-columns:repeat(7,minmax(0,1fr));gap:5px}.trip-calendar-weekdays{margin-top:12px}.trip-calendar-weekdays span{text-align:center;color:var(--kia-muted);font-size:11px;font-weight:800;text-transform:uppercase}.trip-calendar-grid{margin-top:7px}.trip-calendar-grid button{position:relative;min-height:52px;padding:6px;border:1px solid transparent;border-radius:8px;background:var(--kia-control);color:var(--kia-text);cursor:pointer}.trip-calendar-grid button.outside{opacity:.4}.trip-calendar-grid button.today{border-color:color-mix(in srgb,var(--blue) 45%,var(--kia-line))}.trip-calendar-grid button.selected{border-color:var(--blue);background:color-mix(in srgb,var(--blue) 15%,var(--kia-control));color:var(--blue)}.trip-calendar-grid button small{position:absolute;right:5px;bottom:4px;min-width:18px;height:18px;padding:0 4px;border-radius:9px;background:var(--blue);color:var(--kia-card);font-size:10px;line-height:18px}.trip-calendar-today{margin-top:10px;min-height:34px;padding:0 13px;font-weight:700}
      .trip-history-content{margin-top:20px}.trip-history-content>h3{font-size:16px}.location-trip-summary{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:9px;margin-top:14px}.location-trip-summary>div{padding:12px;border:1px solid var(--kia-line);border-radius:8px;background:var(--kia-control)}.location-trip-summary span,.location-trip-summary strong{display:block}.location-trip-summary span{color:var(--kia-muted);font-size:11px}.location-trip-summary strong{margin-top:5px;font-size:16px}.location-history-limit{margin-top:12px;color:var(--kia-muted);font-size:12px}
      .trip-calendar-layout>.trip-route-map{margin-top:0;display:grid;grid-template-rows:auto minmax(0,1fr)}.trip-calendar-layout>.trip-route-map .trip-route-map-canvas{height:100%;min-height:0;aspect-ratio:auto}.trip-route-placeholder{min-height:320px;padding:24px;border:1px dashed var(--kia-line);border-radius:10px;background:var(--kia-recessed);display:flex;align-items:center;justify-content:center;gap:14px;color:var(--kia-muted);text-align:left}.trip-route-placeholder>ha-icon{color:var(--blue);--mdc-icon-size:38px}.trip-route-placeholder strong,.trip-route-placeholder span{display:block}.trip-route-placeholder span{margin-top:5px;font-size:12px}
      .location-trip-summary small{display:block;margin-top:3px;color:var(--blue);font-size:10px;font-weight:700}.trip-route-map{margin-top:14px;border:1px solid var(--kia-line);border-radius:10px;overflow:hidden;background:var(--kia-control)}.trip-route-map-heading{padding:11px 13px;display:flex;align-items:center;justify-content:space-between;gap:12px}.trip-route-map-heading span,.trip-route-map-heading strong{display:block}.trip-route-map-heading span,.trip-route-map-heading small{color:var(--kia-muted);font-size:11px}.trip-route-map-heading strong{margin-top:3px;font-size:14px}.trip-route-map-canvas{position:relative;aspect-ratio:1200/430;min-height:240px;overflow:hidden;background:var(--kia-recessed)}.trip-route-map-canvas svg{display:block;width:100%;height:100%}.trip-route-line{fill:none;stroke:var(--blue);stroke-width:7;stroke-linecap:round;stroke-linejoin:round;paint-order:stroke;filter:drop-shadow(0 1px 2px rgba(0,0,0,.35))}.trip-route-line.route-1{stroke:#7e57c2}.trip-route-line.route-2{stroke:#f59e0b}.trip-route-line.route-3{stroke:#ef5350}.trip-route-point{stroke:white;stroke-width:4}.trip-route-point.start{fill:var(--blue)}.trip-route-point.end{fill:#ef5350}.trip-route-quality,.trip-route-map-canvas>a{position:absolute;bottom:7px;padding:4px 7px;border-radius:5px;background:color-mix(in srgb,var(--kia-card) 88%,transparent);font-size:10px;color:var(--kia-text);z-index:2}.trip-route-quality{left:8px}.trip-route-map-canvas>a{right:8px;text-decoration:none}.location-trip-table{margin-top:14px;border:1px solid var(--kia-line);border-radius:10px;overflow-x:auto;background:var(--kia-control)}.location-trip-table-head,.location-trip-row>summary{min-width:1040px;display:grid;grid-template-columns:1.05fr .8fr .8fr .85fr .85fr 1.2fr .9fr .95fr 28px;align-items:center}.location-trip-table-head{background:var(--kia-recessed)}.location-trip-table-head>span{padding:10px 11px;font-size:11px;font-weight:800}.location-trip-row{border-top:1px solid var(--kia-line)}.location-trip-row>summary{list-style:none;cursor:pointer}.location-trip-row>summary::-webkit-details-marker{display:none}.location-trip-row>summary:hover,.location-trip-row>summary:focus-visible{background:color-mix(in srgb,var(--blue) 7%,var(--kia-control))}.location-trip-row>summary>ha-icon{color:var(--blue);transition:transform .2s}.location-trip-row[open]>summary>ha-icon{transform:rotate(180deg)}.trip-cell{min-width:0;padding:11px}.trip-cell small,.trip-cell strong{display:block}.trip-cell small{color:var(--kia-muted);font-size:10px}.trip-cell strong{margin-top:3px;font-size:12px;overflow-wrap:anywhere}.location-trip-detail{min-width:760px;padding:14px;border-top:1px solid var(--kia-line);background:var(--kia-recessed);display:grid;grid-template-columns:minmax(220px,.8fr) minmax(0,1.8fr);gap:18px}.location-trip-detail p{color:var(--kia-muted);font-size:12px}.trip-energy-breakdown{display:grid;grid-template-columns:repeat(5,minmax(100px,1fr));gap:8px}.trip-energy-breakdown>span{padding:9px 10px;border-left:2px solid color-mix(in srgb,var(--blue) 48%,var(--kia-line));background:var(--kia-control)}.trip-energy-breakdown small,.trip-energy-breakdown strong{display:block}.trip-energy-breakdown small{color:var(--kia-muted);font-size:10px}.trip-energy-breakdown strong{margin-top:4px;font-size:12px}
      @media (max-width:980px) { .location-detail { grid-template-columns:1fr 1fr; grid-template-areas:"map map" "summary summary" "parking trip" "daily daily" "history history" "back back"; } .location-detail-map { min-height:500px; } .trip-calendar-layout{grid-template-columns:1fr}.trip-calendar-layout>.trip-route-map .trip-route-map-canvas{min-height:280px;aspect-ratio:1200/430}.location-period-summary,.location-trip-summary{grid-template-columns:1fr 1fr}.location-daily-table{overflow-x:auto}.location-daily-row{min-width:760px}.location-trip-item{grid-template-columns:1fr}.location-trip-metrics{grid-template-columns:repeat(3,minmax(0,1fr))} }
      @media (max-width:640px) { .location-detail { grid-template-columns:1fr; grid-template-areas:"map" "summary" "parking" "trip" "daily" "history" "back"; } .location-detail-map { min-height:0; padding:16px; grid-template-rows:auto minmax(300px,52vh) auto; } .location-detail-summary,.location-context-card,.location-history-section { padding:18px; } .location-stat-grid,.location-trip-stats,.location-period-summary,.location-trip-summary { grid-template-columns:1fr; } .location-trip-metrics{grid-template-columns:1fr 1fr}.location-trip-item{padding:14px}.trip-calendar{padding:10px}.trip-calendar-grid button{min-height:44px;padding:4px}.trip-calendar-grid button small{right:3px;bottom:3px}.location-history-top{gap:8px}.trip-route-map-canvas{min-height:210px}.location-trip-detail{grid-template-columns:1fr}.trip-energy-breakdown{grid-template-columns:1fr 1fr} }
    `;
  }

  _settingsTabStyles() {
    return `
      .settings-detail { margin-top:10px; display:grid; gap:10px; }
      .settings-heading { min-height:108px; padding:22px 26px; display:grid; grid-template-columns:auto minmax(0,1fr) auto; align-items:center; gap:18px; }
      .settings-heading-icon { width:58px; height:58px; border-radius:8px; display:grid; place-items:center; background:color-mix(in srgb,var(--blue) 16%,var(--kia-card)); border:1px solid color-mix(in srgb,var(--blue) 48%,var(--kia-line)); }
      .settings-heading-icon ha-icon { color:var(--blue); --mdc-icon-size:32px; }
      .settings-eyebrow,.settings-panel-title span { display:block; color:var(--blue); font-size:11px; font-weight:800; letter-spacing:.09em; text-transform:uppercase; }
      .settings-heading h2 { margin:2px 0 5px; font-size:clamp(23px,2vw,31px); } .settings-heading p,.settings-panel>p { color:var(--kia-muted); line-height:1.5; }
      .settings-health { min-height:36px; padding:0 12px; border:1px solid; border-radius:999px; display:flex; align-items:center; gap:7px; font-size:12px; font-weight:800; white-space:nowrap; } .settings-health ha-icon { --mdc-icon-size:18px; } .settings-health.healthy { color:var(--green); background:color-mix(in srgb,var(--green) 10%,var(--kia-card)); border-color:color-mix(in srgb,var(--green) 45%,var(--kia-line)); } .settings-health.attention { color:var(--amber); background:color-mix(in srgb,var(--amber) 10%,var(--kia-card)); border-color:color-mix(in srgb,var(--amber) 45%,var(--kia-line)); }
      .settings-grid { display:grid; grid-template-columns:minmax(0,1.15fr) minmax(320px,.85fr); gap:10px; } .settings-panel { padding:22px; min-width:0; }
      .settings-panel-title { display:grid; grid-template-columns:38px minmax(0,1fr) auto; align-items:center; gap:11px; margin-bottom:16px; } .settings-panel-title>ha-icon { color:var(--blue); --mdc-icon-size:28px; } .settings-panel-title h2 { margin-top:2px; font-size:19px; } .settings-panel-title>strong { color:var(--kia-muted); font-size:12px; } .settings-panel-title>strong.healthy-text { color:var(--green); } .settings-panel-title>strong.attention-text { color:var(--amber); } .settings-panel-title>strong.critical-text { color:var(--red); } .settings-panel-title>strong.muted-text { color:var(--kia-muted); }
      .settings-connectivity { grid-column:1/-1; } .settings-connection-grid { display:grid; grid-template-columns:1fr 1fr; gap:10px; } .settings-connection { min-width:0; padding:15px; border:1px solid var(--kia-line); border-left:4px solid var(--kia-muted); border-radius:8px; background:var(--kia-control); display:grid; grid-template-columns:34px minmax(0,1fr); gap:12px; } .settings-connection>ha-icon { color:var(--kia-muted); --mdc-icon-size:29px; } .settings-connection>div>span { color:var(--kia-muted); font-size:11px; font-weight:800; letter-spacing:.06em; text-transform:uppercase; } .settings-connection h3 { margin-top:3px; font-size:18px; } .settings-connection dl { margin:12px 0 0; display:grid; gap:7px; } .settings-connection dl div { display:flex; justify-content:space-between; gap:16px; } .settings-connection dt { color:var(--kia-muted); } .settings-connection dd { margin:0; font-weight:700; text-align:right; } .settings-connection p { margin-top:12px; color:var(--kia-muted); font-size:12px; line-height:1.45; } .settings-connection.healthy { border-left-color:var(--green); } .settings-connection.healthy>ha-icon,.settings-connection.healthy h3 { color:var(--green); } .settings-connection.attention { border-left-color:var(--amber); } .settings-connection.attention>ha-icon,.settings-connection.attention h3 { color:var(--amber); } .settings-connection.critical { border-left-color:var(--red); } .settings-connection.critical>ha-icon,.settings-connection.critical h3 { color:var(--red); }
      .settings-diagnostic { margin-top:18px; padding:14px; border:1px solid var(--kia-line); border-radius:8px; display:grid; grid-template-columns:28px minmax(0,1fr); gap:11px; align-items:start; background:var(--kia-control); } .settings-diagnostic ha-icon { --mdc-icon-size:24px; } .settings-diagnostic.healthy ha-icon { color:var(--green); } .settings-diagnostic.attention ha-icon { color:var(--amber); } .settings-diagnostic strong,.settings-diagnostic span { display:block; } .settings-diagnostic span { margin-top:4px; color:var(--kia-muted); font-size:12px; line-height:1.45; text-transform:capitalize; } .settings-mapping code { color:var(--blue); font:inherit; font-weight:700; }
      .settings-theme-preview { height:72px; display:grid; grid-template-columns:repeat(4,1fr); gap:8px; margin-bottom:14px; } .theme-swatch { border-radius:8px; border:1px solid var(--kia-line); } .theme-swatch.brand { background:var(--blue); } .theme-swatch.success { background:var(--green); } .theme-swatch.warning { background:var(--amber); } .theme-swatch.surface { background:var(--kia-recessed); } .settings-theme dl { margin:0; } .settings-theme dl div { padding:10px 0; border-top:1px solid var(--kia-line); display:flex; justify-content:space-between; gap:20px; } .settings-theme dt { color:var(--kia-muted); } .settings-theme dd { margin:0; font-weight:700; text-align:right; }
      .settings-actions { margin-top:17px; display:grid; grid-template-columns:1fr 1fr; gap:10px; } .settings-actions button { min-height:76px; padding:12px 14px; border:1px solid var(--kia-line); border-radius:8px; background:var(--kia-control); display:grid; grid-template-columns:32px 1fr; gap:11px; align-items:center; text-align:left; } .settings-actions button:hover { border-color:var(--blue); } .settings-actions button:disabled { cursor:not-allowed; opacity:.48; } .settings-actions button ha-icon { color:var(--blue); --mdc-icon-size:27px; } .settings-actions button strong,.settings-actions button small { display:block; } .settings-actions button small { margin-top:3px; color:var(--kia-muted); }
      .settings-notice { margin-top:12px !important; padding:9px 11px; border-radius:8px; color:var(--blue) !important; background:color-mix(in srgb,var(--blue) 10%,var(--kia-card)); border:1px solid color-mix(in srgb,var(--blue) 35%,var(--kia-line)); font-size:12px; } .settings-feedback { display:grid; gap:8px; } .settings-feedback .status-row { min-height:47px; } .settings-note { margin-top:14px !important; padding-top:14px; border-top:1px solid var(--kia-line); display:grid; grid-template-columns:22px 1fr; gap:9px; color:var(--kia-muted); font-size:12px; line-height:1.45; } .settings-note ha-icon { color:var(--blue); --mdc-icon-size:19px; }
      @media (max-width:900px) { .settings-grid,.settings-connection-grid { grid-template-columns:1fr; } .settings-heading { grid-template-columns:auto 1fr; } .settings-health { grid-column:1/-1; justify-self:start; } } @media (max-width:560px) { .settings-heading,.settings-panel { padding:17px; } .settings-heading-icon { width:48px; height:48px; } .settings-actions { grid-template-columns:1fr; } .settings-panel-title { grid-template-columns:32px minmax(0,1fr); } .settings-panel-title>strong { grid-column:2; } .settings-theme dl div,.settings-connection dl div { display:block; } .settings-theme dd,.settings-connection dd { margin-top:3px; text-align:left; } }
    `;
  }

  _tabStyles() {
    return [
      this._batteryTabStyles(),
      this._vehicleTabStyles(),
      this._climateTabStyles(),
      this._energyTabStyles(),
      this._locationTabStyles(),
      this._settingsTabStyles(),
    ].join("");
  }

  _chip(icon, label, value, extra = "") {
    return `<span class="chip ${extra}"><ha-icon icon="${icon}"></ha-icon><span>${label}</span><strong>${this._safe(value)}</strong></span>`;
  }

  _metric(icon, label, value, good = false) {
    return `<div class="metric"><ha-icon icon="${icon}"></ha-icon><div><span>${label}</span><strong class="${good ? "good-text" : ""}">${this._safe(value)}</strong></div></div>`;
  }

  _row(icon, label, value, ok = true) {
    return `<div class="status-row"><ha-icon icon="${icon}"></ha-icon><span>${label}</span><strong>${this._safe(value)}</strong><ha-icon class="${ok ? "ok" : "warn"}" icon="${ok ? "mdi:check-circle-outline" : "mdi:alert-circle-outline"}"></ha-icon></div>`;
  }

  _openClosed(key) {
    return this._active(key) ? "Open" : "Closed";
  }

  _numberControl(entityKey, value, unit, label = "Adjust target") {
    const entityId = this._entity(entityKey) || "";
    if (!entityId.startsWith("number.")) return `<b>${this._safe(value)} ${this._safe(unit)}</b>`;
    const attrs = this._rangeAttrs(entityKey, 50, 100, 1);
    return `<div class="limit-control"><div><b>${this._safe(value)} ${this._safe(unit)}</b><small>${label}</small></div><input data-number="${entityKey}" type="range" min="${attrs.min}" max="${attrs.max}" step="${attrs.step}" value="${value}"></div>`;
  }


  _renderOverviewTab(context) {
    const { battery, batteryPct, range, location, lastUpdated, chargeLimitValue, chargeLimitUnit, dcLimitMarkup, mapTiles, markerImage } = context;
    return `<main class="grid">
          <section class="panel battery-panel">
            <div class="panel-title"><ha-icon icon="mdi:battery-charging"></ha-icon><h2>Battery</h2><button data-nav="battery"><ha-icon icon="mdi:chevron-right"></ha-icon></button></div>
            <div class="battery-gauge"><div class="battery-bar"><span style="width:${batteryPct}%"></span></div><strong>${battery}<small>%</small></strong></div>
            <div class="battery-facts"><div><span>Range</span><b>${range}</b></div><div><span>Est. range AC on</span><b>${range}</b></div><div class="wide"><span>AC charging limit</span>${this._numberControl("charging_limit", chargeLimitValue, chargeLimitUnit)}</div>${dcLimitMarkup}</div>
          </section>

          <section class="panel actions-panel">
            <div class="panel-title"><ha-icon icon="mdi:flash"></ha-icon><h2>Quick Actions</h2><button data-nav="settings"><ha-icon icon="mdi:chevron-right"></ha-icon></button></div>
            <div class="actions"><button data-action="refresh"><ha-icon icon="mdi:refresh"></ha-icon><span>Refresh Data</span></button><button data-action="start_climate"><ha-icon icon="mdi:fan"></ha-icon><span>Start Climate</span></button><button data-action="stop_climate"><ha-icon class="warm" icon="mdi:fan-off"></ha-icon><span>Stop Climate</span></button><button data-action="start_charging"><ha-icon class="good" icon="mdi:ev-plug-type2"></ha-icon><span>Start Charging</span></button></div>
            ${this._notice ? `<p class="notice">${this._safe(this._notice)}</p>` : ""}
          </section>

          <section class="panel vehicle-panel">
            <div class="panel-title"><ha-icon icon="mdi:car-estate"></ha-icon><h2>Vehicle Status</h2><button data-nav="vehicle"><ha-icon icon="mdi:chevron-right"></ha-icon></button></div>
            <div class="vehicle-list">
              ${this._row("mdi:lock-outline", "Doors", this._locked() ? "Closed" : "Open", this._locked())}
              ${this._row("mdi:car-door", "Windows", "Closed", true)}
              ${this._row("mdi:car-back", "Trunk", this._openClosed("trunk"), !this._active("trunk"))}
              ${this._row("mdi:car-cog", "Hood", this._openClosed("hood"), !this._active("hood"))}
              ${this._row("mdi:car-light-high", "Lights", this._active("lights") ? "On" : "Off", true)}
              ${this._row("mdi:ev-plug-type2", "Charge port", this._active("charge_port") ? "Open" : "Closed", true)}
            </div>
          </section>

          <section class="panel location-panel">
            <div class="panel-title"><ha-icon icon="mdi:map-marker-outline"></ha-icon><h2>Location</h2><button data-nav="location"><ha-icon icon="mdi:chevron-right"></ha-icon></button></div>
            <div class="location-layout"><div class="map">${mapTiles ? `<div class="map-tiles" style="${mapTiles.style}">${mapTiles.tiles}</div>` : ""}<span class="map-marker"><img src="${markerImage}" alt="EV6 location" onerror="this.style.display='none';this.nextElementSibling.style.display='block'"><ha-icon icon="mdi:car-sports"></ha-icon></span></div><div class="location-meta"><span>Last parked</span><b>${this._safe(location)}</b></div></div>
          </section>

          <section class="panel tire-panel">
            <div class="panel-title"><ha-icon icon="mdi:car-tire-alert"></ha-icon><h2>Tire Status</h2><button data-nav="vehicle"><ha-icon icon="mdi:chevron-right"></ha-icon></button></div>
            <div class="tires"><div class="tire-side"><b>${this._tireStatus("tire_front_left")}</b><span>Front left</span><b>${this._tireStatus("tire_rear_left")}</b><span>Rear left</span></div><img src="/local/vehicles/ev6_top.png" alt="Top view"><div class="tire-side"><b>${this._tireStatus("tire_front_right")}</b><span>Front right</span><b>${this._tireStatus("tire_rear_right")}</b><span>Rear right</span></div></div>
          </section>

          <section class="panel health-panel"><ha-icon class="shield" icon="mdi:shield-check-outline"></ha-icon><div><h2>All systems normal</h2><p>No active dashboard warnings. Review Vehicle and Settings after each Home Assistant update.</p></div><ha-icon class="ghost" icon="mdi:shield-check-outline"></ha-icon></section>
        </main>

        <footer class="footer card"><span><ha-icon icon="mdi:information-outline"></ha-icon>Data provided by Kia Connect</span><span>Updated ${lastUpdated}</span><button data-action="refresh"><ha-icon icon="mdi:refresh"></ha-icon></button></footer>`;
  }

  _renderActiveTab(context) {
    const renderers = {
      overview: this._renderOverviewTab,
      battery: this._renderBatteryTab,
      vehicle: this._renderVehicleTab,
      climate: this._renderClimateTab,
      energy: this._renderEnergyTab,
      location: this._renderLocationTab,
      settings: this._renderSettingsTab,
    };
    return (renderers[this._activeTab] || renderers.overview).call(this, context);
  }

  _render() {
    if (!this.shadowRoot) return;

    const title = this._config.title || "Kia EV6";
    const battery = this._number("battery_level", 0);
    const batteryPct = Math.max(0, Math.min(100, Number(battery) || 0));
    const range = `${this._number("battery_range")} ${this._unit("battery_range", "km")}`;
    const odometer = `${this._number("odometer")} ${this._unit("odometer", "km")}`;
    const location = this._state("location", "Home");
    const lastUpdated = this._formatDate(this._state("last_updated", "--"));
    const chargingText = this._charging() ? "Charging" : "Not charging";
    const climateText = this._climateOn() ? "On" : "Off";
    const chargeLimitValue = this._number("charging_limit", 100);
    const chargeLimitUnit = this._unit("charging_limit", "%") || "%";
    const dcChargeLimitValue = this._number("dc_charging_limit", 100);
    const dcChargeLimitUnit = this._unit("dc_charging_limit", "%") || "%";
    const dcLimitMarkup = this._entity("dc_charging_limit")
      ? `<div class="wide"><span>DC charging limit</span>${this._numberControl("dc_charging_limit", dcChargeLimitValue, dcChargeLimitUnit)}</div>`
      : "";
    const lockedText = this._locked() ? "Locked" : "Unlocked";
    const vehicleConnection = this._vehicleConnectionHealth();
    const vehicleConnectionClass = vehicleConnection.tone === "healthy" ? "online" : vehicleConnection.tone;
    const mapTiles = this._mapTileGrid();
    const markerImage = this._asset(this._config.images?.map_marker || "ev6_top.png");

    this.shadowRoot.innerHTML = this._localize(`
      <style>${this._styles()}${this._tabStyles()}</style>
      <ha-card class="kia-shell">
        <section class="hero card">
          <div class="car-stage"><img src="${this._carImage()}" alt="${this._safe(title)}" onerror="this.src='/local/vehicles/ev6_side.png'"></div>
          <div class="divider"></div>
          <div class="hero-data">
            ${this._metric("mdi:speedometer", "Odometer", odometer)}
            ${this._metric("mdi:ev-plug-type2", "Charging state", chargingText, this._charging())}
            ${this._metric("mdi:calendar-clock", "Last updated", lastUpdated)}
            ${this._metric("mdi:thermometer", "Climate", climateText)}
          </div>
          <div class="status-stack">
            <button class="chip" data-info="door_lock"><ha-icon icon="mdi:lock"></ha-icon><span>Lock</span><strong>${lockedText}</strong></button>
            ${this._chip(vehicleConnection.icon, "Connection", vehicleConnection.label, vehicleConnectionClass)}
            ${this._chip("mdi:battery", "Battery", `${battery} %`)}
          </div>
        </section>

        <nav class="section-nav card">
          <div class="nav-items">
            ${this._nav("mdi:view-dashboard-outline", "Overview", "overview")}
            ${this._nav("mdi:battery-charging", "Battery", "battery")}
            ${this._nav("mdi:car", "Vehicle", "vehicle")}
            ${this._nav("mdi:fan", "Climate", "climate")}
            ${this._nav("mdi:chart-line", "Energy", "energy")}
            ${this._nav("mdi:map-marker-outline", "Location", "location")}
            ${this._nav("mdi:tune", "Settings", "settings")}
          </div>
        </nav>

        ${this._renderActiveTab({ battery, batteryPct, range, location, lastUpdated, chargeLimitValue, chargeLimitUnit, dcLimitMarkup, mapTiles, markerImage })}
      </ha-card>`);

    this.shadowRoot.querySelectorAll("[data-nav]").forEach((el) => el.addEventListener("click", () => this._navigate(el.dataset.nav)));
    this.shadowRoot.querySelectorAll("[data-info]").forEach((el) => el.addEventListener("click", () => this._moreInfo(el.dataset.info)));
    this.shadowRoot.querySelectorAll("[data-action]").forEach((el) => el.addEventListener("click", () => this._handleAction(el.dataset.action)));
    this.shadowRoot.querySelectorAll("[data-entity-action]").forEach((el) => el.addEventListener("click", () => this._callEntity(el.dataset.entityAction, el.dataset.service || "turn_on", el.dataset.confirm || "")));
    this.shadowRoot.querySelectorAll("[data-number]").forEach((el) => el.addEventListener("change", () => this._setNumber(el.dataset.number, el.value, el.dataset.confirm || "")));
    this.shadowRoot.querySelectorAll("[data-select]").forEach((el) => el.addEventListener("click", () => this._setSelect(el.dataset.select, el.dataset.option)));
    this.shadowRoot.querySelectorAll("[data-daily-history-toggle]").forEach((el) => el.addEventListener("click", () => this._toggleDailyHistory()));
    this.shadowRoot.querySelectorAll("[data-trip-view]").forEach((el) => el.addEventListener("click", () => this._setTripView(el.dataset.tripView)));
    this.shadowRoot.querySelectorAll("[data-trip-month]").forEach((el) => el.addEventListener("click", () => this._shiftTripMonth(Number.parseInt(el.dataset.tripMonth, 10) || 0)));
    this.shadowRoot.querySelectorAll("[data-trip-date]").forEach((el) => el.addEventListener("click", () => this._selectTripDate(el.dataset.tripDate)));
    this.shadowRoot.querySelectorAll("[data-trip-refresh]").forEach((el) => el.addEventListener("click", () => this._loadTripCalendar(true)));
    this.shadowRoot.querySelectorAll("[data-charger-history-period]").forEach((el) => el.addEventListener("click", () => this._setChargerHistoryPeriod(Number.parseInt(el.dataset.chargerHistoryPeriod, 10))));
    this.shadowRoot.querySelectorAll("[data-charger-history-refresh]").forEach((el) => el.addEventListener("click", () => this._loadChargerHistory(true)));
    this.shadowRoot.querySelectorAll("[data-charger-history-toggle]").forEach((el) => el.addEventListener("click", () => this._toggleChargerHistory()));
  }

  _styles() {
    return `
      :host {
        display:block;
        --kia-bg:var(--primary-background-color,#080d13);
        --kia-card:var(--card-background-color,#141b24);
        --kia-panel:color-mix(in srgb,var(--kia-card) 92%,var(--kia-bg));
        --kia-control:color-mix(in srgb,var(--kia-card) 86%,var(--primary-color,#42c8ff) 6%);
        --kia-recessed:color-mix(in srgb,var(--kia-bg) 88%,#000 12%);
        --kia-line:var(--divider-color,#21384b);
        --kia-text:var(--primary-text-color,#f5f8fb);
        --kia-muted:var(--secondary-text-color,#aab7c5);
        --blue:var(--primary-color,#42c8ff);
        --green:var(--success-color,#64f276);
        --amber:var(--warning-color,#ffd15a);
        --red:var(--error-color,#ff5252);
        font-family:var(--primary-font-family, Inter, system-ui, sans-serif);
      }
      ha-card.kia-shell { background:var(--kia-bg); color:var(--kia-text); border:0; box-shadow:var(--ha-card-box-shadow,none); padding:clamp(10px,1.1vw,18px); }
      .card,.panel { background:var(--kia-card); background:linear-gradient(145deg, var(--kia-panel), var(--kia-card)); border:1px solid var(--kia-line); border-radius:8px; box-shadow:var(--ha-card-box-shadow,0 8px 22px rgba(0,0,0,.14)); }
      button { font:inherit; color:inherit; cursor:pointer; } h2,p { margin:0; } .good-text { color:var(--green) !important; }
      .hero { min-height:clamp(230px,23vw,330px); display:grid; grid-template-columns:minmax(420px,1.05fr) 1px minmax(460px,.88fr) minmax(172px,210px); align-items:center; gap:clamp(18px,2.4vw,40px); padding:22px clamp(28px,4.5vw,88px); overflow:hidden; }
      .car-stage { min-width:0; display:flex; justify-content:flex-start; align-items:center; } .car-stage img { width:min(760px,100%); height:clamp(190px,22vw,300px); object-fit:contain; object-position:left center; filter:drop-shadow(0 18px 16px rgba(0,0,0,.25)); }
      .divider { height:70%; background:var(--kia-line); opacity:.72; } .hero-data { justify-self:start; width:min(640px,100%); display:grid; grid-template-columns:1fr 1fr; gap:30px 48px; } .metric { display:grid; grid-template-columns:42px 1fr; gap:12px; align-items:center; min-width:0; } .metric ha-icon { color:var(--kia-muted); --mdc-icon-size:34px; } .metric span,.battery-facts span,.location-layout span,.tire-side span { color:var(--kia-muted); font-size:14px; line-height:1.2; } .metric strong { display:block; font-size:clamp(16px,1.1vw,20px); line-height:1.16; overflow-wrap:anywhere; }
      .section-nav { margin-top:10px; padding:10px 18px 16px; border-bottom:3px solid var(--blue); } .nav-items { display:grid; grid-template-columns:repeat(7,minmax(0,1fr)); gap:14px; } .status-stack { display:grid; grid-template-columns:1fr; gap:8px; align-self:center; justify-self:end; width:100%; }
      .nav-tile,.actions button { min-height:84px; border-radius:8px; border:1px solid var(--kia-line); background:var(--kia-control); display:flex; flex-direction:column; align-items:center; justify-content:center; gap:8px; font-weight:700; } .nav-tile ha-icon { color:var(--blue); --mdc-icon-size:32px; } .nav-tile.active { border-color:var(--blue); background:color-mix(in srgb,var(--blue) 18%,var(--kia-card)); box-shadow:inset 0 -3px 0 var(--blue); } .nav-tile.active span { color:var(--blue); }
      .chip { min-height:40px; padding:0 12px; border-radius:8px; border:1px solid var(--kia-line); background:color-mix(in srgb,var(--blue) 14%,var(--kia-card)); display:grid; grid-template-columns:22px 1fr auto; align-items:center; gap:8px; font-weight:700; font-size:13px; text-align:left; } .chip ha-icon { color:var(--blue); --mdc-icon-size:18px; } .chip span { color:var(--kia-muted); } .chip.online { background:color-mix(in srgb,var(--green) 18%,var(--kia-card)); } .chip.online ha-icon,.chip.online strong { color:var(--green); } .chip.attention { background:color-mix(in srgb,var(--amber) 14%,var(--kia-card)); } .chip.attention ha-icon,.chip.attention strong { color:var(--amber); } .chip.critical { background:color-mix(in srgb,var(--red) 12%,var(--kia-card)); } .chip.critical ha-icon,.chip.critical strong { color:var(--red); } .chip.muted ha-icon,.chip.muted strong { color:var(--kia-muted); }
      .grid { margin-top:12px; display:grid; grid-template-columns:1fr 1fr 1.28fr; grid-template-areas:"battery actions vehicle" "location location tires" "location location health"; gap:12px; align-items:stretch; } .panel { min-height:160px; padding:18px 22px; position:relative; overflow:hidden; } .battery-panel{grid-area:battery}.actions-panel{grid-area:actions}.vehicle-panel{grid-area:vehicle}.location-panel{grid-area:location;min-height:340px}.tire-panel{grid-area:tires}.health-panel{grid-area:health}
      .panel-title { display:flex; align-items:center; gap:12px; margin-bottom:12px; } .panel-title h2 { flex:1; font-size:20px; } .panel-title ha-icon { color:var(--blue); } .panel-title button,.footer button { border:0; background:transparent; padding:0; color:var(--kia-muted); }
      .battery-gauge { display:grid; grid-template-columns:minmax(0,1fr) auto; align-items:center; gap:18px; margin:4px 0 18px; } .battery-gauge strong { font-size:clamp(28px,2.25vw,40px); line-height:1; white-space:nowrap; } .battery-gauge small { font-size:.48em; margin-left:4px; } .battery-bar { height:34px; border-radius:8px; padding:5px; background:var(--kia-recessed); border:1px solid var(--kia-line); overflow:hidden; } .battery-bar span { display:block; height:100%; border-radius:6px; background:linear-gradient(90deg,var(--red) 0 30%,var(--amber) 30% 55%,var(--green) 55% 100%); }
      .battery-facts { display:grid; grid-template-columns:repeat(2,minmax(0,1fr)); gap:10px 18px; } .battery-facts div { min-width:0; } .battery-facts .wide { grid-column:1 / -1; } .battery-facts b { display:block; font-size:clamp(15px,1.1vw,19px); line-height:1.1; overflow-wrap:anywhere; } .limit-control { display:grid; grid-template-columns:auto minmax(120px,1fr); gap:14px; align-items:center; } .limit-control small { display:block; color:var(--kia-muted); font-size:12px; margin-top:2px; } .limit-control input { width:100%; accent-color:var(--blue); }
      .actions { display:grid; grid-template-columns:repeat(2,minmax(0,1fr)); gap:12px; } .actions ha-icon { color:var(--blue); --mdc-icon-size:34px; } .actions .warm { color:var(--amber); } .actions .good { color:var(--green); } .notice { margin-top:12px; color:var(--kia-muted); font-size:13px; line-height:1.35; }
      .vehicle-list { display:grid; gap:13px; padding-inline:8px; } .status-row { display:grid; grid-template-columns:30px 1fr auto 28px; align-items:center; gap:12px; color:var(--kia-muted); } .status-row strong { color:var(--kia-text); } .status-row ha-icon { color:var(--kia-muted); } .status-row .ok { color:var(--green); } .status-row .warn { color:var(--amber); }
      .location-layout { display:grid; grid-template-columns:1fr; grid-template-rows:minmax(260px,1fr) auto; gap:12px; align-items:stretch; height:100%; } .map { min-height:clamp(280px,22vw,380px); border-radius:8px; background:color-mix(in srgb,var(--kia-control) 62%,var(--blue) 8%); display:grid; place-items:center; position:relative; overflow:hidden; isolation:isolate; } .map-tiles { position:absolute; left:0; top:0; width:var(--map-size,1280px); height:var(--map-size,1280px); display:grid; grid-template-columns:repeat(var(--map-grid,5),256px); grid-template-rows:repeat(var(--map-grid,5),256px); transform-origin:0 0; z-index:0; filter:saturate(1.18) contrast(1.08); } .map-tiles img,.map-tile-empty { width:256px; height:256px; display:block; } .map-tile-empty { background:var(--kia-control); } .map:before { content:""; position:absolute; inset:0; background:color-mix(in srgb,var(--kia-card) 4%,transparent); pointer-events:none; z-index:1; } .map-marker { width:46px; height:46px; border-radius:0; background:transparent; border:0; display:grid; place-items:center; box-shadow:none; z-index:2; } .map-marker img { width:42px; height:42px; object-fit:contain; filter:drop-shadow(0 4px 6px rgba(0,0,0,.34)); } .map-marker ha-icon { display:none; color:var(--blue); --mdc-icon-size:28px; } .location-meta { display:grid; grid-template-columns:1fr auto; gap:6px 16px; align-items:end; } .location-meta span { grid-column:1 / -1; } .location-meta b { display:block; font-size:20px; margin:0; }
      .tires { display:grid; grid-template-columns:1fr 86px 1fr; align-items:center; gap:18px; } .tires img { width:86px; height:136px; object-fit:contain; justify-self:center; } .tire-side { display:grid; gap:3px; } .tire-side b { font-size:18px; } .tire-side b:before { content:""; display:inline-block; width:8px; height:8px; border-radius:50%; background:var(--green); margin-right:8px; box-shadow:0 0 7px var(--green); } .tire-side:first-child { text-align:right; }
      .health-panel { display:flex; align-items:center; gap:26px; } .shield { color:var(--green); --mdc-icon-size:56px; } .health-panel h2 { font-size:22px; } .health-panel p { color:var(--kia-muted); margin-top:6px; } .ghost { position:absolute; right:28px; bottom:18px; opacity:.12; --mdc-icon-size:72px; }
      .footer { margin-top:12px; min-height:44px; padding:0 16px; display:flex; align-items:center; justify-content:space-between; color:var(--kia-muted); } .footer span { display:flex; align-items:center; gap:8px; }
      .detail-placeholder { margin-top:12px; min-height:clamp(300px,38vw,560px); padding:clamp(28px,5vw,72px); display:flex; align-items:center; justify-content:center; gap:24px; text-align:left; } .detail-placeholder>ha-icon { color:var(--blue); --mdc-icon-size:64px; } .detail-placeholder span { color:var(--blue); font-size:13px; font-weight:800; letter-spacing:.08em; text-transform:uppercase; } .detail-placeholder h2 { margin-top:6px; font-size:clamp(28px,3vw,44px); } .detail-placeholder p { max-width:620px; margin-top:10px; color:var(--kia-muted); line-height:1.5; }
      @media (max-width:1180px){.hero{grid-template-columns:1fr;gap:16px}.divider{display:none}.hero-data{grid-template-columns:repeat(2,minmax(0,1fr))}.status-stack{grid-template-columns:repeat(3,minmax(0,1fr));justify-self:stretch}.grid{grid-template-columns:1fr 1fr;grid-template-areas:"battery actions" "vehicle vehicle" "location location" "tires health"}.location-panel{min-height:auto}.location-layout{grid-template-rows:auto auto;height:auto}.map{min-height:clamp(260px,36vw,360px)}.nav-items{grid-template-columns:repeat(3,1fr)}}
      @media (max-width:760px){ha-card.kia-shell{padding:10px}.chip{min-height:36px;padding:0 10px;font-size:12px}.hero{padding:18px;min-height:0}.car-stage img{height:210px;object-position:center}.hero-data,.grid{grid-template-columns:1fr}.grid{grid-template-areas:"battery" "actions" "vehicle" "location" "tires" "health"}.nav-items{grid-template-columns:repeat(2,1fr)}.status-stack{grid-template-columns:1fr}.battery-gauge,.battery-facts,.limit-control{grid-template-columns:1fr}.location-layout{height:auto}.footer{flex-direction:column;align-items:flex-start;padding:12px 16px;gap:8px}.detail-placeholder{min-height:320px;flex-direction:column;text-align:center}}
    `;
  }
}

customElements.define("kia-dashboard-card", KiaDashboardCard);
window.customCards = window.customCards || [];
window.customCards.push({ type: "kia-dashboard-card", name: "Kia Dashboard Card", description: "Responsive Kia EV6 overview card for Home Assistant.", version: KIA_DASHBOARD_CARD_VERSION });
